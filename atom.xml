<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>#hsfzxjy#</title>
  
  <subtitle>言えない言葉は、ここに隠される</subtitle>
  <link href="http://i.hsfzxjy.site/atom.xml" rel="self"/>
  
  <link href="http://i.hsfzxjy.site/"/>
  <updated>2021-06-11T16:41:27.642Z</updated>
  <id>http://i.hsfzxjy.site/</id>
  
  <author>
    <name>hsfzxjy</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Where does the randomness of CUDA kernels come from?</title>
    <link href="http://i.hsfzxjy.site/2021-06-11-where-does-the-randomness-of-cuda-kernels-come-from/"/>
    <id>http://i.hsfzxjy.site/2021-06-11-where-does-the-randomness-of-cuda-kernels-come-from/</id>
    <published>2021-06-10T16:00:00.000Z</published>
    <updated>2021-06-11T16:41:27.642Z</updated>
    
    <content type="html"><![CDATA[<p>You might have heard that many CUDA operators have some kind of non-determinism, and to eliminate the randomness, one must pay for the degradation of performance. We have been warned many times by blog posts or framework documentation, but few of them give a detailed explanation for the source of randomness. This post is going to explore the problem.</p><p>When talked about GPU computation, one may have an impression of some super-fast hardwares. The surprising speed comes from intensive parallelism of the architecture, which allows users to run thousands of routines on parallel (by contrast the number is dozens on ordinary CPUs). The routines are called <strong>threads</strong>, and similar to the concept with the same name in operating systems, they suffer from <strong>non-deterministic execution order</strong> and <strong>data race condition</strong>.</p><p>Non-deterministic execution order means, if we arrange all instructions of different threads into a sequence, ordered by their occurrence time, the sequence could vary greatly across executions. If two threads run on parallel, each with a single instruction, we cannot tell which one is executed first. This is the fundamental origin of randomness, and is inevitable.</p><p>Data race condition is one of the consequences of non-deterministic execution order. When the threads is manipulate some shared variables, and the manipulation is not <em>atomic</em>, i.e. consists of interruptible instruction sequence, the program might yield undesired results. Programs should be carefully designed to avoid race condition, with the help of locks or atomic operations. CUDA supports provides atomic arithmetic routines like <code>atomicAdd()</code> or <code>atomicMax()</code> for safe access to shared memory.</p><p>By far we have seen that there does exist some kind of randomness inside GPUs, and if not handled properly, our program will give incorrect results when working with shared variables. One may argue that, we have atomic operations like <code>atomicAdd()</code>, and if the program correctly sums up the same collection of numbers, although the order might not be guaranteed, it should always returns the same result. But this is wrong – some arithmetic operations <strong>does rely on the order of operands</strong>. Take the following CUDA program as an example:</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function">__global__ <span class="keyword">void</span> <span class="title">sum</span><span class="params">(<span class="keyword">float</span> *result)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">float</span> i = (<span class="keyword">float</span>)blockIdx.x;</span><br><span class="line">    atomicAdd(result, i / <span class="number">10</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> N = <span class="number">1000</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">float</span> *cu_result;</span><br><span class="line">    cudaMalloc(&amp;cu_result, <span class="keyword">sizeof</span>(<span class="keyword">float</span>));</span><br><span class="line">    <span class="keyword">float</span> result;</span><br><span class="line">    <span class="keyword">int</span>   i;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">        cudaMemset(cu_result, <span class="number">0</span>, <span class="keyword">sizeof</span>(<span class="keyword">float</span>));</span><br><span class="line">        sum&lt;&lt;&lt;N, <span class="number">1</span>&gt;&gt;&gt;(cu_result);</span><br><span class="line">        cudaMemcpy(&amp;result, cu_result, <span class="keyword">sizeof</span>(<span class="keyword">float</span>), cudaMemcpyDeviceToHost);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%f\n&quot;</span>, result);</span><br><span class="line">    &#125;</span><br><span class="line">    cudaFree(cu_result);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> In this simple program, we implement a parallelized summing function with <code>atomicAdd()</code>. We sum up $\{0.0,0.1,0.2,\ldots,99.8,99.9\}$ for 10 times and print out the results. The results should look like:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">49949.996094</span><br><span class="line">49950.003906</span><br><span class="line">49950.000000</span><br><span class="line">49950.000000</span><br><span class="line">49949.996094</span><br><span class="line">49950.000000</span><br><span class="line">49949.988281</span><br><span class="line">49949.992188</span><br><span class="line">49949.996094</span><br><span class="line">49950.000000</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> which is completely non-deterministic with some random small errors (the accurate answer should be 49950).</p><p>The key is that, <strong>floating-point arithemtics are non-associative</strong>. The equation $(a + b) + c = a + (b + c)$ does NOT hold for floating-point numbers $a, b, c$. </p><p>To understand this, let’s consider an extreme example – a very simple decimal floating-point representation with no fraction digits, i.e., numbers are represented as $\pm b \times 10^{e}$, where $b \in \{0 \ldots 9\}$. We will calculate $(0.8+1.5)-1.6$ and $0.8+(1.5-1.6)$ under this kind of “specification”. Below are their representation:</p>$$\begin{aligned}0.8 &\rightarrow 8 \times 10^{-1} \\1.5 &\rightarrow 2 \times 10^{0}  \\1.6 &\rightarrow 2 \times 10^{0} \\\end{aligned}$$<p style="margin-top: 0;"> For $(0.8 + 1.5)-1.6$, we first shift the base of $8 \times 10^{-1}$ by 1 digit and get $0.8 \times 10^{0}$. Due to an underflow, it is truncated and becomes $0 \times 10^{0}$. Then sum up $0 \times 10^{0}$ (0.8) and $2 \times 10^{0}$ (1.5) and get $2 \times 10^{0}$. Further $2 \times 10^{0} - 2 \times 10^{0}$ (1.6), results in $0 \times 10^{0}$.</p><p style="margin-top: 0;"> For $0.8 + (1.5-1.6)$, first calculate $2 \times 10^0$ (1.5) $-2 \times 10^0$ (1.6) and get $0 \times 10^{0}$. Then $8 \times 10^{-1}$ (0.8) adding with $0 \times 10^0$, results in $8 \times 10^{-1}$.</p><p>The example showcases a situation that floating-point underflow during summation could lead to extra numeric error, and further causes non-associativity in the arithemtic. It is simple, but can be generalized to the current IEEE 754 floating-point numbers. <em>The operands’ order matters in floating-point arithemtics!</em></p><p>So here’s the answer. When running parallelized routines, GPUs have some innate randomness which comes from non-deterministic execution order. If a program consists of order-dependent operations, it will give non-deterministic output. Floating-point arithemtics, used widely in CUDA algorithms, are also such kind of operations, though less known by people. Atomic arithemtics solve the race condition problem, but do not guarantee the order. </p><p>If one requires determinism, the program should sequentialize the operations by using single thread or special synchronization, which will lead to a performance degrade. There is always a trade-off between determinism and performance.</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;You might have heard that many CUDA operators have some kind of non-determinism, and to eliminate the randomness, one must pay for the de</summary>
      
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="CUDA" scheme="http://i.hsfzxjy.site/tags/CUDA/"/>
    
    <category term="Determinism" scheme="http://i.hsfzxjy.site/tags/Determinism/"/>
    
  </entry>
  
  <entry>
    <title>Performant Bulk Mutations in IndexedDB</title>
    <link href="http://i.hsfzxjy.site/2021-05-30-indexeddb-performant-bulk-mutations/"/>
    <id>http://i.hsfzxjy.site/2021-05-30-indexeddb-performant-bulk-mutations/</id>
    <published>2021-05-29T16:00:00.000Z</published>
    <updated>2021-06-03T07:22:14.218Z</updated>
    
    <content type="html"><![CDATA[<p>IndexedDB seems to be inefficient when performing bulk mutations, like dumping a huge list of items into an object store – at least I think so at the first sight on the MDN docs. It provides no explicit API for the job as SQL does (you may add as many records after the <code>INSERT INTO ... VALUES</code> clause), so all we can do is to use a loop from client, and thus cannot benefit from database internal optimization (if there’s any). The mutation requests, in addition, appear to be spawned sequentially only. The tutorial recommends a paradigm to raise a request within the <code>success</code> event callback of the previous request, which is in fact a sequential execution. Such code will be definitely slow.</p><p>We may conduct a quick benchmark on the above approach:</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">;(<span class="keyword">async</span> () =&gt; &#123;</span><br><span class="line">    <span class="keyword">await</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">const</span> r = indexedDB.deleteDatabase(<span class="string">&quot;test&quot;</span>)</span><br><span class="line">        r.onsuccess = r.onerror = resolve</span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="keyword">const</span> items = <span class="built_in">Array</span>.from(&#123; <span class="attr">length</span>: <span class="number">100000</span> &#125;, <span class="function">(<span class="params">_, i</span>) =&gt;</span> (&#123; <span class="attr">id</span>: i &#125;))</span><br><span class="line">    <span class="keyword">const</span> store = <span class="keyword">await</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">        indexedDB.open(<span class="string">&quot;test&quot;</span>, <span class="number">1</span>).onupgradeneeded = <span class="function">(<span class="params">event</span>) =&gt;</span> &#123;</span><br><span class="line">            <span class="keyword">const</span> db = event.target.result</span><br><span class="line">            <span class="keyword">const</span> store = db.createObjectStore(<span class="string">&quot;store&quot;</span>, &#123; <span class="attr">keyPath</span>: <span class="string">&quot;id&quot;</span> &#125;)</span><br><span class="line">            store.createIndex(<span class="string">&quot;id&quot;</span>, <span class="string">&quot;id&quot;</span>)</span><br><span class="line">            resolve(store)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    <span class="built_in">console</span>.time(<span class="string">&quot;bulkAdd&quot;</span>)</span><br><span class="line">    <span class="keyword">await</span> bulkAdd(store, items)</span><br><span class="line">    <span class="built_in">console</span>.timeEnd(<span class="string">&quot;bulkAdd&quot;</span>)</span><br><span class="line">&#125;)()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bulkAdd</span>(<span class="params">store, items</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> failures = []</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="function"><span class="keyword">function</span> <span class="title">_perform</span>(<span class="params">idx</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">const</span> req = store.add(items[idx])</span><br><span class="line">            req.onsuccess = <span class="function">(<span class="params">event</span>) =&gt;</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (idx === items.length - <span class="number">1</span>) resolve(failures)</span><br><span class="line">                <span class="keyword">else</span> _perform(idx + <span class="number">1</span>)</span><br><span class="line">            &#125;</span><br><span class="line">            req.onerror = <span class="function">(<span class="params">event</span>) =&gt;</span> &#123;</span><br><span class="line">                failures.push(items[idx].id)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        _perform(<span class="number">0</span>)</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> Practically, we concern more about failed records than the ones inserted successfully. We thus only take down the indices of those records, which improves the efficiency at least a little bit.</p><p>The timing is rather unstable, but on average, it takes 30~40 seconds to insert 100k records or 2000~3000 records per second, which is not an ideal speed.</p><a id="more"></a><p>So why should the requests be aranged like this? I found a statement in the <a href="https://developer.mozilla.org/en-US/docs/Web/API/IDBTransaction/commit">MDN docs</a>:</p><blockquote><p style="text-indent: 0;"> Note that <code>commit()</code> doesn’t normally have to be called — a transaction will automatically commit when all outstanding requests have been satisfied and no new requests have been made. <code>commit()</code> can be used to start the commit process without waiting for events from outstanding requests to be dispatched.</p></blockquote><p style="text-indent: 0; margin-top: 0;"> Unlike many other databases, IndexedDB transaction implicitly commits changes, at the instant when <em>all callbacks of previous requests are invoked (and returned), and there’s no more new requests</em>. Hence a later request must be raised before the previous one completed or within its <code>success</code> or <code>error</code> callbacks, otherwise the transaction will be closed. Raising new requests in callbacks is safe, but not very efficient.</p><p>Can we do it better? Absolutely. We can perform the mutations “concurrently”, that is, raising all requests at a time.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bulkAdd</span>(<span class="params">store, items</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> failures = []</span><br><span class="line">    <span class="keyword">const</span> promises = []</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> item <span class="keyword">of</span> items) &#123;</span><br><span class="line">        <span class="keyword">const</span> req = store.add(item)</span><br><span class="line">        promises.push(</span><br><span class="line">            <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">                req.onsuccess = resolve</span><br><span class="line">                req.onerror = <span class="function">() =&gt;</span> &#123;</span><br><span class="line">                    failures.push(item.id)</span><br><span class="line">                    resolve()</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;)</span><br><span class="line">        )</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Promise</span>.all(promises).then(<span class="function">() =&gt;</span> failures)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> The execution time is ~15 seconds now, around 2x faster than the first implementation.</p><p>But wait … is this implementation reliable? Is it possible that at some point all previous requests are completed, but there’s still new requests to be raised? The answer is yes, but the transaction won’t be closed for this, since <em>the callbacks have not yet been invoked</em>.</p><p>Recall that Javascript has a single-threaded execution model. All callbacks reside in a queue and executed sequentially when the thread is idle. The mechanism ensures that mutation request callbacks won’t be executed before all requests raised, as the raising operations and callbacks are executed in the same thread. So even if some requests complete early, their <code>success</code> or <code>error</code> callbacks will still be executed after all requests are created. The implementation is definitely reliable.</p><p>Now we have reached a speed of ~6000 records per second. Can we make it even better? Of course yes!</p><p>If you read the code carefully, you may notice that <code>req.onsuccess</code> is doing something vain. A <code>resolve</code> function is passed as <code>success</code> callback, just to inform the timing of request completion. However, we are not interested about the timing of each request completion. Instead, only the timing of last completion is important.</p><p>We may expect the callbacks be executed in the order they are raised. They do actually. But weirdly, the MDN docs does not advertise such point, and we have to dive into the <a href="https://www.w3.org/TR/IndexedDB-3/">IndexedDB Specification</a> to find the clues. After struggling against the verbose documentation, I observe something at section <a href="https://www.w3.org/TR/IndexedDB-3/#async-execute-request">Asynchronously executing a request</a>. Briefly speaking,</p><ul><li>  A request won’t be processed before previous requests’ <code>processed</code> flags be set. (See <em>5.6.5.1 Wait until request is the first item in transaction’s request list that is not processed.</em>)</li><li>  A request will immediately queue a task after its <code>processed</code> flag set, within which the callbacks are invoked. (See <em>5.6.5.6: Queue a task to run these steps</em>)</li></ul><p style="text-indent: 0; margin-top: 0;"> which means, the tasks for firing events will have an order as the raised requests. Consequently, we might be able to know the timing of all completions, by only listening to the <code>success</code>/<code>error</code> events of the last request:</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bulkAdd</span>(<span class="params">store, items</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">const</span> failures = []</span><br><span class="line">        <span class="keyword">let</span> req, item</span><br><span class="line">        <span class="keyword">for</span> (item <span class="keyword">of</span> items) &#123;</span><br><span class="line">            req = store.add(item)</span><br><span class="line">            req.onerror = <span class="function">() =&gt;</span> failures.push(item.id)</span><br><span class="line">        &#125;</span><br><span class="line">        req.onsuccess = <span class="function">() =&gt;</span> resolve(failures)</span><br><span class="line">        req.onerror = <span class="function">() =&gt;</span> &#123;</span><br><span class="line">            failures.push(item.id)</span><br><span class="line">            resolve(failures)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> This time we have a better running time of 10~12 seconds, which is 8000~10000 records per second, nearly 4x faster than the first implementation! The table below summarizes how the performance get improved along the way:</p><table><thead><tr><th>impl.</th><th>time(s)</th><th>speed(r/s)</th></tr></thead><tbody><tr><td>sequential</td><td>30~40</td><td>2000~3000</td></tr><tr><td>concurrent</td><td>~15</td><td>~6000</td></tr><tr><td>concurrent+less listeners</td><td>10~12</td><td>8000~10000</td></tr></tbody></table><p>The last implementation is also how <a href="https://dexie.org/">Dexie.js</a> does for efficient bulk mutations. The library states on its homepage:</p><blockquote><p>Its bulk operations utilize an often-overlooked feature in IndexedDB, ignoring success callbacks when possible.</p></blockquote><p style="text-indent: 0; margin-top: 0;"> but few documentation or posts can be found to elaborate the mechanism. It is also the reason why this post is written.</p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li>  <a href="https://www.w3.org/TR/IndexedDB-3/#async-execute-request">Indexed Database API 3.0 - 5.6: Asynchronously executing a request</a></li><li>  <a href="https://github.com/dfahlander/Dexie.js/blob/76ab6ac4bd/src/dbcore/dbcore-indexeddb.ts#L118">Dexie.js/src/dbcore/dbcore-indexeddb.ts Line 118</a></li><li>  <a href="https://stackoverflow.com/a/52555073/3278171">StackOverflow - Optimized Bulk (Chunk) Upload Of Objects Into IndexedDB</a></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;IndexedDB seems to be inefficient when performing bulk mutations, like dumping a huge list of items into an object store – at least I think so at the first sight on the MDN docs. It provides no explicit API for the job as SQL does (you may add as many records after the &lt;code&gt;INSERT INTO ... VALUES&lt;/code&gt; clause), so all we can do is to use a loop from client, and thus cannot benefit from database internal optimization (if there’s any). The mutation requests, in addition, appear to be spawned sequentially only. The tutorial recommends a paradigm to raise a request within the &lt;code&gt;success&lt;/code&gt; event callback of the previous request, which is in fact a sequential execution. Such code will be definitely slow.&lt;/p&gt;
&lt;p&gt;We may conduct a quick benchmark on the above approach:&lt;/p&gt;
&lt;figure class=&quot;highlight javascript&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;;(&lt;span class=&quot;keyword&quot;&gt;async&lt;/span&gt; () =&amp;gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;await&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;built_in&quot;&gt;Promise&lt;/span&gt;(&lt;span class=&quot;function&quot;&gt;(&lt;span class=&quot;params&quot;&gt;resolve&lt;/span&gt;) =&amp;gt;&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        &lt;span class=&quot;keyword&quot;&gt;const&lt;/span&gt; r = indexedDB.deleteDatabase(&lt;span class=&quot;string&quot;&gt;&amp;quot;test&amp;quot;&lt;/span&gt;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        r.onsuccess = r.onerror = resolve&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &amp;#125;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;const&lt;/span&gt; items = &lt;span class=&quot;built_in&quot;&gt;Array&lt;/span&gt;.from(&amp;#123; &lt;span class=&quot;attr&quot;&gt;length&lt;/span&gt;: &lt;span class=&quot;number&quot;&gt;100000&lt;/span&gt; &amp;#125;, &lt;span class=&quot;function&quot;&gt;(&lt;span class=&quot;params&quot;&gt;_, i&lt;/span&gt;) =&amp;gt;&lt;/span&gt; (&amp;#123; &lt;span class=&quot;attr&quot;&gt;id&lt;/span&gt;: i &amp;#125;))&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;const&lt;/span&gt; store = &lt;span class=&quot;keyword&quot;&gt;await&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;built_in&quot;&gt;Promise&lt;/span&gt;(&lt;span class=&quot;function&quot;&gt;(&lt;span class=&quot;params&quot;&gt;resolve&lt;/span&gt;) =&amp;gt;&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        indexedDB.open(&lt;span class=&quot;string&quot;&gt;&amp;quot;test&amp;quot;&lt;/span&gt;, &lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;).onupgradeneeded = &lt;span class=&quot;function&quot;&gt;(&lt;span class=&quot;params&quot;&gt;event&lt;/span&gt;) =&amp;gt;&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            &lt;span class=&quot;keyword&quot;&gt;const&lt;/span&gt; db = event.target.result&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            &lt;span class=&quot;keyword&quot;&gt;const&lt;/span&gt; store = db.createObjectStore(&lt;span class=&quot;string&quot;&gt;&amp;quot;store&amp;quot;&lt;/span&gt;, &amp;#123; &lt;span class=&quot;attr&quot;&gt;keyPath&lt;/span&gt;: &lt;span class=&quot;string&quot;&gt;&amp;quot;id&amp;quot;&lt;/span&gt; &amp;#125;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            store.createIndex(&lt;span class=&quot;string&quot;&gt;&amp;quot;id&amp;quot;&lt;/span&gt;, &lt;span class=&quot;string&quot;&gt;&amp;quot;id&amp;quot;&lt;/span&gt;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            resolve(store)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &amp;#125;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;built_in&quot;&gt;console&lt;/span&gt;.time(&lt;span class=&quot;string&quot;&gt;&amp;quot;bulkAdd&amp;quot;&lt;/span&gt;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;await&lt;/span&gt; bulkAdd(store, items)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;built_in&quot;&gt;console&lt;/span&gt;.timeEnd(&lt;span class=&quot;string&quot;&gt;&amp;quot;bulkAdd&amp;quot;&lt;/span&gt;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;)()&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;function&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;bulkAdd&lt;/span&gt;(&lt;span class=&quot;params&quot;&gt;store, items&lt;/span&gt;) &lt;/span&gt;&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;const&lt;/span&gt; failures = []&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;built_in&quot;&gt;Promise&lt;/span&gt;(&lt;span class=&quot;function&quot;&gt;(&lt;span class=&quot;params&quot;&gt;resolve&lt;/span&gt;) =&amp;gt;&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        &lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;function&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;_perform&lt;/span&gt;(&lt;span class=&quot;params&quot;&gt;idx&lt;/span&gt;) &lt;/span&gt;&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            &lt;span class=&quot;keyword&quot;&gt;const&lt;/span&gt; req = store.add(items[idx])&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            req.onsuccess = &lt;span class=&quot;function&quot;&gt;(&lt;span class=&quot;params&quot;&gt;event&lt;/span&gt;) =&amp;gt;&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;                &lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (idx === items.length - &lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;) resolve(failures)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;                &lt;span class=&quot;keyword&quot;&gt;else&lt;/span&gt; _perform(idx + &lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            req.onerror = &lt;span class=&quot;function&quot;&gt;(&lt;span class=&quot;params&quot;&gt;event&lt;/span&gt;) =&amp;gt;&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;                failures.push(items[idx].id)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        _perform(&lt;span class=&quot;number&quot;&gt;0&lt;/span&gt;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &amp;#125;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;p&gt;@par Practically, we concern more about failed records than the ones inserted successfully. We thus only take down the indices of those records, which improves the efficiency at least a little bit.&lt;/p&gt;
&lt;p&gt;The timing is rather unstable, but on average, it takes 30~40 seconds to insert 100k records or 2000~3000 records per second, which is not an ideal speed.&lt;/p&gt;</summary>
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Web" scheme="http://i.hsfzxjy.site/tags/Web/"/>
    
    <category term="IndexedDB" scheme="http://i.hsfzxjy.site/tags/IndexedDB/"/>
    
  </entry>
  
  <entry>
    <title>Auto Rebuild .pyx Files with pyximport</title>
    <link href="http://i.hsfzxjy.site/2021-05-16-cython-auto-reload/"/>
    <id>http://i.hsfzxjy.site/2021-05-16-cython-auto-reload/</id>
    <published>2021-05-15T16:00:00.000Z</published>
    <updated>2021-05-18T12:11:46.385Z</updated>
    
    <content type="html"><![CDATA[<p>Modules written in Cython usually comes with a <code>setup.py</code> script, compiling the Cython source codes into native shared libary. For whom not so familiar with Python’s packaging and distributing toolchains, such step is sometimes forbidding, and turns out to be a stumbling block for Cython freshmen. Moreover, the workflow, “run setup.py -&gt; debug -&gt; edit .pyx files -&gt; run setup.py”, is also not convenient and troublesome for fast iterating projects.</p><p><code>pyximport</code> is a handy tool from Cython official, to address the above problem. The module enables users to “directly import” <code>.pyx</code> files, with no explicit <code>setup.py</code> required. Let’s take an example here. Say we have two files residing in the same directory:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">import</span> pyximport</span><br><span class="line"></span><br><div class="_hl"><span class="line">pyximport.install(language_level=<span class="number">3</span>)</span><br></div><span class="line"></span><br><span class="line"><span class="keyword">import</span> foo</span><br><span class="line"></span><br><span class="line">print(foo.func(<span class="number">3</span>))</span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># foo.pyx</span></span><br><span class="line">cpdef <span class="built_in">int</span> sqr(<span class="built_in">int</span> x):</span><br><span class="line">    <span class="keyword">return</span> x * x</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> The magically <code class="_hl-label">highlighted</code> line, registers some import hooks to let Python recognize <code>.pyx</code> files. <code>pyximport</code> automatically compiles or re-compiles the <code>.pyx</code> files behind the scene, when they are imported for the first time, or modified in the future.</p><a id="more"></a><p>By default, the built shared libaries are stored at <code>~/.pyxbld/</code>, which can be overrided with the <code>build_dir</code> argument of <code>pyximport.install</code>. One may also pass <code>inplace=True</code>, to place the libraries sibling to their source files. For example:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># main.py</span></span><br><span class="line"><span class="keyword">import</span> pyximport</span><br><span class="line"></span><br><div class="_hl"><span class="line">pyximport.install(language_level=<span class="number">3</span>, inplace=<span class="literal">True</span>)</span><br></div><span class="line"></span><br><span class="line"><span class="keyword">import</span> foo</span><br><span class="line"></span><br><span class="line">print(foo.func(<span class="number">3</span>))</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> will generate a <code>.so</code> (or <code>.pyd</code> in Windows) file in the same directory.</p><p>But there’s a problem with inplace building – the extension will not be automatically rebuilt. If you modify the <code>.pyx</code> file and re-run the program, the compilation process will not be triggered. The reason is that Python itself is able to recognize <code>.so</code> or <code>.pyd</code> modules, and their importers have higher priority than customized ones. Thus, if there’s a <code>.so</code> and a <code>.pyx</code> file reside in the same directory, the <code>.so</code> file will be imported instead of <code>.pyx</code>.</p><p>So if you want the compiled binaries be placed within your project directory, the best practice is to specify the <code>build_dir</code> argument. By setting <code>build_dir</code> to somewhere inside your project, compiled libraries will be kept in your project directory, and at the same time you can still enjoy the automatic rebuild service.</p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li>  <a href="https://cython.readthedocs.io/en/latest/src/userguide/source_files_and_compilation.html#compiling-with-pyximport">https://cython.readthedocs.io/en/latest/src/userguide/source_files_and_compilation.html#compiling-with-pyximport</a></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;Modules written in Cython usually comes with a &lt;code&gt;setup.py&lt;/code&gt; script, compiling the Cython source codes into native shared libary. For whom not so familiar with Python’s packaging and distributing toolchains, such step is sometimes forbidding, and turns out to be a stumbling block for Cython freshmen. Moreover, the workflow, “run setup.py -&amp;gt; debug -&amp;gt; edit .pyx files -&amp;gt; run setup.py”, is also not convenient and troublesome for fast iterating projects.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;pyximport&lt;/code&gt; is a handy tool from Cython official, to address the above problem. The module enables users to “directly import” &lt;code&gt;.pyx&lt;/code&gt; files, with no explicit &lt;code&gt;setup.py&lt;/code&gt; required. Let’s take an example here. Say we have two files residing in the same directory:&lt;/p&gt;
&lt;figure class=&quot;highlight python&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;# main.py&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;import&lt;/span&gt; pyximport&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;div class=&quot;_hl&quot;&gt;&lt;span class=&quot;line&quot;&gt;pyximport.install(language_level=&lt;span class=&quot;number&quot;&gt;3&lt;/span&gt;)&lt;/span&gt;&lt;br&gt;&lt;/div&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;import&lt;/span&gt; foo&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;print(foo.func(&lt;span class=&quot;number&quot;&gt;3&lt;/span&gt;))&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;figure class=&quot;highlight python&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;# foo.pyx&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;cpdef &lt;span class=&quot;built_in&quot;&gt;int&lt;/span&gt; sqr(&lt;span class=&quot;built_in&quot;&gt;int&lt;/span&gt; x):&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt; x * x&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;p&gt;@par The magically &lt;code&gt;@#highlighted&lt;/code&gt; line, registers some import hooks to let Python recognize &lt;code&gt;.pyx&lt;/code&gt; files. &lt;code&gt;pyximport&lt;/code&gt; automatically compiles or re-compiles the &lt;code&gt;.pyx&lt;/code&gt; files behind the scene, when they are imported for the first time, or modified in the future.&lt;/p&gt;</summary>
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Cython" scheme="http://i.hsfzxjy.site/tags/Cython/"/>
    
  </entry>
  
  <entry>
    <title>Cython and Threads</title>
    <link href="http://i.hsfzxjy.site/2021-05-07-cython-and-threads/"/>
    <id>http://i.hsfzxjy.site/2021-05-07-cython-and-threads/</id>
    <published>2021-05-06T16:00:00.000Z</published>
    <updated>2021-05-08T09:24:15.109Z</updated>
    
    <content type="html"><![CDATA[<p>Pure Python <em>sucks</em> in the scene of parallel computing, due to the existence of the Global Interpreter Lock (aka GIL). GIL prevents codes from different threads to access or manipulate the interpreter. The mechanism alleviates the risk of race condition, but makes multi-threading program “single-threaded” as well. Sadly, there’s no way to release the lock from pure Python.</p><p>OK. So what about not using pure Python? Shall we make an extension to bypass the mechanism? The answer is yes, and that’s what most of scientific libaries do.</p><p>As of writing extensions, Cython is a good choice, less verbose, and more similar to Python syntactically. In Cython, one can release GIL temporarily for a block using the <code>with nogil:</code> syntax. Will it better utilize multi-core CPU? Let’s have a try.</p><a id="more"></a><p>We will use a toy example, say, a naive matrix multiplication, for benchmarking. Start with a C-only version:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#cython: boundscheck=False</span></span><br><span class="line"><span class="comment">#cython: wraparound=False</span></span><br><span class="line"><span class="comment">#cython: nonecheck=False</span></span><br><span class="line"><span class="comment">#cython: cdivision=True</span></span><br><span class="line"><span class="comment">#cython: languagelevel=3</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line">cimport numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">cdef void _matmul(</span><br><span class="line">    np.float_t[:, :] Av,</span><br><span class="line">    np.float_t[:, :] Bv,</span><br><span class="line">    np.float_t[:, :] Cv,</span><br><span class="line">    <span class="built_in">int</span> M, <span class="built_in">int</span> N, <span class="built_in">int</span> P,</span><br><span class="line">) nogil:</span><br><span class="line">    cdef <span class="built_in">int</span> i, j, k</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(M):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(P):</span><br><span class="line">            <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(N):</span><br><span class="line">                Cv[i, j] += Av[i, k] * Bv[k, j]</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> The function above is straight-forward. We then create a wrapper for it, so that it can be called by Python code:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cpdef matmul(</span><br><span class="line">    np.ndarray[dtype=np.float_t, ndim=<span class="number">2</span>] A,</span><br><span class="line">    np.ndarray[dtype=np.float_t, ndim=<span class="number">2</span>] B,</span><br><span class="line">    <span class="built_in">object</span> use_gil,</span><br><span class="line">):</span><br><span class="line">    cdef <span class="built_in">int</span> M = A.shape[<span class="number">0</span>]</span><br><span class="line">    cdef <span class="built_in">int</span> N = A.shape[<span class="number">1</span>]</span><br><span class="line">    cdef <span class="built_in">int</span> P = B.shape[<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line">    C = np.zeros((M, P))</span><br><span class="line">    cdef np.float_t[:, :] Av = A, Bv = B, Cv = C</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> use_gil:</span><br><span class="line">        _matmul(Av, Bv, Cv, M, N, P)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">with</span> nogil:</span><br><span class="line">            _matmul(Av, Bv, Cv, M, N, P)</span><br><span class="line">    <span class="keyword">return</span> C</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> Now the Cython part is ready. We then create a script for benchmarking:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> timeit</span><br><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"><span class="keyword">import</span> itertools</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> pyximport</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">pyximport.install(setup_args=&#123;<span class="string">&quot;include_dirs&quot;</span>: np.get_include()&#125;, inplace=<span class="literal">True</span>, language_level=<span class="number">3</span>)</span><br><span class="line"><span class="keyword">import</span> matmul</span><br><span class="line"></span><br><span class="line">N = <span class="number">1200</span></span><br><span class="line">A = np.random.rand(N, N)</span><br><span class="line">B = np.random.rand(N, N)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">runner</span>(<span class="params">nthreads: <span class="built_in">int</span>, use_gil: <span class="built_in">bool</span></span>) -&gt; <span class="keyword">None</span>:</span></span><br><span class="line">    args = (A, B, use_gil)</span><br><span class="line"></span><br><span class="line">    threads = []</span><br><span class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(nthreads):</span><br><span class="line">        threads.append(threading.Thread(target=matmul.matmul, args=args))</span><br><span class="line">        threads[-<span class="number">1</span>].start()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> thread <span class="keyword">in</span> threads:</span><br><span class="line">        thread.join()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">make_grid</span>(<span class="params">**kwargs</span>):</span></span><br><span class="line">    space = [[(name, v) <span class="keyword">for</span> v <span class="keyword">in</span> lst] <span class="keyword">for</span> name, lst <span class="keyword">in</span> kwargs.items()]</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">map</span>(<span class="built_in">dict</span>, itertools.product(*space))</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> kw <span class="keyword">in</span> make_grid(</span><br><span class="line">        nthreads=[<span class="number">1</span>, <span class="number">2</span>],</span><br><span class="line">        use_gil=[<span class="literal">True</span>, <span class="literal">False</span>],</span><br><span class="line">):</span><br><span class="line">    print(kw)</span><br><span class="line">    print(timeit.timeit(<span class="string">&quot;runner(**kw)&quot;</span>, <span class="built_in">globals</span>=<span class="built_in">dict</span>(runner=runner, kw=kw), number=<span class="number">1</span>))</span><br></pre></td></tr></table></figure><p>Two matrices are as input, each with a rather large size <code>1200 x 1200</code>, and <code>matmul</code> will be tested with four settings. The result is listed as below:</p><p style="text-indent: 0;"></p><table><thead><tr><th align="center">nthreads</th><th align="center">GIL</th><th align="center">time (s)</th></tr></thead><tbody><tr><td align="center">1</td><td align="center">N</td><td align="center">3.47</td></tr><tr><td align="center">1</td><td align="center">Y</td><td align="center">3.51</td></tr><tr><td align="center">2</td><td align="center">N</td><td align="center">3.78</td></tr><tr><td align="center">2</td><td align="center">Y</td><td align="center">6.96</td></tr></tbody></table><p style="text-indent: 0;"> The first two rows show that, with single thread, <code>matmul</code> has comparable performance whether or not releasing GIL. This is desired, since GIL should not lead to performance degrade in single-threaded scene. But things change when it comes to multi-threading. If not releasing GIL, the time doubles when two computing threads run in parallel, whilst in another setting (GIL released), the performance remains unchanged.</p><p style="text-indent: 0;"></p><p>We may step further to investigate the behavior of <code>prange</code>. <code>prange</code> is a facility provided by Cython for more convenient parallel computing, using the famous OpenMP as backend. We can write a <code>prange</code> version <code>_matmul</code> with only minor modification:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cdef void _matmul_p(</span><br><span class="line">    np.float_t[:, :] Av,</span><br><span class="line">    np.float_t[:, :] Bv,</span><br><span class="line">    np.float_t[:, :] Cv,</span><br><span class="line">    <span class="built_in">int</span> M, <span class="built_in">int</span> N, <span class="built_in">int</span> P,</span><br><span class="line">) nogil:</span><br><span class="line">    cdef <span class="built_in">int</span> i, j, k, ij</span><br><span class="line"></span><br><span class="line">    cdef <span class="built_in">int</span> MP = M * P</span><br><span class="line">    <span class="keyword">for</span> ij <span class="keyword">in</span> prange(MP, schedule=<span class="string">&#x27;guided&#x27;</span>):</span><br><span class="line">        i = ij // P</span><br><span class="line">        j = ij % P</span><br><span class="line">        <span class="keyword">for</span> k <span class="keyword">in</span> <span class="built_in">range</span>(N):</span><br><span class="line">            Cv[i, j] += Av[i, k] * Bv[k, j]</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> and the wrapper <code>matmul</code>:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cpdef matmul(</span><br><span class="line">    np.ndarray[dtype=np.float_t, ndim=<span class="number">2</span>] A,</span><br><span class="line">    np.ndarray[dtype=np.float_t, ndim=<span class="number">2</span>] B,</span><br><span class="line">    <span class="built_in">object</span> use_gil,</span><br><div class="_hl"><span class="line">    <span class="built_in">object</span> parallel,</span><br></div><span class="line">):</span><br><span class="line">    cdef <span class="built_in">int</span> M = A.shape[<span class="number">0</span>]</span><br><span class="line">    cdef <span class="built_in">int</span> N = A.shape[<span class="number">1</span>]</span><br><span class="line">    cdef <span class="built_in">int</span> P = B.shape[<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line">    C = np.zeros((M, P))</span><br><span class="line">    cdef np.float_t[:, :] Av = A, Bv = B, Cv = C</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> use_gil:</span><br><div class="_hl"><span class="line">        <span class="keyword">if</span> parallel:</span><br><span class="line">            _matmul_p(Av, Bv, Cv, M, N, P)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            _matmul(Av, Bv, Cv, M, N, P)</span><br></div><span class="line">    <span class="keyword">else</span>:</span><br><div class="_hl"><span class="line">        <span class="keyword">if</span> parallel:</span><br><span class="line">            _matmul_p(Av, Bv, Cv, M, N, P)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">with</span> nogil:</span><br><span class="line">                _matmul(Av, Bv, Cv, M, N, P)</span><br></div><span class="line">    <span class="keyword">return</span> C</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> and also, the benchmark script:</p><figure class="highlight python"><table><tr><td class="code"><pre><div class="_hl"><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">runner</span>(<span class="params">nthreads: <span class="built_in">int</span>, use_gil: <span class="built_in">bool</span>, parallel: <span class="built_in">bool</span></span>) -&gt; <span class="keyword">None</span>:</span></span><br><span class="line">    args = (A, B, use_gil, parallel)</span><br></div><span class="line">    <span class="keyword">if</span> nthreads == <span class="number">0</span>:</span><br><span class="line">        matmul.matmul(*args)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">    threads = []</span><br><span class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(nthreads):</span><br><span class="line">        threads.append(threading.Thread(target=matmul.matmul, args=args))</span><br><span class="line">        threads[-<span class="number">1</span>].start()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> thread <span class="keyword">in</span> threads:</span><br><span class="line">        thread.join()</span><br></pre></td></tr></table></figure><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> kw <span class="keyword">in</span> make_grid(</span><br><span class="line">        nthreads=[<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>],</span><br><span class="line">        use_gil=[<span class="literal">True</span>, <span class="literal">False</span>],</span><br><div class="_hl"><span class="line">        parallel=[<span class="literal">True</span>, <span class="literal">False</span>],</span><br></div><span class="line">):</span><br><span class="line">    print(kw)</span><br><span class="line">    print(timeit.timeit(<span class="string">&quot;runner(**kw)&quot;</span>, <span class="built_in">globals</span>=<span class="built_in">dict</span>(runner=runner, kw=kw), number=<span class="number">1</span>))</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> OpenMP requires extra compilation flags, so a <code>.pyxbld</code> file is needed:</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># matmul.pyxbld</span></span><br><span class="line"><span class="keyword">from</span> setuptools <span class="keyword">import</span> Extension</span><br><span class="line"><span class="keyword">from</span> Cython.Build <span class="keyword">import</span> cythonize</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">make_ext</span>(<span class="params">modname, pyxfilename</span>):</span></span><br><span class="line">    ext = Extension(</span><br><span class="line">        modname,</span><br><span class="line">        [pyxfilename],</span><br><span class="line">        extra_compile_args=[<span class="string">&#x27;-fopenmp&#x27;</span>],</span><br><span class="line">        extra_link_args=[<span class="string">&#x27;-fopenmp&#x27;</span>],</span><br><span class="line">    )</span><br><span class="line">    <span class="keyword">return</span> cythonize(</span><br><span class="line">        [ext],</span><br><span class="line">        language_level=<span class="number">3</span>,</span><br><span class="line">        annotate=<span class="literal">True</span>,</span><br><span class="line">    )[<span class="number">0</span>]</span><br></pre></td></tr></table></figure><p style="text-indent: 0;"></p><table><thead><tr><th align="center">nthreads</th><th align="center">GIL</th><th align="center">time w/o par. (s)</th><th align="center">time w/ par. (s)</th></tr></thead><tbody><tr><td align="center">1</td><td align="center">N</td><td align="center">3.47</td><td align="center">0.82</td></tr><tr><td align="center">1</td><td align="center">Y</td><td align="center">3.51</td><td align="center">0.89</td></tr><tr><td align="center">2</td><td align="center">N</td><td align="center">3.78</td><td align="center">1.79</td></tr><tr><td align="center">2</td><td align="center">Y</td><td align="center">6.96</td><td align="center">1.81</td></tr></tbody></table><p><code>prange</code> comes with an amazing boost in performance! <code>_matmul_p</code> is 3~4x faster in single-threaded settings. The number might vary on your own computer, depending on the CPU cores you have. In settings with two threads, running time doubles, which means <code>prange</code> <em>does</em> efficiently eat up all CPU resources.</p><p>We can also notice that, the GIL switch seemingly does not affect <code>prange</code>. The reason is <code>prange</code> requires GIL to be released, and it actually does by default.</p><p style="text-indent: 0;"></p><blockquote><p style="text-indent: 0;"> Cython supports native parallelism through the cython.parallel module. To use this kind of parallelism, the GIL must be released (see Releasing the GIL). It currently supports OpenMP, but later on more backends might be supported. – <em><a href="https://cython.readthedocs.io/en/latest/src/userguide/parallelism.html">Using Parallelism</a></em></p></blockquote><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><ul><li>  <strong>If there’s no need to acquire GIL, just release it.</strong> This happens when you are manipulating some C data structures, and not attempting to interfere with the interpreter.</li><li>  <strong>If there’s massive looping in your Cython code, feel free to accelerate with <code>prange</code>.</strong> <code>prange</code> will effeciently schedule the computation on all CPU resources.</li><li>  <strong>If there’s some macro tasks which are not easy to parallelize in Cython, schedule them via <code>threading</code> module.</strong> <code>threading</code> sucks for most of the time, but if the tasks not always acquiring GIL, it should work well just like threads in other languages.</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;Pure Python &lt;em&gt;sucks&lt;/em&gt; in the scene of parallel computing, due to the existence of the Global Interpreter Lock (aka GIL). GIL prevents codes from different threads to access or manipulate the interpreter. The mechanism alleviates the risk of race condition, but makes multi-threading program “single-threaded” as well. Sadly, there’s no way to release the lock from pure Python.&lt;/p&gt;
&lt;p&gt;OK. So what about not using pure Python? Shall we make an extension to bypass the mechanism? The answer is yes, and that’s what most of scientific libaries do.&lt;/p&gt;
&lt;p&gt;As of writing extensions, Cython is a good choice, less verbose, and more similar to Python syntactically. In Cython, one can release GIL temporarily for a block using the &lt;code&gt;with nogil:&lt;/code&gt; syntax. Will it better utilize multi-core CPU? Let’s have a try.&lt;/p&gt;</summary>
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Cython" scheme="http://i.hsfzxjy.site/tags/Cython/"/>
    
  </entry>
  
  <entry>
    <title>Obtain a Random Unused TCP Port with Bash</title>
    <link href="http://i.hsfzxjy.site/2021-03-10-obtain-a-random-unused-tcp-port-with-bash/"/>
    <id>http://i.hsfzxjy.site/2021-03-10-obtain-a-random-unused-tcp-port-with-bash/</id>
    <published>2021-03-09T16:00:00.000Z</published>
    <updated>2021-05-12T15:36:42.302Z</updated>
    
    <content type="html"><![CDATA[<p>We might sometimes want to randomly choose an unused TCP port on Linux. This occurs from time to time on a server, when the administrator wants to expose an HTTP port for each user. Or, you just need an available port for IPC. Let’s make it happen with bash only.</p><p style="margin-top: 0;"> The function below does the right job:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="function"><span class="title">unused_port</span></span>() &#123;</span><br><span class="line">    N=<span class="variable">$&#123;1:-1&#125;</span></span><br><span class="line">    comm -23 \</span><br><span class="line">        &lt;(seq <span class="string">&quot;1025&quot;</span> <span class="string">&quot;65535&quot;</span> | sort) \</span><br><span class="line">        &lt;(ss -Htan |</span><br><span class="line">            awk <span class="string">&#x27;&#123;print $4&#125;&#x27;</span> |</span><br><span class="line">            cut -d<span class="string">&#x27;:&#x27;</span> -f2 |</span><br><span class="line">            sort -u) |</span><br><span class="line">        shuf |</span><br><span class="line">        head -n <span class="string">&quot;<span class="variable">$N</span>&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> We are going to step through the code next.</p><a id="more"></a><p style="text-indent: 0;"></p><p>Our first step is to obtain a list of occupied ports, which can be accomplished by command</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ss -Htan</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> <code>ss</code> is a tool for investigating sockets, printing out currently used ports as a table like:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">State        Recv-Q   Send-Q                               Local Address:Port                              Peer Address:Port               Process</span><br><span class="line">ESTAB        0        0                                        127.0.0.1:45342                                127.0.0.1:1081</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> Argument <code>-Htan</code> controls the printing style of <code>ss</code>. <code>-H</code> removes table header; <code>-t</code> lists only TCP ports; <code>-a</code> lists all ports, including listening and unlistening ones; <code>-n</code> enforces ports to be printed numerically (otherwise <code>80</code> will be resolved as <code>http</code>). The command produces an output like:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">LISTEN       0        128                                        0.0.0.0:17500                                            0.0.0.0:*</span><br><span class="line">LISTEN       0        128                                      127.0.0.1:17600                                            0.0.0.0:*</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> We then use some string manipulation tools to extract the port numbers:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ss -Htan |</span><br><div class="_hl"><span class="line">    awk <span class="string">&#x27;&#123;print $4&#125;&#x27;</span> |</span><br><span class="line">    cut -d<span class="string">&#x27;:&#x27;</span> -f2 |</span><br><span class="line">    sort -u</span><br></pre></div></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> Here, <code>awk &#39;&#123;print $4&#125;&#39;</code> selects the 4th item (e.g., <code>0.0.0.0:17500</code>) from each line. <code>cut -d&#39;:&#39; -f2</code> splits each item with <code>:</code>, and then prints out the second part (e.g., <code>17500</code>). <code>sort -u</code> sorts the items and removes duplicated ones.</p><p>Now we get the list, say <code>LIST2</code>. Our next step is to create another list <code>LIST1</code> by “inversing” <code>LIST2</code>, such that <code>LIST1</code> (disjointly) unioning <code>LIST2</code> equals <code>FULLLIST</code> (all legal ports).</p><p style="margin-top: 0;"> <code>FULLLIST</code> can be obtained easily with <code>seq &quot;1025&quot; &quot;65535&quot; | sort</code>. The “inverse” operation, meanwhile, can be achieved using <code>comm</code>.</p><blockquote><p style="text-indent: 0;"> Simply, <code>comm</code> takes two files <code>FILE1</code> and <code>FILE2</code> as input, and produces output with three columns:</p><ul><li>  Column 1 contains lines unique to <code>FILE1</code>;</li><li>  Column 2 contains lines unique to <code>FILE2</code>;</li><li>  Column 3 contains lines common to both files.</li></ul></blockquote><p style="text-indent: 0; margin-top: 0;"> Apparently we only needs the first column, so use <code>-23</code> to suppress the other two ones. Put them together as:</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">comm -23 \</span><br><span class="line">    &lt;(seq <span class="string">&quot;1025&quot;</span> <span class="string">&quot;65535&quot;</span> | sort) \</span><br><span class="line">    &lt;(ss -Htan |</span><br><span class="line">        awk <span class="string">&#x27;&#123;print $4&#125;&#x27;</span> |</span><br><span class="line">        cut -d<span class="string">&#x27;:&#x27;</span> -f2 |</span><br><span class="line">        sort -u)</span><br></pre></td></tr></table></figure><blockquote><p style="text-indent: 0;"> The syntax <code>&lt;(command)</code> is called <strong>process substitution</strong>. It is equivalent to:</p><ul><li>  Spawn <code>command</code> <em>in current shell</em> and pipe its stdout to <code>/dev/fd/&lt;some number&gt;</code>;</li><li>  Substitute <code>&lt;(...)</code> with <code>/dev/fd/&lt;some number&gt;</code>.</li></ul></blockquote><p>The last step, we further extend the command into a convenient function.</p><figure class="highlight bash"><table><tr><td class="code"><pre><div class="_hl"><span class="line"><span class="keyword">function</span> <span class="function"><span class="title">unused_port</span></span>() &#123;</span><br><span class="line">    N=<span class="variable">$&#123;1:-1&#125;</span></span><br></div><span class="line">    comm -23 \</span><br><span class="line">        &lt;(seq <span class="string">&quot;1025&quot;</span> <span class="string">&quot;65535&quot;</span> | sort) \</span><br><span class="line">        &lt;(ss -Htan |</span><br><span class="line">            awk <span class="string">&#x27;&#123;print $4&#125;&#x27;</span> |</span><br><span class="line">            cut -d<span class="string">&#x27;:&#x27;</span> -f2 |</span><br><span class="line">            sort -u) |</span><br><div class="_hl"><span class="line">        shuf |</span><br><span class="line">        head -n <span class="string">&quot;<span class="variable">$N</span>&quot;</span></span><br></div><span class="line">&#125;</span><br></pre></td></tr></table></figure><p style="text-indent: 0; margin-top: 0;"> The function receives an argument <code>N</code>, shuffles the available port numbers, and choose <code>N</code> ports from the list.</p><p style="text-indent: 0;"> Code is available at <a href="https://github.com/hsfzxjy/bashi/blob/master/functions/unused_port.sh">hsfzxjy/bashi</a> on Github.</p><h1 id="References"><a href="#References" class="headerlink" title="References"></a>References</h1><ul><li>  <a href="https://unix.stackexchange.com/a/423052/353147">https://unix.stackexchange.com/a/423052/353147</a></li><li>  <a href="https://tldp.org/LDP/abs/html/process-sub.html">https://tldp.org/LDP/abs/html/process-sub.html</a></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;We might sometimes want to randomly choose an unused TCP port on Linux. This occurs from time to time on a server, when the administrator wants to expose an HTTP port for each user. Or, you just need an available port for IPC. Let’s make it happen with bash only.&lt;/p&gt;
&lt;p&gt;@nomargin The function below does the right job:&lt;/p&gt;
&lt;figure class=&quot;highlight bash&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;function&lt;/span&gt; &lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;title&quot;&gt;unused_port&lt;/span&gt;&lt;/span&gt;() &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    N=&lt;span class=&quot;variable&quot;&gt;$&amp;#123;1:-1&amp;#125;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    comm -23 \&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        &amp;lt;(seq &lt;span class=&quot;string&quot;&gt;&amp;quot;1025&amp;quot;&lt;/span&gt; &lt;span class=&quot;string&quot;&gt;&amp;quot;65535&amp;quot;&lt;/span&gt; | sort) \&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        &amp;lt;(ss -Htan |&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            awk &lt;span class=&quot;string&quot;&gt;&amp;#x27;&amp;#123;print $4&amp;#125;&amp;#x27;&lt;/span&gt; |&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            cut -d&lt;span class=&quot;string&quot;&gt;&amp;#x27;:&amp;#x27;&lt;/span&gt; -f2 |&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;            sort -u) |&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        shuf |&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        head -n &lt;span class=&quot;string&quot;&gt;&amp;quot;&lt;span class=&quot;variable&quot;&gt;$N&lt;/span&gt;&amp;quot;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;p&gt;@par We are going to step through the code next.&lt;/p&gt;</summary>
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Linux" scheme="http://i.hsfzxjy.site/tags/Linux/"/>
    
    <category term="Bash" scheme="http://i.hsfzxjy.site/tags/Bash/"/>
    
  </entry>
  
  <entry>
    <title>Information Theory: KL Divergence</title>
    <link href="http://i.hsfzxjy.site/2020-01-15-information-theory-kl-divergence/"/>
    <id>http://i.hsfzxjy.site/2020-01-15-information-theory-kl-divergence/</id>
    <published>2020-01-14T16:00:00.000Z</published>
    <updated>2021-05-07T09:11:39.373Z</updated>
    
    <content type="html"><![CDATA[<p>Assume there are two hypotheses $H_1$ and $H_2$, r.v. $X$ ranged in alphabets $a_1,\ldots\,a_k$. Under hypothesis $H_i$, $X$ has pdf $p(X=a_j|H_i)=p_i(a_j)$. According to Law of Total Probability, we have:</p>$$ p(H_i|a_k) = \frac{p(H_i)p_i(a_k)}{p_1(a_k)p(H_1)+p_2(a_k)p(H_2)} $$<p style="text-indent: 0; margin-top: 0;"> The formula can be transformed into:</p>$$ \log \frac{p_2(a_k)}{p_1(a_k)} = \log \frac{p(H_2|a_k)}{p(H_1|a_k)} - \log \frac{p(H_2)}{p(H_1)} $$<p style="text-indent: 0; margin-top: 0;"> which implies that, $\log \frac{p_2(a_k)}{p_1(a_k)}$ equals the difference of log likelihood ratio before and after conditioning $X=a_k$. We define $\log \frac{p_2(a_k)}{p_1(a_k)}$ be the discrimination information for $H_2$ over $H_1$, when $X=a_k$. The expectation of discrimination information is KL divergence, denoted as:</p>$$D_{KL}(P_2||P_1) = \sum_k p_2(a_k) \log \frac{p_2(a_k)}{p_1(a_k)} $$<p style="text-indent: 0; margin-top: 0;"> which sometimes denoted as $I(p2,p1;X)$, or simply $I(p2,p1)$ if without ambiguity.</p><p>KL Divergence can be interpreted as a measure of expected information for $X$ gained after distribution shifted from $p_1$ to $p_2$, where $p_1$ and $p_2$ regarded as prior and post-prior distributions. </p><a id="more"></a><p style="text-indent: 0; margin-top: 0;"> Further we may define the (symmetrised) divergence between $p_2$ and $p_1$:</p>$$J(p_2,p_1;X)=D_{KL}(p_2||p_1) + D_{KL}(p_1||p_2) = J(p_1,p_2;X)$$<p style="text-indent: 0; margin-top: 0;"> Divergence $J(p_2,p_1)$ is a measure of difference between two distributions satisfying</p><ul><li>$J(p_1,p_2) \geq 0$ and equality holds iff $p_1=p_2$;</li><li>$J(p_1,p_2) = J(p_2,p_1)$.</li></ul><p style="text-indent: 0; margin-top: 0;"> but not triangle inequality, and thus $J(\cdot,\cdot)$ is not a distance function.</p><h2 id="Generalization"><a href="#Generalization" class="headerlink" title="Generalization"></a>Generalization</h2><p>We may generalize the definition to continuous variables as:</p>$$D_{KL}(p_2||p_1) = \int p_2(x) \log \frac{p_2(x)}{p_1(x)} dx$$<p style="text-indent: 0; margin-top: 0;"> or multivariate cases:</p>$$D_{KL}(p_2(\vec{x})||p_1(\vec{x})) = \int p_2(\vec{x}) \log \frac{p_2(\vec{x})}{p_1(\vec{x})} d\vec{x}$$<p style="text-indent: 0; margin-top: 0;"> or further, conditional cases:</p>$$D_{KL}(p_2(X|Y)||p_1(X|Y)) = \int p_2(y) p_2(x|y) \log \frac{p_2(x|y)}{p_1(x|y)} dxdy$$<h2 id="Relation-to-Shannon-Entropy"><a href="#Relation-to-Shannon-Entropy" class="headerlink" title="Relation to Shannon Entropy"></a>Relation to Shannon Entropy</h2><p>For a discrete variable $X$, with distribution $p$, we have:</p>$$H(X)=\log n - D_{KL}(p||v)$$<p style="text-indent: 0; margin-top: 0;"> where $n$ is number of values $X$ can take on, and $v$ is the uniform distribution on those values. The lemma suggests that $D_{KL}(p||v)$ measures the information difference between $p$ and a totally chaotic “bottom distribution”. From the perspective of transmission, KL divergence equals the length of additional bits required to encode a message, when the distribution of source alphabets shifts from $p$ to $v$.</p><p>For a continuous variable $X$ ranged within a finite set of volume $L$, we have:</p>$$H(X) = \log L - D_{KL}(p||v)$$<p style="text-indent: 0; margin-top: 0;"> where $v$ is a uniform distribution over the range of $X$. The interpretation is similar to discrete cases.</p><h2 id="Relation-to-Mutual-Information"><a href="#Relation-to-Mutual-Information" class="headerlink" title="Relation to Mutual Information"></a>Relation to Mutual Information</h2><p>We can observe that</p>$$I(X;Y)=D_{KL}(P(XY)||P(X)P(Y))$$<p style="text-indent: 0; margin-top: 0;"> which suggests mutual information $I(X;Y)$ is the information difference when distribution of $(X,Y)$ shifted from $P(X)P(Y)$ to $P(X,Y)$, i.e., from independent to dependent. </p><h2 id="Additional-Properties-of-KL-Divergence"><a href="#Additional-Properties-of-KL-Divergence" class="headerlink" title="Additional Properties of KL Divergence"></a>Additional Properties of KL Divergence</h2><p style="text-indent: 0;"> <strong>Addictivity</strong> Assume $X,Y$ are independent, and $P(x,y)=P_1(x)P_2(y)$, $Q$ likewisely. We have</p>$$\begin{align}D_{KL}(P(X,Y)||Q(X,Y)) &= \int P(x,y) \log \frac{P(x,y)}{Q(x,y)} dxdy \\&= \int P_1(x)P_2(y) \log \frac{P_1(x)P_2(y)}{Q_1(x)Q_2(y)} dxdy \\&= \int P_1(x) \log \frac{P_1(x)}{Q_1(x)} dx + \int P_2(y) \log \frac{P_2(y)}{Q_2(y)} dy \\&= D_{KL}(P_1||Q_1) + D_{KL}(P_2||Q_2)\end{align}$$<p style="text-indent: 0;"> <strong>Convexity</strong> Assume $p_1,p_2,q_1,q_2,p,q$ are distributions, $0 \leq \lambda \leq 1$. We have</p>$$D_{KL}(\lambda p_1 + (1 - \lambda) p_2 || q) \leq \lambda D_{KL}(p_1||q) + (1 - \lambda) D_{KL}(p_2||q)$$<p style="text-indent: 0; margin-top: 0;"> which can be derived from $\log x \leq x - 1$ when $0 < x < 1$. Similarly we have</p>$$D_{KL}(p || \lambda q_1 + (1 - \lambda) q_2) \leq \lambda D_{KL}(p||q_1) + (1 - \lambda) D_{KL}(p||q_2)$$<p style="text-indent: 0; margin-top: 0;"> which can be derived from the convexity of $\log(\cdot)$. Further we have</p>$$D_{KL}(\lambda p_1 + (1 - \lambda) p_2 || \lambda q_1 + (1 - \lambda) q_2) \leq \lambda D_{KL}(p_1 || q_1) + (1 - \lambda) D_{KL}(p_2|| q_2)$$<p style="text-indent: 0; margin-top: 0;"> which can be proved using the <a href="https://en.wikipedia.org/wiki/Log_sum_inequality">Log Sum Inequality</a>.</p><p style="text-indent: 0;"> <strong>Invariance</strong> Assume $U=g(X)$, then $D_{KL}(p(X)||q(X))=D_{KL}(p(U)||q(U))$, i.e., the form of KL divergence remains invariant under transforms. Mutual information, however, does not have such property.</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;Assume there are two hypotheses $H_1$ and $H_2$, r.v. $X$ ranged in alphabets $a_1,\ldots\,a_k$. Under hypothesis $H_i$, $X$ has pdf $p(X=a_j|H_i)=p_i(a_j)$. According to Law of Total Probability, we have:&lt;/p&gt;
$$ p(H_i|a_k) = \frac{p(H_i)p_i(a_k)}{p_1(a_k)p(H_1)+p_2(a_k)p(H_2)} $$

&lt;p&gt;@par The formula can be transformed into:&lt;/p&gt;
$$ \log \frac{p_2(a_k)}{p_1(a_k)} = \log \frac{p(H_2|a_k)}{p(H_1|a_k)} - \log \frac{p(H_2)}{p(H_1)} $$

&lt;p&gt;@par which implies that, $\log \frac{p_2(a_k)}{p_1(a_k)}$ equals the difference of log likelihood ratio before and after conditioning $X=a_k$. We define $\log \frac{p_2(a_k)}{p_1(a_k)}$ be the discrimination information for $H_2$ over $H_1$, when $X=a_k$. The expectation of discrimination information is KL divergence, denoted as:&lt;/p&gt;
$$D_{KL}(P_2||P_1) = \sum_k p_2(a_k) \log \frac{p_2(a_k)}{p_1(a_k)} $$

&lt;p&gt;@par which sometimes denoted as $I(p2,p1;X)$, or simply $I(p2,p1)$ if without ambiguity.&lt;/p&gt;
&lt;p&gt;KL Divergence can be interpreted as a measure of expected information for $X$ gained after distribution shifted from $p_1$ to $p_2$, where $p_1$ and $p_2$ regarded as prior and post-prior distributions. &lt;/p&gt;</summary>
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Information Theory" scheme="http://i.hsfzxjy.site/tags/Information-Theory/"/>
    
  </entry>
  
  <entry>
    <title>Information Theory: Entropy and Mutual Information</title>
    <link href="http://i.hsfzxjy.site/2020-01-04-information-theory-entropy-and-mutual-information/"/>
    <id>http://i.hsfzxjy.site/2020-01-04-information-theory-entropy-and-mutual-information/</id>
    <published>2020-01-03T16:00:00.000Z</published>
    <updated>2021-05-07T09:19:04.421Z</updated>
    
    <content type="html"><![CDATA[<p>Given a discrete r.v. $X$, where $X$ ranged in $\{a_1, \ldots, a_n\}$, $\mathbb{P}(X=a_k)=p_k$. Entropy $H(X)$ is defined as:</p>$$H(X)= - \sum_k p_k \log p_k$$<p style="text-indent: 0; margin-top: 0;"> When regarded as a function of $\{p_k\}$, entropy satisfies the following properties:</p><ol><li> $H(p_1,\ldots,p_n)$ is continuous, and non-negative;</li><li> $H(p_1,\ldots,p_n)$ is convex w.r.t. $(p_1,\ldots,p_n)$;</li><li> $H(p_1,\ldots,p_n)$ has a unique maxima $(\frac{1}{n},\ldots,\frac{1}{n})$;</li><li> $H(n):=H(\frac{1}{n},\ldots,\frac{1}{n})$ increases along with $n$;</li><li> $H(p_1,\ldots,p_n)=H(p_1+\ldots+p_k,p_{k+1},\ldots,p_n)+(p_1+\ldots+p_k)H(p_{k+1}',\ldots,p_n')$.</li></ol><p style="text-indent: 0; margin-top: 0;"> Property 5 is so-called addictivity. That is, if we observe $X$ in two steps, firstly obtaining a value from $\{\hat{a},a_{k+1},\ldots,a_n\}$ and then another value from $\{a_1,\ldots,a_k\}$ if $\hat{a}$ selected, the entropy of the whole system should be sum of these two subsystems.</p><p>Note that a function satisfying property 1, 4, 5 must have a form of $H(\vec{p})= - C \sum_k p_k \log p_k$, which reveals that entropy function is unique.</p><p>Entropy measures the <strong>uncertainty</strong> of a random value. Intuitively, entropy reaches its maximum $\log n$ when all alphabets occur with same probability, and likewise has a minimum of $0$ if $p_k=1$ for some $k$.</p><p>Entropy also represents the smallest average length to encode a message. Say we have a message consisting of alphabets $a_1,\ldots,a_n$, occurring with probability $p_1,\ldots,p_n$. Now we want to assign a code (an $N$-ary string) to each alphabet, with no two codes sharing a same prefix. The length of the codes are denoted as $l_1,\ldots,l_n$. <a href="https://en.wikipedia.org/wiki/Shannon%27s_source_coding_theorem">Shannon’s source coding theroem</a> states that the average code length $\sum_k p_k l_k$ could not be less than $H(p_1,\ldots,p_n)$ (taking $N$ as logarithm base).</p><a id="more"></a><p>We may generalize the definition to 2D random variables likewisely. Suppose $\mathbb{P}((X,Y)=(a_k,b_j))$ $=p(a_k,b_j)$. We define: $$H(XY)=\sum_{k,j} - p(a_k,b_j) \log p(a_k,b_j)$$. Rewrite the formula as:</p>$$\begin{align}H(XY) &= \sum_{k,j} - p(a_k,b_j) \log p(a_k,b_j) \\      &= \sum_k\sum_j - p(a_k,b_j) \log \left[ p(b_j) p(a_k|b_j) \right] \\      &= \sum_j -p(b_j) \log p(b_j) - \sum_j p(b_j) \sum_k p(a_k|b_j) \log p(a_k|b_j) \\      &= H(Y) - \sum_j p(b_j) H(X|b_j) \\      &= H(Y) + H(X|Y) \\\end{align}$$<p style="text-indent: 0; margin-top: 0;"> where $H(X|Y)$ is weighted average of $H(X|\cdot)$, called <strong>conditional entropy</strong>. Symmetrically, we have $H(X)+H(Y|X)=H(Y)+H(X|Y)$. Further we have $H(XY) \leq H(X)+H(Y)$, since:</p>$$\begin{align}H(XY)-H(X)-H(Y) &= \sum_{k,j} p(a_k,b_j) \log \frac{p(a_k)p(b_j)}{p(a_k,b_j)} \\ &\leq \sum_{k,j} p(a_k,b_j) \left[ \frac{p(a_k)p(b_j)}{p(a_k,b_j)} - 1 \right] \\ &=0\end{align}$$<p style="text-indent: 0; margin-top: 0;"> the equality holds $\iff X, Y$ are independent.</p><p>In summary, we have:</p>$$\begin{align}H(XY) &\leq H(X)+H(Y) \\H(Y|X) &\leq H(Y) \\H(X|Y) &\leq H(X) \\\end{align}$$<p style="text-indent: 0; margin-top: 0;"> The latter two are intuitive – entropy (uncertainty) decreases if provided some prior.</p><h2 id="Discrete-Mutual-Information"><a href="#Discrete-Mutual-Information" class="headerlink" title="Discrete Mutual Information"></a>Discrete Mutual Information</h2><p>The mutual information of $X,Y$ is defined as:</p>$$\begin{align}I(X|Y) &= H(X)-H(X|Y) \\       &= - \sum_k p(a_k) \log p(a_k) + \sum_j \sum_k p(a_k,b_j) \log p(a_k|b_j) \\       &= \sum_{j,k} p(a_k,b_j) \log \frac{p(a_k, b_j)}{p(a_k)p(b_j)}\end{align}$$<p style="text-indent: 0; margin-top: 0;"> Intuitively, $I(X;Y)$ is the reduced information after prior $Y$ provided. It’s worth noting that $I(X;Y)=I(Y;X)$ and thus $I(\cdot;\cdot)$ is symmetric. </p><p>We can define conditional mutual information likewisely:</p>$$\begin{align}I(X;Y|Z) &= \sum_i p(z_i) I(X|z_i;Y|z_i) \\         &= \sum_i p(z_i) \sum_{k,j} p(a_k,b_j|z_i) \log \frac{p(a_k, b_j|z_i)}{p(a_k|z_i)p(b_j|z_i)} \\         &= \sum_{i,j,k} p(a_k,b_j,z_i) \log \frac{p(a_k, b_j,z_i)p(z_i)}{p(a_k,z_i)p(b_j,z_i)}\end{align}$$<p>Some facts can be derived using the definition of entropy and mutual information:</p>$$\begin{align}I(X;Y)   &= H(X) - H(X|Y) \\         &= H(X) + H(Y) - H(XY) \\I(X;YZ)  &= H(X) + H(YZ) - H(XYZ) \\I(X;Y|Z) &= H(X|Z) - H(X|YZ) \\         &= H(XZ) + H(YZ) - H(Z) - H(XYZ)\end{align}$$<p style="text-indent: 0; margin-top: 0;"> We can further define mutual information among three (or more) variables as:</p>$$\begin{align}I(X;Y;Z) &= I(X;Y)-I(X;Y|Z) \\         &= H(X)+H(Y)+H(Z)-H(XY)-H(YZ)-H(XZ)+H(XYZ) \\\end{align}$$<p style="text-indent: 0; margin-top: 0;"> We may observse some similarity between the above formula and <a href="https://en.wikipedia.org/wiki/Inclusion%E2%80%93exclusion_principle">Inclusion-Exclusion Principle</a>. In fact, we can build a formal correspondence between entropy and set measure, as:</p>$$\begin{matrix}&H(X)=\mu(A)     &H(XY)=\mu(A \cup B) \\&H(X|Y)=\mu(A-B) &I(X;Y)=\mu(A \cap B) \\&H(X;Y|Z)=\mu((A \cap B)-C) &I(X;Y;Z)=\mu(A \cap B \cap C)\end{matrix}$$<p style="text-indent: 0; margin-top: 0;"> where $\mu$ is some set measure. The correspondence may be used as a helper for memorizing.</p><h2 id="Continuous-Cases"><a href="#Continuous-Cases" class="headerlink" title="Continuous Cases"></a>Continuous Cases</h2><p>We first try to generalize entropy to continuous r.v.. Suppose $X$ has pdf $p(x)$, and $\pi=\{x_i\}_{-\infty}^\infty$ is a partition of $\mathbb{R}$, $|\pi|=\Delta x$. We may resemble discrete cases and write down the formula of “entropy”:</p>$$\sum_i - p(x_i) \log (p(x_i) \Delta x)$$<p style="text-indent: 0; margin-top: 0;"> Taking $\Delta x \rightarrow 0$, we have:</p>$$\begin{align}&\lim_{\Delta x \rightarrow 0}\sum_i - p(x_i) \log (p(x_i) \Delta x) \\=&\int -p(x) \log p(x) dx - \lim_{\Delta x \rightarrow 0}\sum_i - p(x_i) \log \Delta x\end{align}$$<p style="text-indent: 0; margin-top: 0;"> In practice, we omit the second term (since it’s infinite) and define the first one as <strong>differential entropy</strong>:</p>$$h(X):=\int_\mathbb{R} -p(x) \log p(x) dx$$<p>We can define $h(X|Y)$ and $I(X;Y)$ likewisely.</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;Given a discrete r.v. $X$, where $X$ ranged in $\{a_1, \ldots, a_n\}$, $\mathbb{P}(X=a_k)=p_k$. Entropy $H(X)$ is defined as:&lt;/p&gt;
$$H(X)= - \sum_k p_k \log p_k$$

&lt;p&gt;@par When regarded as a function of $\{p_k\}$, entropy satisfies the following properties:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt; $H(p_1,\ldots,p_n)$ is continuous, and non-negative;&lt;/li&gt;
&lt;li&gt; $H(p_1,\ldots,p_n)$ is convex w.r.t. $(p_1,\ldots,p_n)$;&lt;/li&gt;
&lt;li&gt; $H(p_1,\ldots,p_n)$ has a unique maxima $(\frac{1}{n},\ldots,\frac{1}{n})$;&lt;/li&gt;
&lt;li&gt; $H(n):=H(\frac{1}{n},\ldots,\frac{1}{n})$ increases along with $n$;&lt;/li&gt;
&lt;li&gt; $H(p_1,\ldots,p_n)=H(p_1+\ldots+p_k,p_{k+1},\ldots,p_n)+(p_1+\ldots+p_k)H(p_{k+1}&#39;,\ldots,p_n&#39;)$.

&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;@par Property 5 is so-called addictivity. That is, if we observe $X$ in two steps, firstly obtaining a value from $\{\hat{a},a_{k+1},\ldots,a_n\}$ and then another value from $\{a_1,\ldots,a_k\}$ if $\hat{a}$ selected, the entropy of the whole system should be sum of these two subsystems.&lt;/p&gt;
&lt;p&gt;Note that a function satisfying property 1, 4, 5 must have a form of $H(\vec{p})= - C \sum_k p_k \log p_k$, which reveals that entropy function is unique.&lt;/p&gt;
&lt;p&gt;Entropy measures the &lt;strong&gt;uncertainty&lt;/strong&gt; of a random value. Intuitively, entropy reaches its maximum $\log n$ when all alphabets occur with same probability, and likewise has a minimum of $0$ if $p_k=1$ for some $k$.&lt;/p&gt;
&lt;p&gt;Entropy also represents the smallest average length to encode a message. Say we have a message consisting of alphabets $a_1,\ldots,a_n$, occurring with probability $p_1,\ldots,p_n$. Now we want to assign a code (an $N$-ary string) to each alphabet, with no two codes sharing a same prefix. The length of the codes are denoted as $l_1,\ldots,l_n$. &lt;a href=&quot;https://en.wikipedia.org/wiki/Shannon%27s_source_coding_theorem&quot;&gt;Shannon’s source coding theroem&lt;/a&gt; states that the average code length $\sum_k p_k l_k$ could not be less than $H(p_1,\ldots,p_n)$ (taking $N$ as logarithm base).&lt;/p&gt;</summary>
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Information Theory" scheme="http://i.hsfzxjy.site/tags/Information-Theory/"/>
    
  </entry>
  
  <entry>
    <title>铁板烧</title>
    <link href="http://i.hsfzxjy.site/2019-12-10-yakimono/"/>
    <id>http://i.hsfzxjy.site/2019-12-10-yakimono/</id>
    <published>2019-12-09T16:00:00.000Z</published>
    <updated>2020-11-09T08:47:47.986Z</updated>
    
    <content type="html"><![CDATA[<p>最近喜欢上了那家铁板烧。在寸土寸金的北京，那家店占据了食街不大的一角。开放式的厨房几乎就是它的全部。一圈细长的大理石柜台，被椅子围得严严实实。人们就此坐下，点菜，颇有一番日式的味道。隔着不高的玻璃，可以看到三个大厨在柜台另一边忙碌。食材触到铁板的「吱」响，伴随时不时的爆裂声，白色的烟汽袅起，混合着诱人的香味，好不热闹。</p><p>入冬后，食街上的人更多了。日落后，室外温度急剧下降，这时来热闹的地方吃点热热的东西，再合适不过了。独自点上一份肉，一份菜，一碟满满的炒饭，吃得满面油光，奢侈而满足。推门而出，白天发生的不愉快，或是接下来要加班的怠惰，多少缓解了一些。但每次吃完，衣服上，头发上总会带上一股味道——铁板烧的味道，油烟的味道。</p><p>有人不在乎这味道，觉得这是烟火味，是相关料理的灵魂，是生活的气息。在以前，我是很痛恨这种说法的。吃火锅，或是烧烤时，高温食材逸散出的分子，几乎无孔不入，附在衣服上、头发上，就像被泼了一身油。每一回都要努力地洗澡，再将衣服由里至外换一遍，方可除去这种味道。我不能忍受这样的味道，藉而讨厌相关的料理。但近几年，这种想法似乎在慢慢消失，我开始能接受这种味道。尽管还是接受不了烧烤，但这种铁板烧倒是可以，甚至慢慢喜欢上了。</p><p>讨厌烟火味，和睡前一定要洗澡一样，这些观念的背后似乎有着奇妙的机理。人们小时候的经历，或是大人的说教，或是自身的负面遭遇，仿佛在主导这些观念。怕小鸡的以前踩死过小鸡，不敢吃鱼的是被鱼刺卡过。但我的这个观念好像是自发产生的，没有听从任何人表达过这样的观点，而是打心底里就讨厌这种味道。</p><p>如此根深蒂固的观念，又是如何被改变的呢？大概是太忙碌，忙得无暇顾及这类事。烟汽熏天的铁板烧，是我不可多得的闲暇时光。生活于生存之上，生存尚不能满足，又谈何更高层次的吹毛求疵？</p><p>一个习惯的改变，或者是一种生活方式的丧失。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;最近喜欢上了那家铁板烧。在寸土寸金的北京，那家店占据了食街不大的一角。开放式的厨房几乎就是它的全部。一圈细长的大理石柜台，被椅子围得严严实实。人们就此坐下，点菜，颇有一番日式的味道。隔着不高的玻璃，可以看到三个大厨在柜台另一边忙碌。食材触到铁板的「吱」响，伴随时不时的爆裂声</summary>
      
    
    
    
    <category term="Soliloquy" scheme="http://i.hsfzxjy.site/categories/Soliloquy/"/>
    
    
  </entry>
  
  <entry>
    <title>西郊线</title>
    <link href="http://i.hsfzxjy.site/2019-10-03-xjx/"/>
    <id>http://i.hsfzxjy.site/2019-10-03-xjx/</id>
    <published>2019-10-02T16:00:00.000Z</published>
    <updated>2020-11-16T05:57:52.903Z</updated>
    
    <content type="html"><![CDATA[<p>西郊线真的是电车，那种拖着两个辫子，走得不快的电车。沿途的车站也是很小的车站。不到两米宽的站台，甚至比广州的BRT站更窄一些，立了一排凳子，仅此而已。半露天的车站，有遮雨棚，却没有真正意义的墙。透过一人多高的玻璃看，站台外沿长满了狗尾巴草，再往后铺开了大片的草甸和桦林，一直延伸到远处的香山，和仿佛水洗后的天空。午前的秋阳给眼前的一切都染上了愉悦的暖色。</p><a id="more"></a><p>从巴沟逃下环状线，可以乘西郊线继续远离城市。我讨厌高楼林立的城市，也许是因为两个多月两点一线的生活。但我又离不开城市的便利，活像得斯德哥尔摩患者。在仅有的假日出游或许是个好选择，也或许是必要的。和地下的铁棺材不同，驶在地面上的西郊线能让人看清城市的衰减。巴沟往后，城市仿佛被逐渐稀释于山林，房子逐渐变矮直至消失，草甸开始成片最后蔓延开来。草甸有些许枯黄，或是因为季节或是因为纬度，却莫名搭上了秋阳的颜色。人真是奇怪的动物，明明知道自己无法生活于此，却会因这里的景色产生快感。</p><p><img src="https://imbed-1254463987.cos.ap-nanjing.myqcloud.com/xjx-img0.jpg" alt="../assets/2019-10-03-xjx-imgs/img0.jpg&gt;xjx-img0.jpg!width=800"></p><p>车门处无疑是最佳的观景点。将头抵在车门的玻璃上，窗外的世界便充满整个视野。这是一个不同步的世界。极力远眺，大地的尽头几乎是静止的；目光逐渐收回，从桦林到草甸再到脚下的铁轨，世界开始运动起来。西郊线也不同于一般陆上的地铁，不需要开在高架桥上，而是真的「在地上行走」。所谓铁轨，不过是两根细细的钢轨铺设在地面上，地面也没有其他特别之处，就像是行车的公路，水泥作的底板，边缘与荒草相接。这让人不禁产生一种错觉，即电车是直接行走在原野上，无拘无束，劈开半人高的草缓缓前进，就像「千と千尋の神隠し」里的水上火车。</p><p><img src="https://imbed-1254463987.cos.ap-nanjing.myqcloud.com/xjx-img1.jpg" alt="../assets/2019-10-03-xjx-imgs/img1.jpg&gt;xjx-img1.jpg!width=800"></p><p>偶尔有几处，路面突破了草地的禁锢，笔直伸向远方——那便是遇到了匝道口，铁路和公路相交的地方。电车经过时，匝道口当然是亮着红灯的。许多匝道口是空无一人的，没有在等候的灵魂，只有信号灯孤独地亮着。如果匝道口外有个观察者，在他看来，电车短暂地劈开了世界，世界在驶过后又重新相溶，恢复宁静；但从车内看，一闪而过的匝道像是分叉的铁轨，电车由此仿佛真的能驶出草甸，驶向大地的尽头。</p><p>电车为什么迷人呢？电气时代的工业设计是一点，人总是怀旧的。对于熟悉动漫的我，电车也是现实和童话的交叉点。但更重要的是，电车是「自由的」。虽有铁轨约束，坐在电车上的我却有如直接驶于公路上、原野间，与汽车并肩，与自然同行。这是我所不敢企求的。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;西郊线真的是电车，那种拖着两个辫子，走得不快的电车。沿途的车站也是很小的车站。不到两米宽的站台，甚至比广州的BRT站更窄一些，立了一排凳子，仅此而已。半露天的车站，有遮雨棚，却没有真正意义的墙。透过一人多高的玻璃看，站台外沿长满了狗尾巴草，再往后铺开了大片的草甸和桦林，一直延伸到远处的香山，和仿佛水洗后的天空。午前的秋阳给眼前的一切都染上了愉悦的暖色。&lt;/p&gt;</summary>
    
    
    
    <category term="Soliloquy" scheme="http://i.hsfzxjy.site/categories/Soliloquy/"/>
    
    
  </entry>
  
  <entry>
    <title>Proof of the Gumbel Max Trick</title>
    <link href="http://i.hsfzxjy.site/2019-08-01-proof-of-gumbel-max-trick/"/>
    <id>http://i.hsfzxjy.site/2019-08-01-proof-of-gumbel-max-trick/</id>
    <published>2019-07-31T16:00:00.000Z</published>
    <updated>2021-05-07T09:20:31.937Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Statement"><a href="#Statement" class="headerlink" title="Statement"></a>Statement</h2><p>Assume that $\alpha_1, \alpha_2, \ldots, \alpha_n$ satisify $\sum_k\alpha_k=1$. Define     </p>$$Z=\arg\max_k\{\log\alpha_k+G_k\}$$<p style="text-indent: 0; margin-top: 0;"> where $G_1,\ldots,G_n \text{ i.i.d.}\sim Gumbel(0,1)$, whose PDF and CDF are defined as</p>$$\begin{align}f(x)&=e^{-(x+e^{-x})} \\F(x)&=e^{-e^{-x}}\end{align}$$<p style="text-indent: 0; margin-top: 0;">. Then $\mathbb{P}(Z=k)=\alpha_k$.</p><h2 id="Proof"><a href="#Proof" class="headerlink" title="Proof"></a>Proof</h2><p>Set $u_k=\log{\alpha_k}+G_k$. We prove by direct calculations.</p>$$\begin{align}\mathbb{P}(Z=k)&=\mathbb{P}(u_k \geq u_j,\forall j \neq k) \\&=\int_{-\infty}^\infty \mathbb{P}(u_k \geq u_j, \forall j \neq k|u_k)\mathbb{P}(u_k) du_k \\&=\int_{-\infty}^\infty \prod_{j\neq k}\mathbb{P}(u_k \geq u_j|u_k)\mathbb{P}(u_k) du_k \\&=\int_{-\infty}^\infty \prod_{j\neq k}e^{-e^{-u_k+\log \alpha_j}} e^{-(u_k-\log\alpha_k+e^{-(u_k-\log\alpha_k)})} du_k \\&=\int_{-\infty}^\infty e^{-\sum_{j\neq k}\alpha_je^{-u_k}} \alpha_k e^{-(u_k+\alpha_k e^{-u_k})} du_k \\&=\alpha_k \int_{-\infty}^\infty e^{-u_k-(\alpha_k+\sum_{j\neq k}\alpha_j)e^{-u_k}} du_k \\&= \alpha_k\end{align}$$.<h2 id="Application"><a href="#Application" class="headerlink" title="Application"></a>Application</h2><p>The trick is commonly used in DL to make sampling over a discrete distribution differentiable. </p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a href="https://en.wikipedia.org/wiki/Gumbel_distribution">Gumbel Distribution - Wikipedia</a></li><li><a href="https://arxiv.org/pdf/1611.01144.pdf">Categorical Reparameterization with Gumbel-Softmax (ICLR 2017)</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;Statement&quot;&gt;&lt;a href=&quot;#Statement&quot; class=&quot;headerlink&quot; title=&quot;Statement&quot;&gt;&lt;/a&gt;Statement&lt;/h2&gt;&lt;p&gt;Assume that $\alpha_1, \alpha_2, \ldots, \</summary>
      
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Probability" scheme="http://i.hsfzxjy.site/tags/Probability/"/>
    
    <category term="Math" scheme="http://i.hsfzxjy.site/tags/Math/"/>
    
  </entry>
  
  <entry>
    <title>Option::as_ref</title>
    <link href="http://i.hsfzxjy.site/2019-06-26-option-as_ref/"/>
    <id>http://i.hsfzxjy.site/2019-06-26-option-as_ref/</id>
    <published>2019-06-25T16:00:00.000Z</published>
    <updated>2020-11-17T15:28:33.937Z</updated>
    
    <content type="html"><![CDATA[<p>Let’s consider the following function:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::ptr::NonNull;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">transform</span></span>&lt;T&gt;(option: &amp;<span class="built_in">Option</span>&lt;NonNull&lt;T&gt;&gt;) -&gt; <span class="built_in">Option</span>&lt;&amp;T&gt; &#123;</span><br><span class="line">    option.map(|x| <span class="keyword">unsafe</span> &#123; x.as_ref() &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The function <code>transform</code> takes an <code>Option&lt;NonNull&lt;T&gt;&gt;</code> as input, and convert the inner pointer to immutable reference <code>&amp;T</code> if available. The method <code>NonNull::as_ref()</code> is marked unsafe so we need an <code>unsafe</code> block. The snippet causes an compilation error:</p><a id="more"></a><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">error[E0515]: cannot return value referencing function parameter &#96;x&#96;</span><br><span class="line"> --&gt; src&#x2F;main.rs:6:29</span><br><span class="line">  |</span><br><span class="line">6 |     option.map(|x| unsafe &#123; x.as_ref() &#125;)</span><br><span class="line">  |                             -^^^^^^^^^</span><br><span class="line">  |                             |</span><br><span class="line">  |                             returns a value referencing data owned by the current function</span><br><span class="line">  | &#96;x&#96; is borrowed here</span><br></pre></td></tr></table></figure><p>This is a common mistake when transforming <code>Option</code> s. To figure out the problem, we may try to write down the signatures of relevant functions and see how the values flow down:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">Option</span>::<span class="title">map</span></span>&lt;T, F&gt;(<span class="keyword">self</span>, f: F) -&gt; <span class="built_in">Option</span>&lt;&amp;T&gt;</span><br><span class="line">    <span class="keyword">where</span> F: <span class="built_in">FnOnce</span>(NonNull&lt;T&gt;) -&gt; <span class="built_in">Option</span>&lt;&amp;T&gt;</span><br><span class="line">&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// which implies:</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">__closure</span></span>&lt;T&gt;(x: NonNull&lt;T&gt;) -&gt; &amp;T &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">unsafe</span> <span class="function"><span class="keyword">fn</span> <span class="title">NonNull</span>::<span class="title">as_ref</span></span>&lt;T, <span class="symbol">&#x27;a</span>&gt;(&amp;<span class="symbol">&#x27;a</span> <span class="keyword">self</span>) -&gt; &amp;<span class="symbol">&#x27;a</span> T &#123;&#125;</span><br></pre></td></tr></table></figure><p>Here <code>__closure</code> denotes the closure <code>Option::map</code> takes.</p><p>Note that <code>Option::map</code> is a self-consuming method. During the calling, <code>option</code> is destructured, and the internal value with type <code>NonNull&lt;T&gt;</code> flows into <code>__closure</code> . The closure, however, consumes its argument too, so finally <code>x</code> would have a lifetime trapped in the closure. This is what <code>data owned by the current function</code> in the error message means.</p><p>Now let’s look into the closure. Inside we call <code>NonNull::as_ref()</code> to convert a pointer into a reference, which assumes the pointer should live as long as the  reference returned. However, the implication could not be satisfied, since <code>x</code> the pointer lives only within the closure, but the reference would be passed out of the closure.  The compiler consequently complains.</p><p>We can always use <code>Option::as_ref()</code> to solve this type of problems. The function has a signature as below:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">Option</span>::<span class="title">as_ref</span></span>&lt;T&gt;(&amp;<span class="keyword">self</span>) -&gt; <span class="built_in">Option</span>&lt;&amp;T&gt; &#123;&#125;</span><br></pre></td></tr></table></figure><p>Calling <code>.as_ref()</code> on an <code>Option&lt;T&gt;</code> instance, say <code>x</code> , will return another Option with type <code>Option&lt;&amp;T&gt;</code> , say <code>ref_x</code> , which holds the reference to the inner value of <code>x</code> . It’s worth noting that <code>.as_ref()</code> takes <code>x</code> by reference, so no consuming happens here. Now we can perform <code>.map()</code> or <code>.unwrap()</code> on <code>ref_x</code> to extract the internal <code>&amp;T</code> , and call non-consuming methods on it (e.g., <code>NonNull::as_ref</code> ). <code>ref_x</code> will still be dropped after that, but it does not matter, since all it have is a reference.</p><p>We can correct our code like this:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">transform</span></span>&lt;T&gt;(option: &amp;<span class="built_in">Option</span>&lt;NonNull&lt;T&gt;&gt;) -&gt; <span class="built_in">Option</span>&lt;&amp;T&gt; &#123;</span><br><span class="line">    option</span><br><span class="line">        .as_ref()  <span class="comment">// Note here</span></span><br><span class="line">        .map(|x| <span class="keyword">unsafe</span> &#123; x.as_ref() &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;Let’s consider the following function:&lt;/p&gt;
&lt;figure class=&quot;highlight rust&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;use&lt;/span&gt; std::ptr::NonNull;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;fn&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;transform&lt;/span&gt;&lt;/span&gt;&amp;lt;T&amp;gt;(option: &amp;amp;&lt;span class=&quot;built_in&quot;&gt;Option&lt;/span&gt;&amp;lt;NonNull&amp;lt;T&amp;gt;&amp;gt;) -&amp;gt; &lt;span class=&quot;built_in&quot;&gt;Option&lt;/span&gt;&amp;lt;&amp;amp;T&amp;gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    option.map(|x| &lt;span class=&quot;keyword&quot;&gt;unsafe&lt;/span&gt; &amp;#123; x.as_ref() &amp;#125;)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;p&gt;The function &lt;code&gt;transform&lt;/code&gt; takes an &lt;code&gt;Option&amp;lt;NonNull&amp;lt;T&amp;gt;&amp;gt;&lt;/code&gt; as input, and convert the inner pointer to immutable reference &lt;code&gt;&amp;amp;T&lt;/code&gt; if available. The method &lt;code&gt;NonNull::as_ref()&lt;/code&gt; is marked unsafe so we need an &lt;code&gt;unsafe&lt;/code&gt; block. The snippet causes an compilation error:&lt;/p&gt;</summary>
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Rust" scheme="http://i.hsfzxjy.site/tags/Rust/"/>
    
    <category term="Lifetime" scheme="http://i.hsfzxjy.site/tags/Lifetime/"/>
    
  </entry>
  
  <entry>
    <title>Rc and RefCell tricks</title>
    <link href="http://i.hsfzxjy.site/2019-06-23-rc-refcell-pattern/"/>
    <id>http://i.hsfzxjy.site/2019-06-23-rc-refcell-pattern/</id>
    <published>2019-06-22T16:00:00.000Z</published>
    <updated>2020-11-17T15:28:33.937Z</updated>
    
    <content type="html"><![CDATA[<p>Say we need a type <code>Cursor&lt;T&gt;</code> , which contains a mutable reference to <code>T</code> . The type has a method, say <code>.dup()</code> , which returns a new instance of <code>Cursor&lt;T&gt;</code> , while sharing the interior reference across instances. Such design is a common pattern in database driver library. Users might expect to hold multiple cursors simultaneously, each of which owns a reference to the same connection object. </p><p>A possible implementation might look like:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Cursor</span></span>&lt;<span class="symbol">&#x27;a</span>, T&gt; &#123;</span><br><span class="line">    obj: &amp;<span class="symbol">&#x27;a</span> <span class="keyword">mut</span> T,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span>&lt;<span class="symbol">&#x27;a</span>, T&gt; Cursor&lt;<span class="symbol">&#x27;a</span>, T&gt; &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">new</span></span>(t: &amp;<span class="symbol">&#x27;a</span> <span class="keyword">mut</span> T) -&gt; Cursor&lt;<span class="symbol">&#x27;a</span>, T&gt; &#123;</span><br><span class="line">        Cursor &#123; obj: t &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">dup</span></span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>) -&gt; Cursor&lt;T&gt; &#123;</span><br><span class="line">        Cursor &#123; obj: <span class="keyword">self</span>.obj &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> i = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> cursor_a = Cursor::new(&amp;<span class="keyword">mut</span> i);</span><br><span class="line">    <span class="keyword">let</span> _cursor_b = cursor_a.dup();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Perfect and neat, and luckily Rust compiler did not complain. As a freshman in Rust, it’s not easy to keep compiler silent all the time, especially when playing with references. But once I try to chain up the constructor and <code>.dup()</code> , the compiler screams:</p><a id="more"></a><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> i = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">let</span> a = Cursor::new(&amp;<span class="keyword">mut</span> i).dup();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, a.obj);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">   |</span><br><span class="line">21 |     let a &#x3D; Cursor::new(&amp;mut i).dup();</span><br><span class="line">   |             ^^^^^^^^^^^^^^^^^^^      - temporary value is freed at the end of this statement</span><br><span class="line">   |             |</span><br><span class="line">   |             creates a temporary which is freed while still in use</span><br><span class="line">22 |     println!(&quot;&#123;:?&#125;&quot;, a.obj);</span><br><span class="line">   |                      ----- borrow later used here</span><br><span class="line">   |</span><br><span class="line">   &#x3D; note: consider using a &#96;let&#96; binding to create a longer lived value</span><br></pre></td></tr></table></figure><p>It’s wierd. Chaining callings or not should make no difference, at least in most languages – but not in Rust.</p><p>To figure out the problem, let’s expand the signature of <code>.dup()</code> into a more verbose way:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">dup</span></span>&lt;<span class="symbol">&#x27;a</span>&gt;(&amp;<span class="symbol">&#x27;a</span> <span class="keyword">mut</span> <span class="keyword">self</span>) -&gt; Cursor&lt;<span class="symbol">&#x27;a</span>, T&gt; &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Rust compiler allows us to elide the lifetime specifier <code>&#39;a</code> . The signature implies that <code>self</code> should live at least as long as the returned object. However, by chaining the callings, the code is in fact equivalent to:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> __a = Cursor::new(&amp;<span class="keyword">mut</span> i);</span><br><span class="line">    __a.dup()</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>The temporary variable <code>__a</code> lives only within the block, and will be dropped at the end of it. But the return value of <code>__a.dup()</code> is passed out of the block, and thus it has a longer lifetime than <code>__a</code> , which violates the constraints. To fix it, we might instead use a self-consuming version of <code>.dup()</code> , say <code>.into_dup()</code> :</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">into_dup</span></span>(<span class="keyword">self</span>) -&gt; Cursor&lt;<span class="symbol">&#x27;a</span>, T&gt; &#123;</span><br><span class="line">    Cursor &#123; obj: <span class="keyword">self</span>.obj &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>.into_dup()</code> moves all content in <code>self</code> into the new object, and makes no implication on the lifetime. Now we can write code like <code>Cursor::new(&amp;mut i).into_dup()</code> .</p><h2 id="Rc"><a href="#Rc" class="headerlink" title="Rc"></a>Rc</h2><p>The solution above is not satisfactory enough. Apparently we won’t make a self-consuming version for each method, just to please the compiler. In fact, a smart pointer with reference counting may be more suitable for the job. Let’s try rewriting the code with <code>std::rc::Rc</code> :</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::rc::Rc;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Cursor</span></span>&lt;T&gt; &#123;</span><br><span class="line">    obj: Rc&lt;T&gt;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span>&lt;T&gt; Cursor&lt;T&gt; &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">new</span></span>(t: &amp;Rc&lt;T&gt;) -&gt; Cursor&lt;T&gt; &#123;</span><br><span class="line">        Cursor &#123; obj: t.clone() &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">dup</span></span>(&amp;<span class="keyword">self</span>) -&gt; Cursor&lt;T&gt; &#123;</span><br><span class="line">        Cursor &#123;</span><br><span class="line">            obj: <span class="keyword">self</span>.obj.clone(),</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>An <code>Rc</code> instance wraps a pointer (or reference) internally, and cloning the instance implies duplicating the pointer. But from the perspective of compiler, there’s no borrowing involved, and no more need for explicit lifetime declarations in <code>struct</code> and <code>impl</code> . We can now chain the callings as expected:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> i = Rc::new(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">let</span> a = Cursor::new(&amp;i).dup();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;i: &#123;:?&#125; a.obj: &#123;:?&#125;&quot;</span>, a.obj, i);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Output</span></span><br><span class="line"><span class="comment">// i: 1 a.obj: 1</span></span><br></pre></td></tr></table></figure><p>Great! Looks like that <code>Rc</code> is a helper which relaxes the constraints of references, and allow us to do more flexible reference sharing.</p><p>Now let’s try something else – mutating the target via a reference:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> i = Rc::new(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">let</span> a = Cursor::new(&amp;i).dup();</span><br><span class="line">    *Rc::get_mut(&amp;<span class="keyword">mut</span> i).unwrap() = <span class="number">2</span>;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;i: &#123;:?&#125; a.obj: &#123;:?&#125;&quot;</span>, a.obj, i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The 4-th line is more verbose than expected. <code>Rc</code> implements the <code>Deref</code> trait only, but not <code>DerefMut</code> . Instead, it provides <code>Rc::get_mut</code> for mutable dereferencing, which returns an <code>Optional&lt;&amp;mut T&gt;</code> . The <code>Optional</code> wrapper implies a possible failure during dereferencing. This may be wierd at the first sight. An <code>Rc&lt;T&gt;</code> object owns a <code>T</code> object, so the reference should never be invalid.</p><p>Let’s ignore the question for a second and continue. The compiling passes smoothly, but we get a panic at runtime:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">thread &#39;main&#39; panicked at &#39;called &#96;Option::unwrap()&#96; on a &#96;None&#96; value&#39;, src&#x2F;libcore&#x2F;option.rs:347:21</span><br></pre></td></tr></table></figure><p>Oops… <code>Rc::get_mut</code> really returns a <code>None</code> ! To explain this, we might go back to the docs for help:</p><blockquote><ul><li><code>Rc::get_mut</code> Returns a mutable reference to the inner value, if there are no other <code>Rc</code> or <code>Weak</code> pointers to the same value.</li><li><code>Rc::get_mut</code> Returns <code>None</code> otherwise, because it is not safe to mutate a shared value.</li></ul></blockquote><p>The fact is that, <code>Rc::get_mut</code> will perform a run-time check to ensure there’s no other <code>Rc</code> or <code>Weak</code> instances referencing the target, and fails (returns <code>None</code> ) if so. In the code above, there are actually two <code>Rc</code> instances, <code>i</code> and <code>a.obj</code> , sharing a same value internally. Consequently, <code>Rc::get_mut</code> denies the dereferencing. From this perspective, <code>Rc</code> resembles another borrow checker at runtime. </p><p>We might do a quick comparison between <code>Rc</code> s and primitive references:<br><code>Rc</code> :</p><ul><li>At any moment there may exist several <code>Rc&lt;T&gt;</code> instances referencing the same internal value. One can use <code>*</code> to directly deference one of them to immutably access the internal value.</li><li>An <code>Rc&lt;T&gt;</code> instance can be mutably dereferenced if and only if it is the only instance referencing the internal value.</li></ul><p>Reference:</p><ul><li>For a value <code>v</code> with type <code>T</code> , at any moment there may exist several immutable reference to <code>v</code> . Each can be derefenced and returns the pointed value.</li><li>An <code>&amp;mut T</code> to <code>v</code> can exist, if and only if there is no other reference to <code>v</code> .</li></ul><p>Yeah, Rust is safe… But is our design wrong? Not really. We may just misuse the underlying structure.</p><p>The problem is, existence of an <code>Rc&lt;T&gt;</code> instance implies an “immutable reference” to the internal value, and thus disallows mutable access to it in the future. The implication drives out data races, but is too aggressive for our scenario. In some occasions, accessing / writing an object will never collide. A better design is deferring the checking to the time interval value being accessed.</p><h2 id="RefCell-and-Interior-Mutability-Pattern"><a href="#RefCell-and-Interior-Mutability-Pattern" class="headerlink" title="RefCell and Interior Mutability Pattern"></a>RefCell and Interior Mutability Pattern</h2><p>But we still need <code>Rc</code> , since it’s almost the only choice for reference sharing. The trick is that we should find some ways <strong>to mutate the internal value with only an immutable reference provided</strong>.</p><p>In Rust, the concept mutability is “infectious”. Even if we just attempt to partially mutate an object, a mutable reference to the complete object is still needed. It’s inconvenient for objects consisting of several independent parts. Such design is not always bad, but it does not play well with the borrow checker. For more generic scenarios, sometimes mutability of an object should be hidden, so that it can be mutated with only immutable reference provided. This is what we called Interior Mutability Pattern in Rust.</p><p>The corresponding underlying structure is <code>RefCell</code> . A <code>RefCell</code> instance has <code>.borrow()</code> and <code>.borrow_mut()</code> method for immutable or mutable borrowing. Both of the two methods have argument <code>&amp;self</code> , and thus no requirement for compile-time mutability. They will still panic if the borrowing violate the rules, but for most of the time, they can be used without any bother.</p><h2 id="Rc-RefCell"><a href="#Rc-RefCell" class="headerlink" title="Rc + RefCell"></a>Rc + RefCell</h2><p>Let’s use <code>Rc&lt;RefCell&lt;T&gt;&gt;</code> pattern to rewrite our program:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::cell::RefCell;</span><br><span class="line"><span class="keyword">use</span> std::rc::Rc;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Cursor</span></span>&lt;T&gt; &#123;</span><br><span class="line">    obj: Rc&lt;RefCell&lt;T&gt;&gt;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span>&lt;T&gt; Cursor&lt;T&gt; &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">new</span></span>(t: &amp;Rc&lt;RefCell&lt;T&gt;&gt;) -&gt; Cursor&lt;T&gt; &#123;</span><br><span class="line">        Cursor &#123; obj: t.clone() &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">dup</span></span>(&amp;<span class="keyword">self</span>) -&gt; Cursor&lt;T&gt; &#123;</span><br><span class="line">        Cursor &#123;</span><br><span class="line">            obj: <span class="keyword">self</span>.obj.clone(),</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> i = Rc::new(RefCell::new(<span class="number">1</span>));</span><br><span class="line">    <span class="keyword">let</span> a = Cursor::new(&amp;i).dup();</span><br><span class="line">    *i.borrow_mut() = <span class="number">2</span>;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;i: &#123;:?&#125; a.obj: &#123;:?&#125;&quot;</span>, a.obj, i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The declaration get a little more verbose, but, it does work! We can now share the reference between objects, and mutate the interval value via an immutable borrowing. Thanks to the automatic dereference in Rust, the <code>Rc</code> layer is in fact transparent and we can write statement like <code>i.borrow_mut()</code> , which reduces verbosity to some extend.</p><p>Now if we add an additional line:</p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> i = Rc::new(RefCell::new(<span class="number">1</span>));</span><br><span class="line">    <span class="keyword">let</span> a = Cursor::new(&amp;i).dup();</span><br><span class="line">    <span class="keyword">let</span> _<span class="keyword">ref</span> = i.borrow();  <span class="comment">// Another borrow here</span></span><br><span class="line">    *i.borrow_mut() = <span class="number">2</span>;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;i: &#123;:?&#125; a.obj: &#123;:?&#125;&quot;</span>, a.obj, i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The program still compiles, but will panic at runtime:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">thread &#39;main&#39; panicked at &#39;already borrowed: BorrowMutError&#39;, src&#x2F;libcore&#x2F;result.rs:999:5</span><br></pre></td></tr></table></figure><p>At the moment of <code>i.borrow_mut()</code> , there already exists another immutable borrowing <code>_ref</code> , which consequently violates the rules. We can see that <code>RefCell</code> relaxes compile-time constraints, but is still safe enough at runtime.</p><h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul><li><a href="https://doc.rust-lang.org/std/rc/struct.Rc.html#method.get_mut"> <code>Rc::get_mut</code> </a></li><li><a href="https://doc.rust-lang.org/book/ch15-05-interior-mutability.html"> <code>RefCell&lt;T&gt;</code> and the Interior Mutability Pattern </a></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;Say we need a type &lt;code&gt;Cursor&amp;lt;T&amp;gt;&lt;/code&gt; , which contains a mutable reference to &lt;code&gt;T&lt;/code&gt; . The type has a method, say &lt;code&gt;.dup()&lt;/code&gt; , which returns a new instance of &lt;code&gt;Cursor&amp;lt;T&amp;gt;&lt;/code&gt; , while sharing the interior reference across instances. Such design is a common pattern in database driver library. Users might expect to hold multiple cursors simultaneously, each of which owns a reference to the same connection object. &lt;/p&gt;
&lt;p&gt;A possible implementation might look like:&lt;/p&gt;
&lt;figure class=&quot;highlight rust&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;class&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;struct&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;Cursor&lt;/span&gt;&lt;/span&gt;&amp;lt;&lt;span class=&quot;symbol&quot;&gt;&amp;#x27;a&lt;/span&gt;, T&amp;gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    obj: &amp;amp;&lt;span class=&quot;symbol&quot;&gt;&amp;#x27;a&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;mut&lt;/span&gt; T,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;impl&lt;/span&gt;&amp;lt;&lt;span class=&quot;symbol&quot;&gt;&amp;#x27;a&lt;/span&gt;, T&amp;gt; Cursor&amp;lt;&lt;span class=&quot;symbol&quot;&gt;&amp;#x27;a&lt;/span&gt;, T&amp;gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;fn&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;new&lt;/span&gt;&lt;/span&gt;(t: &amp;amp;&lt;span class=&quot;symbol&quot;&gt;&amp;#x27;a&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;mut&lt;/span&gt; T) -&amp;gt; Cursor&amp;lt;&lt;span class=&quot;symbol&quot;&gt;&amp;#x27;a&lt;/span&gt;, T&amp;gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        Cursor &amp;#123; obj: t &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;fn&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;dup&lt;/span&gt;&lt;/span&gt;(&amp;amp;&lt;span class=&quot;keyword&quot;&gt;mut&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;self&lt;/span&gt;) -&amp;gt; Cursor&amp;lt;T&amp;gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;        Cursor &amp;#123; obj: &lt;span class=&quot;keyword&quot;&gt;self&lt;/span&gt;.obj &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;fn&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;main&lt;/span&gt;&lt;/span&gt;() &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;let&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;mut&lt;/span&gt; i = &lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;let&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;mut&lt;/span&gt; cursor_a = Cursor::new(&amp;amp;&lt;span class=&quot;keyword&quot;&gt;mut&lt;/span&gt; i);&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;let&lt;/span&gt; _cursor_b = cursor_a.dup();&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;

&lt;p&gt;Perfect and neat, and luckily Rust compiler did not complain. As a freshman in Rust, it’s not easy to keep compiler silent all the time, especially when playing with references. But once I try to chain up the constructor and &lt;code&gt;.dup()&lt;/code&gt; , the compiler screams:&lt;/p&gt;</summary>
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Rust" scheme="http://i.hsfzxjy.site/tags/Rust/"/>
    
    <category term="Lifetime" scheme="http://i.hsfzxjy.site/tags/Lifetime/"/>
    
  </entry>
  
  <entry>
    <title>三月十日杂感</title>
    <link href="http://i.hsfzxjy.site/2019-03-10-notitle/"/>
    <id>http://i.hsfzxjy.site/2019-03-10-notitle/</id>
    <published>2019-03-09T16:00:00.000Z</published>
    <updated>2020-05-28T09:02:07.749Z</updated>
    
    <content type="html"><![CDATA[<p>我还是说出了那句话。我们走到一起，已经有七天了。</p><p>起初并没有表白的想法。当晚看完电影后，两人都有微妙的感觉，但直到最后也没有表露心意。「就当做又一次平常的约电影好了」目送她进楼时我想。看电影时没有说出来，她回去了我就更说不出来了——我认为我们当时还是普通朋友关系，表白显得太唐突了。我兴奋又失落地回到宿舍。兴奋的是我们之间的关系貌似又近了一些，失落的是这样下去大概率是没有结果的。</p><p>但当晚，她接连发了几条动态，内容和语气都暗示了她对我的好感，只是和我一样，缺乏勇气。两个相互有好感却又没有恋爱经验的人（当然这是我后来知道的），就要这样还未开始便结束了彼此的关系，这怕是上天对胆小鬼的惩罚吧。</p><a id="more"></a><p>但转机在第二天出现了。DJW 在察觉到情况后开始疯狂怂恿我，整整一天没有停过。那天可以说是过得浑浑噩噩的，我的脑海中像是有两只军队在对战，想法瞬息万变，完全没有精力处理其他的事情。可到了晚上，一切开始明晰起来——我下定了决心要表白。我花了很长时间拟好台词，准备应对各种可能出现的情况。</p><p>表白时，我的心脏就像要炸裂一般。起初她感到震惊，表现出了一点犹豫，但最后还是欣然接受了，决定试着和我交往。这在我预想中已经是很好的结果了，在此之前，我从来没想过会被人喜欢。就算是对方有着朦胧的感觉，对我而言也是超幸福的一件事。</p><p>这一周过得是非常开心的。我们开始一起吃饭，一起自习，一起走在回宿舍的路上。我陪她看电影，她喜欢的夏目友人帐；我请她的闺蜜吃饭；我们去游览女生节的活动；她送了我亲手编织的心形绳结；我们一起在操场的草坪上看星星。随着时间的流逝，她也慢慢不那么拘束，我们正在变成人们口中所说的情侣。</p><p>有时我也在后悔，没有更早地表露自己的心意。一年前我们就认识了，走到一起却又花了这么长的时间。大学时光已经过了四分之三，如果更早地在一起，说不定会有更多美好的回忆。我感到自责，觉得自己浪费了她的时间。</p><p>未来，未来又怎么样呢？她想留在本校，我却要去追逐自己真正的理想。站在这个时间节点看我们终要分开的。如果她愿意，也可以异地恋——这很痛苦，但却是最好的结果。这是我一直以来最大的顾虑，也是之前阻碍我表白的一个重要原因。每每想起这件事，我的心里又不由得有些失落。</p><p>但不管怎么样，一切都开始了。这个时间点不算太晚，未来还可以选择。我希望我接下来能扮演好这个角色。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;我还是说出了那句话。我们走到一起，已经有七天了。&lt;/p&gt;
&lt;p&gt;起初并没有表白的想法。当晚看完电影后，两人都有微妙的感觉，但直到最后也没有表露心意。「就当做又一次平常的约电影好了」目送她进楼时我想。看电影时没有说出来，她回去了我就更说不出来了——我认为我们当时还是普通朋友关系，表白显得太唐突了。我兴奋又失落地回到宿舍。兴奋的是我们之间的关系貌似又近了一些，失落的是这样下去大概率是没有结果的。&lt;/p&gt;
&lt;p&gt;但当晚，她接连发了几条动态，内容和语气都暗示了她对我的好感，只是和我一样，缺乏勇气。两个相互有好感却又没有恋爱经验的人（当然这是我后来知道的），就要这样还未开始便结束了彼此的关系，这怕是上天对胆小鬼的惩罚吧。&lt;/p&gt;</summary>
    
    
    
    <category term="Soliloquy" scheme="http://i.hsfzxjy.site/categories/Soliloquy/"/>
    
    
  </entry>
  
  <entry>
    <title>三月一日杂感</title>
    <link href="http://i.hsfzxjy.site/2019-03-01-notitle/"/>
    <id>http://i.hsfzxjy.site/2019-03-01-notitle/</id>
    <published>2019-02-28T16:00:00.000Z</published>
    <updated>2020-05-28T09:02:07.745Z</updated>
    
    <content type="html"><![CDATA[<p>我是怎么想的呢？我应该是喜欢她了。她看起来像是一个单纯的女孩，长得可爱，有些呆，总是一副样子很努力的样子。我们有共同的话题，应该可以谈得来，虽然谈话常因为关系不够深入而戛然而止。</p><p>她是怎么想的呢？她专心起来很少注意周围，我们有许多次出现在同一个场所，但她很少注意到我。看她朋友圈，大学时应该是有过被人纠缠的烦恼。她成绩不是很理想，为未来烦恼着。</p><p>我希求的是一段相互平等的关系，两人的能力和价值观不应该有太大的差距，两人的走近不应该妨碍各自的理想。我不希望我的另一半为了我而放弃自己一直追求的东西。伴侣就是伴侣，不是任何一方的附属品，也不应被任何一方「占有」。如果坚持这个理念，我们的关系想来是不会长久的，至少我走的路，我要去的地方，她不会想去。</p><p>那么，就让这段关系不要开始？这或许是一个好选择。我们的认识源于偶然，一年中保持着模糊的朋友关系。进一步发展，如果她不接受，那我就会陷入尴尬的境地；就算接受，毕业时也是要告别的。没有开始，就不会有结束。</p><p>但或许，我只是个胆小鬼罢了。</p><p>天性の弱虫さ</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;我是怎么想的呢？我应该是喜欢她了。她看起来像是一个单纯的女孩，长得可爱，有些呆，总是一副样子很努力的样子。我们有共同的话题，应该可以谈得来，虽然谈话常因为关系不够深入而戛然而止。&lt;/p&gt;
&lt;p&gt;她是怎么想的呢？她专心起来很少注意周围，我们有许多次出现在同一个场所，但她很少注</summary>
      
    
    
    
    <category term="Soliloquy" scheme="http://i.hsfzxjy.site/categories/Soliloquy/"/>
    
    
  </entry>
  
  <entry>
    <title>二月十一日杂感</title>
    <link href="http://i.hsfzxjy.site/2019-02-11-notitle/"/>
    <id>http://i.hsfzxjy.site/2019-02-11-notitle/</id>
    <published>2019-02-10T16:00:00.000Z</published>
    <updated>2020-05-28T09:02:07.669Z</updated>
    
    <content type="html"><![CDATA[<p>学生时代的我很怕父亲检查我的作文。四年级以前还没有真正意义的作文一说，因此这大概是从四年级开始的，一直持续到高中。父亲是学文的，在单位也常有写报告的工作。修改作文可以说是比较拿手。现在回想起来，父亲指导作文时还算温和，遇到写得不好的地方也不会破口大骂，只是表情比较严肃。但每当这个时候，我都会感到莫名的畏惧，就像是手无寸铁地撞上一个巨人。为此我会努力减少这种机会，时不时谎称没有布置作文作业，或是隐瞒语文测验。</p><p>我是慑于父亲的威严吗？不完全是。我更多地是不想将自己的不完美暴露在父亲面前。在写作这方面，我一直都无法超越父亲。当然这是一种很正常的心理，父亲也不会因为我的不完美而对我不利。尽管如此，我还是对这种事十分敏感。仔细想想，这样的情况还出现在我生活的其它方面。我会因此刻意隐瞒病情，压制心中的迷茫。除非是再三确认过交流过程不会暴露自己的缺陷，我不会轻易和父亲谈话。</p><p>我不知是自己天生性格使然，还是因小时候某个「导火索」事件所致。在其他人面前，我也或多或少会有类似的倾向，但这也有可能是因与父亲的关系而起的。小时候父亲总是很忙，有时甚至会长时间到另一个城市去工作。若要追究这种现象背后的原因，我想，这段经历要承担大部分的责任。</p><p>当然这不是父亲的错，为了生存，面对两难作出的选择，总会留下一些遗憾的。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;学生时代的我很怕父亲检查我的作文。四年级以前还没有真正意义的作文一说，因此这大概是从四年级开始的，一直持续到高中。父亲是学文的，在单位也常有写报告的工作。修改作文可以说是比较拿手。现在回想起来，父亲指导作文时还算温和，遇到写得不好的地方也不会破口大骂，只是表情比较严肃。但每</summary>
      
    
    
    
    <category term="Soliloquy" scheme="http://i.hsfzxjy.site/categories/Soliloquy/"/>
    
    
  </entry>
  
  <entry>
    <title>一月二十六日杂感</title>
    <link href="http://i.hsfzxjy.site/2019-01-26-notitle/"/>
    <id>http://i.hsfzxjy.site/2019-01-26-notitle/</id>
    <published>2019-01-25T16:00:00.000Z</published>
    <updated>2020-05-28T09:02:07.669Z</updated>
    
    <content type="html"><![CDATA[<p>小姑躺在床上，脸色苍白。一根管子从胸口伸出来，随着呼吸剧烈起伏着。长长的管子在床下绕了一圈，接上了一个白色塑料桶，里面尽是浑浊的黄色液体。病床对面坐着阿伯，一脸忧愁的样子。</p><p>我顿时感到有些难过——但也仅此而已。这感觉像是一种怜悯，却不是因为血缘，而是来自本性深处，对病危的同物种的怜悯。换句话说，如果躺在面前的是一个素未谋面的人，我的怜悯不会因此减少半分。这么说很残忍，但确实是我内心的真实写照。这种想法当然没有向任何人透露过，可在自己的博客我不想隐瞒这一点。</p><p>我身后有一个庞大的家族，但我时刻都想着和他们切断联系。这种想法来源已久了，或许是受到母亲的影响。母亲时常叨念家乡的事，这个人的小心眼，那个人的不作为。但由于外婆依旧健在，母亲还是保持着较高的探亲频率。说是探亲，不过也是周末偶尔回去住个两天，便匆匆回来了。四百公里的路，火车跑起来快得很。</p><p>我所不喜欢的，是老家那种到处是熟人的氛围。在那个小镇子上生活的人们从几代以前就互相认识了，出门走几步遇到的人大都能叫出你的名字。熟人社会有着他们自己认为的优点，办事方便，有困难时可以相互扶持。但在我看来这种关系状态是一种束缚。「熟人」们的指指点点会限制你的行为，即使你的行为并没有什么问题。如果将熟人社会看做一个整体，这个庞然大物的思想进步是非常缓慢的。社会中的人相互牵制，根深蒂固的观念不断同化着想要脱离的人。封闭而保守，也许有人喜欢那种状态，反正我是不喜欢的。</p><p>亲人又怎么样呢？如果不是经常见面，相互没有过印象深刻的经历，不过也是「熟人」罢了。血缘这种东西，粗暴地将一个人与另一个人捆绑在一起，物质上或许有一些牵连，但精神关系的认同仍然得看彼此的共同经历。自小我便和父母在另一个城市生活，除了曾经照顾过我的，家族中的许多人，同辈或是不同辈的，对于我都只是「熟人」。但遗憾的是，我常常需要回到这些「熟人」当中，有时甚至需要过多地流露一些感情，让我感到如坐针毡。而这一切，只因为我们有「血缘关系」。</p><p>有人说，计划生育和网络的崛起让我们这一代变得孤独而冷酷。但我觉得，如果孤独和冷酷不影响我们在现代社会生活的话，倒也无可厚非。熟人的相互关心也罢，沉迷于网络中的光怪陆离也罢，都是为了使精神不空虚，这是一个人活下去的必要条件。过去的人们没有过多的方式解决这个问题，但现在有了，我们应该有权利依自己的意志去选择。我们已经过了几千年的熟人/家族式社会，但不意味着这是常态。二三十年前的文学作品描绘了一个个乡土社会，它们透着幸福的气息，却不能使我产生憧憬。科技在瓦解旧的人际关系和社会结构，在此之前生活的人或许会感到痛惜，但在此之后的人只会无感甚至欣喜。我想，之前许多时代的接口处都会有这样的阵痛。</p><p>我盼望着，能尽早告别这一切。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;小姑躺在床上，脸色苍白。一根管子从胸口伸出来，随着呼吸剧烈起伏着。长长的管子在床下绕了一圈，接上了一个白色塑料桶，里面尽是浑浊的黄色液体。病床对面坐着阿伯，一脸忧愁的样子。&lt;/p&gt;
&lt;p&gt;我顿时感到有些难过——但也仅此而已。这感觉像是一种怜悯，却不是因为血缘，而是来自本性深</summary>
      
    
    
    
    <category term="Soliloquy" scheme="http://i.hsfzxjy.site/categories/Soliloquy/"/>
    
    
  </entry>
  
  <entry>
    <title>SS Configuration</title>
    <link href="http://i.hsfzxjy.site/2019-01-21-ss/"/>
    <id>http://i.hsfzxjy.site/2019-01-21-ss/</id>
    <published>2019-01-21T08:50:00.000Z</published>
    <updated>2020-11-17T15:34:20.066Z</updated>
    
    <content type="html"><![CDATA[<h2 id="SS-Client"><a href="#SS-Client" class="headerlink" title="SS Client"></a>SS Client</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ [sudo] pip3 install shadowsocks</span><br></pre></td></tr></table></figure><p><code>/etc/ss.json</code> :</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;server&quot;</span>: <span class="string">&quot;&lt;server ip&gt;&quot;</span>,</span><br><span class="line">    <span class="string">&quot;server_port&quot;</span>: <span class="string">&quot;&lt;server port&gt;&quot;</span>, <span class="comment">// must be Number</span></span><br><span class="line">    <span class="string">&quot;password&quot;</span>: <span class="string">&quot;&lt;password&gt;&quot;</span>,</span><br><span class="line">    <span class="string">&quot;local_address&quot;</span>: <span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">    <span class="string">&quot;local_port&quot;</span>: <span class="number">1081</span>,</span><br><span class="line">    <span class="string">&quot;timeout&quot;</span>: <span class="number">300</span>,</span><br><span class="line">    <span class="string">&quot;method&quot;</span>: <span class="string">&quot;aes-256-cfb&quot;</span>,</span><br><span class="line">    <span class="string">&quot;fast_open&quot;</span>: <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ [sudo] sslocal -c /etc/ss.json -d start</span><br></pre></td></tr></table></figure><h2 id="proxychains"><a href="#proxychains" class="headerlink" title="proxychains"></a>proxychains</h2><p>clone repository from <code>https://github.com/rofl0r/proxychains-ng</code> , <code>make &amp;&amp; sudo make install</code> .</p><p>Append following lines to <code>/etc/proxychains.conf</code> :</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[ProxyList]</span><br><span class="line"># add proxy here ...</span><br><span class="line"># meanwile</span><br><span class="line"># defaults set to &quot;tor&quot;</span><br><span class="line">socks5127.0.0.11081</span><br></pre></td></tr></table></figure><p>Usage: <code>proxychains [command]</code> .</p><h2 id="Chrome-Addons"><a href="#Chrome-Addons" class="headerlink" title="Chrome Addons"></a>Chrome Addons</h2><p><code>SwitchyOmega</code> .</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;SS-Client&quot;&gt;&lt;a href=&quot;#SS-Client&quot; class=&quot;headerlink&quot; title=&quot;SS Client&quot;&gt;&lt;/a&gt;SS Client&lt;/h2&gt;&lt;figure class=&quot;highlight bash&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td</summary>
      
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="SS" scheme="http://i.hsfzxjy.site/tags/SS/"/>
    
    <category term="proxychains" scheme="http://i.hsfzxjy.site/tags/proxychains/"/>
    
  </entry>
  
  <entry>
    <title>一月七日杂感</title>
    <link href="http://i.hsfzxjy.site/2019-01-07-notitle/"/>
    <id>http://i.hsfzxjy.site/2019-01-07-notitle/</id>
    <published>2019-01-06T16:00:00.000Z</published>
    <updated>2020-05-28T09:02:07.669Z</updated>
    
    <content type="html"><![CDATA[<p>前几天和阿三聚了一餐，这大概是一年到头来我们为数不多能好好聊天的时间。</p><p>我们谈了很多，从考试到动漫到未来。令人惊讶的是，他表现出了一种忧虑，对未来的忧虑。在我看来这是很少见的。忧虑的一方面是关于短期的未来，有CSST面试的压力，以及成堆的考试；另一方面是更遥远的未来，暑研或是升学或是就业。再往前两天的团队聚餐上大家的讨论，关于资本寒冬的，关于机器学习的逐渐饱和，都给我们留下了深刻的印象。</p><p>我问他不做金融吗。他说经过暑假的实习他也对这个行业失去了兴趣，量化交易不过也是机械性的脑力劳动（如有冒犯请见谅，这是我凭回忆归纳的，然而我对这个行业一窍不通）。现在他也开始迷茫将来要做什么。</p><p>他说有点羡慕我的状态，对周围的环境不care，沉浸在自己的小世界当中。这个学校没能让他满意，没有地缘优势，身边足够优秀的人还是少了些。但这些在我看来都不重要，甚至还是优点。</p><p>吃完饭后已经是九点半了。雾霾洇开了路灯的光晕，整个城市笼上了一种眩目的橘黄色。</p><p>在那之后，我又断断续续想了很多。机器学习真的是我喜欢的吗？钻研理论时我是开心的，但我没有记笔记的习惯；做实验有时会让我很头疼，冗长而累人，很多时候结果还并不好。让我快乐的其实是在概念间的思维游走，但真正深入细节却是有些乏味的。许多创意到了底层，无非也是各种指标的相互比较——除非你是一个子领域的开山鼻祖。但这需要热情和灵感，我认为我没有。我之所以处在这其中，是因为它热门——至少当时是的，而且不让我讨厌，无论是数学或是 CS。</p><p>我想我最纯粹的快乐就是写代码了，次之就是钻研各种新东西，数学上或程序上的。但这两者都不能让我活下去。逐利的代码是需要迎合市场的，但这很累，而且会包含许多我讨厌的东西。我从来不是一个好的产品经理，也不想成为。我只希望能依自由意志写代码，折腾各种东西，这些产出多是无用的或是无法迎合市场的，但又确实能让我感到快乐。这个愿望其实是奢侈的。如果把「写代码」替成「追番」、「玩手机」、「打机」，那就能契合到很多人身上。我还是需要一门手艺，支撑我活下去，而让我的纯粹爱好成为我的业余游戏。</p><p>既然如此，为什么不尝试将手艺的价值最大化呢？比如出国。在另一个环境凭这门手艺或许能活得更好。我觉得这其中有太多令我望而却步的东西，包括和很多人打交道，包括环境的切换。很多人觉得没什么，但我却十分在意。或许是刻在基因里的一种病吧。不想和人说话；一旦浸入了一个环境，出来时会痛苦万分。升学时也是，回家时也是。想改也改不了。</p><p>所以呢，依然没有结论，未来依旧是迷茫的。这两年多我错过了很多机会，实不相瞒是故意的。但也意外收获了一些好处，或许可以成为新的契机。很多问题要随着时间的流动才能找到答案，我也祈祷如此吧。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;前几天和阿三聚了一餐，这大概是一年到头来我们为数不多能好好聊天的时间。&lt;/p&gt;
&lt;p&gt;我们谈了很多，从考试到动漫到未来。令人惊讶的是，他表现出了一种忧虑，对未来的忧虑。在我看来这是很少见的。忧虑的一方面是关于短期的未来，有CSST面试的压力，以及成堆的考试；另一方面是更遥远</summary>
      
    
    
    
    <category term="Soliloquy" scheme="http://i.hsfzxjy.site/categories/Soliloquy/"/>
    
    
  </entry>
  
  <entry>
    <title>四月·病</title>
    <link href="http://i.hsfzxjy.site/2018-12-31-april-illness/"/>
    <id>http://i.hsfzxjy.site/2018-12-31-april-illness/</id>
    <published>2018-12-30T16:00:00.000Z</published>
    <updated>2020-05-28T09:02:07.665Z</updated>
    
    <content type="html"><![CDATA[<a id="more"></a><p>四月，我大病了一场。先是呼吸时胸口隐隐作痛，我以为是运动伤害，毕竟始发时刚剧烈地骑完车。后而逐渐变成呼吸困难，镜子里的自己嘴唇发紫，睡觉时也像是有人扼着喉咙。我开始在傍晚时发烧。</p><p>清明前后，春寒未散。宿舍已没有了暖气。清明节我在宿舍躺了两天，睡到饭点起来吃，吃完继续睡。两个舍友出去玩了，合肥一直在下雨，屋里甚是寒冷。</p><p>躺在床上，我不停查找着可能的病源。我是个不轻易上医院的人，但这突如其来的状况着实让我害怕。我想在相关疾病的列表中寻求安慰，希望只是一场小病。为什么不上医院呢？去医院一切就定下来了，无论结果是好是坏。但如果不去，就还有无数的可能。我总是这么想。</p><p>入夜时，我也会想，如果这真是糟糕的病又该怎么办呢？我在一个陌生的城市，一切都会变得麻烦；我的人生才刚刚开始，还有很多事没有做。诸如此类我想了很多很多，从黄昏到夜深，混杂着不安与飘忽不定的希冀入睡，再到早晨。</p><p>后来，病似乎开始好转。发烧渐渐没有了，但胸疼和窒息感却交替着。每当胸疼开始减轻时，就会有只无形的手慢慢握紧我的喉咙；手松开时，胸疼又开始发作。如此往复。我数次怀疑自己再也好不了了。四月在浑浑噩噩中度过了。</p><p>庆幸的是，期中考没有被耽误，我妈来看我时也没有发现异样。这话不知怎么的就脱口而出了，连我也觉得自己有些病态。明明可能是危及生命的事情，却要在众人面前尽力隐藏。我认为这种事不好，却一直在做，仿佛被周围人知道后天会塌下来一样。</p><p>接下来的五月，天气暖了起来，胸疼和窒息开始消退。当然我觉得不完全是温度的作用。持续的雨也渐渐停了。我似乎看到了希望。再到六月和炎热的七月，病终于完全好了。我可以自由地跑动，也可以大力地呼吸。这种没有不适感的感觉，客观上只过了三个月，主观上却长的像一生。</p><p>再后来，我没有和任何人提起过这段经历，如今病好了，也更不会了。我不知道这是什么病，现在也不想知道，以后也希望它不要再来了。这三个月里我想了很多很多。当自己恍惚看见时间的尽头时，才突然意识到曾经浪费了许多时间。多次感叹时间不够用，没能做成想做的事，我却把时间花在许多无关紧要的东西上。小时候我讨厌成为这样的人，但还是成为了这样的人。而如今，生活逐渐被紧要却无趣的事填满了。就算我改正这个毛病，乐趣也变少了。无奈。</p><p>而至于为什么又想起这件事，我也不清楚。只是单纯觉得岁末应该写点东西吧。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;a id=&quot;more&quot;&gt;&lt;/a&gt;

&lt;p&gt;四月，我大病了一场。先是呼吸时胸口隐隐作痛，我以为是运动伤害，毕竟始发时刚剧烈地骑完车。后而逐渐变成呼吸困难，镜子里的自己嘴唇发紫，睡觉时也像是有人扼着喉咙。我开始在傍晚时发烧。&lt;/p&gt;
&lt;p&gt;清明前后，春寒未散。宿舍已没有了暖气。清明</summary>
      
    
    
    
    <category term="Soliloquy" scheme="http://i.hsfzxjy.site/categories/Soliloquy/"/>
    
    
  </entry>
  
  <entry>
    <title>Haskell 笔记：State Monad</title>
    <link href="http://i.hsfzxjy.site/2018-12-15-haskell-state-monad/"/>
    <id>http://i.hsfzxjy.site/2018-12-15-haskell-state-monad/</id>
    <published>2018-12-14T16:00:00.000Z</published>
    <updated>2020-05-28T09:02:07.665Z</updated>
    
    <content type="html"><![CDATA[<p>一个依赖于外部状态 <code>s</code> 的伪函数 <code>f&#39; :: a -&gt; b</code>，我们可以将其改写为 <code>f :: a -&gt; s -&gt; (b, s)</code> 使其良定。即，在输入输出中显式传递状态 <code>s</code>。现在，我们需要利用 Monad 将状态传递过程隐藏起来。</p><p>注意到，输出值 <code>(b, s)</code> 中的末状态 <code>s</code> 不仅依赖于输入状态，更依赖于之前更改过状态的一系列函数及其逻辑。因此我们不能简单地将 Monad 定义为 <code>(a, s)</code> 类似的形式，否则两个函数用 <code>&gt;=&gt;</code> 结合的结果将与函数逻辑无关，这与我们的期望不符。</p><p>考虑如下定义：</p><figure class="highlight haskell"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">newtype</span> <span class="type">State</span> s a = &#123; <span class="title">runState</span> :: <span class="title">s</span> -&gt; (<span class="title">a</span>, <span class="title">s</span>) &#125;</span></span><br></pre></td></tr></table></figure><p>由于 <code>-&gt;</code> 的右结合性，<code>f :: a -&gt; s -&gt; (b, s)</code> 和 <code>f :: a -&gt; State s b</code> 等价。固定 <code>s</code>，则 <code>State s</code> 可以成为一个 Monad。一个类型为 <code>State s a</code> 的值通常也被称为一个 state processor。</p><p>现在尝试定义 <code>(&gt;&gt;=) :: State s a -&gt; (a -&gt; State s b) -&gt; State s b</code>。若 <code>p &gt;&gt;= f</code>，则 <code>p</code> 蕴含了在此之前所有的状态处理逻辑，我们希望将 <code>p</code> 和 <code>f</code> 的逻辑融合在一起，成为一个新的 state processor，并作为返回值。</p><figure class="highlight haskell"><table><tr><td class="code"><pre><span class="line"><span class="title">p</span> &gt;&gt;= f = </span><br><span class="line">    (</span><br><span class="line">        <span class="type">State</span> $ \s -&gt; (b, s&#x27;&#x27;)</span><br><span class="line">        <span class="keyword">where</span></span><br><span class="line">            (a, s&#x27;) = (runState p) s</span><br><span class="line">            p2 = f a <span class="comment">-- :: State s b</span></span><br><span class="line">            (b, s&#x27;&#x27;) = (runState p2) s&#x27;</span><br><span class="line">    )</span><br></pre></td></tr></table></figure><p><code>return</code> 是平凡的：</p><figure class="highlight haskell"><table><tr><td class="code"><pre><span class="line"><span class="title">return</span> a = <span class="type">State</span> $ (\s -&gt; (a, s))</span><br></pre></td></tr></table></figure><p><code>fmap</code> 可以作如下定义：</p><figure class="highlight haskell"><table><tr><td class="code"><pre><span class="line"><span class="title">fmap</span> :: (a -&gt; b) -&gt; (<span class="type">State</span> s a) -&gt; (<span class="type">State</span> s b)</span><br><span class="line"><span class="title">fmap</span> f = </span><br><span class="line">    (</span><br><span class="line">        \pIn -&gt; (</span><br><span class="line">            \s -&gt; (b, s&#x27;)</span><br><span class="line">            <span class="keyword">where</span></span><br><span class="line">                (a, s&#x27;) = (runState pIn) s</span><br><span class="line">                b = f a</span><br><span class="line">        )</span><br></pre></td></tr></table></figure><p>如此一来，我们可以将一系列的依赖外部状态的函数串成一个依赖外部状态的函数，传以初始状态，便可得到结果。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;一个依赖于外部状态 &lt;code&gt;s&lt;/code&gt; 的伪函数 &lt;code&gt;f&amp;#39; :: a -&amp;gt; b&lt;/code&gt;，我们可以将其改写为 &lt;code&gt;f :: a -&amp;gt; s -&amp;gt; (b, s)&lt;/code&gt; 使其良定。即，在输入输出中显式传递状态 &lt;co</summary>
      
    
    
    
    <category term="Tech" scheme="http://i.hsfzxjy.site/categories/Tech/"/>
    
    
    <category term="Haskell" scheme="http://i.hsfzxjy.site/tags/Haskell/"/>
    
  </entry>
  
</feed>
