<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>thoughts of hsfzxjy</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://blog.hsfzxjy.site/"/>
  <updated>2019-01-06T09:00:11.954Z</updated>
  <id>http://blog.hsfzxjy.site/</id>
  
  <author>
    <name>hsfzxjy</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Sublime 配置 Rust 环境</title>
    <link href="http://blog.hsfzxjy.site/rust-sublime/"/>
    <id>http://blog.hsfzxjy.site/rust-sublime/</id>
    <published>2019-01-05T16:00:00.000Z</published>
    <updated>2019-01-06T09:00:11.954Z</updated>
    
    <content type="html"><![CDATA[<h2 id="rustup-v-s-cargo"><a href="#rustup-v-s-cargo" class="headerlink" title="rustup v.s. cargo"></a>rustup v.s. cargo</h2><p>cargo 是 Rust <strong>最底层的</strong>包管理器，类似 npm 或 pip。</p>
<p>rustup 是 Rust 的工具链管理器，允许开发者在多个不同版本的工具间切换。所谓工具不仅包括 rustc 和 cargo，还包括 rustfmt、racer 等一系列辅助开发的模块。类似于 Anaconda、Pipenv 之于 Python，或是 n 之于 Node.js。</p>
<p>Rust 的工具链默认存在于 <code>~/.cargo/bin</code> 中，rustup 会用代理脚本覆盖其中的可执行文件，从而用户可以通过命令行标志方便地切换版本。</p>
<h2 id="stable-v-s-beta-v-s-nightly"><a href="#stable-v-s-beta-v-s-nightly" class="headerlink" title="stable v.s. beta v.s. nightly"></a>stable v.s. beta v.s. nightly</h2><p>Rust 官方默认提供三个通道，稳定性依次递降，即时性依次递增。当然，还有众多的第三方版本。</p>
<p>Rust 工具链普遍有一个标志 <code>+&lt;channel&gt;</code> 用于注明通道版本，如：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">$ rustc +nighly --version</div><div class="line">rustc 1.33.0-nightly (68fe5182c 2019-01-05)</div><div class="line">$ rustc +stable --version</div><div class="line">rustc 1.31.1 (b6c32da9b 2018-12-18)</div></pre></td></tr></table></figure>
<h2 id="Sublime-配置"><a href="#Sublime-配置" class="headerlink" title="Sublime 配置"></a>Sublime 配置</h2><p>我们希望让 Sublime 支持如下功能：</p>
<ol>
<li>Auto Formatting</li>
<li>Auto Completement</li>
<li>Check on Save</li>
</ol>
<p>为此需要两个插件：</p>
<ul>
<li>Rust Enhanced（提供 3）</li>
<li>Anaconda Rust（提供 1 和 2）</li>
</ul>
<h3 id="Rust-Enhanced"><a href="#Rust-Enhanced" class="headerlink" title="Rust Enhanced"></a>Rust Enhanced</h3><p>直接安装即可。</p>
<h3 id="Anaconda-Rust"><a href="#Anaconda-Rust" class="headerlink" title="Anaconda Rust"></a>Anaconda Rust</h3><p>官方版本停更了，<code>rustfmt</code> 相关功能有一些问题。为此需要使用我修改过的版本：<a href="https://github.com/hsfzxjy/anaconda_rust" target="_blank" rel="noopener">https://github.com/hsfzxjy/anaconda_rust</a> 。</p>
<p>依赖 <code>rustfmt</code> 和 <code>racer</code>。</p>
<h4 id="racer"><a href="#racer" class="headerlink" title="racer"></a>racer</h4><p><code>racer</code> 需要 <code>nightly</code> 版本，用以下脚本安装：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ cargo +nightly install racer</div></pre></td></tr></table></figure>
<p>此方法需要本地编译，时间比较久。</p>
<p>自动补全的完成还需要一份 Rust 库的源码，可以从<a href="https://forge.rust-lang.org/other-installation-methods.html" target="_blank" rel="noopener">这个页面</a> 下载。</p>
<p>解压完成后需要配置 <code>Anaconda Rust</code> 插件的 <code>rust_src_path</code> 选项。</p>
<h4 id="rustfmt"><a href="#rustfmt" class="headerlink" title="rustfmt"></a>rustfmt</h4><p><code>rustfmt</code> 支持使用 <code>rustup</code> 安装，更省时间：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ rustup component add rustfmt-preview</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;rustup-v-s-cargo&quot;&gt;&lt;a href=&quot;#rustup-v-s-cargo&quot; class=&quot;headerlink&quot; title=&quot;rustup v.s. cargo&quot;&gt;&lt;/a&gt;rustup v.s. cargo&lt;/h2&gt;&lt;p&gt;cargo 是 Rust
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Rust" scheme="http://blog.hsfzxjy.site/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>四月·病</title>
    <link href="http://blog.hsfzxjy.site/april-illness/"/>
    <id>http://blog.hsfzxjy.site/april-illness/</id>
    <published>2018-12-30T16:00:00.000Z</published>
    <updated>2018-12-31T07:31:40.782Z</updated>
    
    <content type="html"><![CDATA[<p>四月，我大病了一场。先是呼吸时胸口隐隐作痛，我以为是运动伤害，毕竟始发时刚剧烈地骑完车。后而逐渐变成呼吸困难，镜子里的自己嘴唇发紫，睡觉时也像是有人扼着喉咙。我开始在傍晚时发烧。</p>
<p>清明前后，春寒未散。宿舍已没有了暖气。清明节我在宿舍躺了两天，睡到饭点起来吃，吃完继续睡。两个舍友出去玩了，合肥一直在下雨，屋里甚是寒冷。</p>
<p>躺在床上，我不停查找着可能的病源。我是个不轻易上医院的人，但这突如其来的状况着实让我害怕。我想在相关疾病的列表中寻求安慰，希望只是一场小病。为什么不上医院呢？去医院一切就定下来了，无论结果是好是坏。但如果不去，就还有无数的可能。我总是这么想。</p>
<p>入夜时，我也会想，如果这真是糟糕的病又该怎么办呢？我在一个陌生的城市，一切都会变得麻烦；我的人生才刚刚开始，还有很多事没有做。诸如此类我想了很多很多，从黄昏到夜深，混杂着不安与飘忽不定的希冀入睡，再到早晨。</p>
<p>后来，病似乎开始好转。发烧渐渐没有了，但胸疼和窒息感却交替着。每当胸疼开始减轻时，就会有只无形的手慢慢握紧我的喉咙；手松开时，胸疼又开始发作。如此往复。我数次怀疑自己再也好不了了。四月在浑浑噩噩中度过了。</p>
<p>庆幸的是，期中考没有被耽误，我妈来看我时也没有发现异样。这话不知怎么的就脱口而出了，连我也觉得自己有些病态。明明可能是危及生命的事情，却要在众人面前尽力隐藏。我认为这种事不好，却一直在做，仿佛被周围人知道后天会塌下来一样。</p>
<p>接下来的五月，天气暖了起来，胸疼和窒息开始消退。当然我觉得不完全是温度的作用。持续的雨也渐渐停了。我似乎看到了希望。再到六月和炎热的七月，病终于完全好了。我可以自由地跑动，也可以大力地呼吸。这种没有不适感的感觉，客观上只过了三个月，主观上却长的像一生。</p>
<p>再后来，我没有和任何人提起过这段经历，如今病好了，也更不会了。我不知道这是什么病，现在也不想知道，以后也希望它不要再来了。这三个月里我想了很多很多。当自己恍惚看见时间的尽头时，才突然意识到曾经浪费了许多时间。多次感叹时间不够用，没能做成想做的事，我却把时间花在许多无关紧要的东西上。小时候我讨厌成为这样的人，但还是成为了这样的人。而如今，生活逐渐被紧要却无趣的事填满了。就算我改正这个毛病，乐趣也变少了。无奈。</p>
<p>而至于为什么又想起这件事，我也不清楚。只是单纯觉得岁末应该写点东西吧。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;四月，我大病了一场。先是呼吸时胸口隐隐作痛，我以为是运动伤害，毕竟始发时刚剧烈地骑完车。后而逐渐变成呼吸困难，镜子里的自己嘴唇发紫，睡觉时也像是有人扼着喉咙。我开始在傍晚时发烧。&lt;/p&gt;
&lt;p&gt;清明前后，春寒未散。宿舍已没有了暖气。清明节我在宿舍躺了两天，睡到饭点起来吃，吃
    
    </summary>
    
      <category term="Essays" scheme="http://blog.hsfzxjy.site/categories/Essays/"/>
    
    
  </entry>
  
  <entry>
    <title>网络时代再读勒庞《乌合之众》</title>
    <link href="http://blog.hsfzxjy.site/Psychologie-des-Foules-network-era/"/>
    <id>http://blog.hsfzxjy.site/Psychologie-des-Foules-network-era/</id>
    <published>2018-12-23T16:00:00.000Z</published>
    <updated>2018-12-23T17:06:27.447Z</updated>
    
    <content type="html"><![CDATA[<p>勒庞所著此书，对社会心理学产生了深远的影响，直至今日仍被人们谈论着。这本书写于遥远的上世纪初，书中的内容，无论是论述方式或是观点，都有着鲜明的时代烙印。后世人难免会觉得勒庞表达了太多的偏见，但书中阐述的对于群体特征、感情与观念等的分析，又的确能解释社会上的一些现象，并给予社会启示。这正如 Robert C. Merton 所评价的“《乌合之众》的当代意义，在于它发现问题的功能而非解决问题的功能”。《乌合之众》提供的更多是一些观念上的指引。要将书中思想运用于某个时代，必须顾及两个时代的背景和群体特征上的差异。尤其是在当今，伴随着互联网的出现，群体的性质也发生了改变。本文将简单赏析《乌合之众》这本书，并尝试结合书中的观点，对网络时代的群体心理进行探讨。</p>
<a id="more"></a>
<p>在书中，勒庞的一个基本观点是“集体精神统一律”，也就是他在开头所提到的“聚集成群的人，他们的感情和思想全都转到了同一个方向，同时他们的个性消失，形成了集体心理”。基于此勒庞探讨了群体的心理特征、群体的信念及影响信念的因素等一系列问题。</p>
<p>上面引用的句子其实有着三个方面的内容。其中“聚集成群的人”点明了研究对象——“群体”。勒庞认为，满足既定条件时，聚集在一起的一群人会表现出一些新的、不同于任一个人品质的特点，从而群体会成为一种特殊的存在。这其中“既定条件”是必不可缺的。黄金时段的商场可以聚集上万人，但他们没有共同目的，不能被称为群体；一支因不满征服而集结成的游行队伍，即使只有区区几十人，却也能形成心理上的统一体。群体中的人有一个特点——这也是第二点内容——即个性消失，心理被同质化。个人进入集体，即意味着其理性和意识的消失，从而进入一种无意识的状态。在这种状态下，文明人会退化成野蛮人。书中此处有一则心理学上的解释：人并非能完全控制自己，通过某些暗示，人可以被代入一种失去自我意识的状态，即常说的“催眠”，而心理群体中的人正是处于这种催眠状态下。这种观点是令人震惊的，并且会让初读者难以接受。同质化进行中，所有的人都无法避免，包括公认的智者与极度理性者。勒庞举了法国大革命中的若干例子，试图佐证这一点。而正是这种个体的无意识，导致了一个更为严重的结果——集体无意识，也就是第三点内容。由于集体无意识的作用，个体受群体心理的支配，很容易会被各种言辞和形象打动，表现出许多只有在群体中才会出现的行为。完全形成的群体很容易受到心理暗示，同时这种暗示会快速地传播到每一个成员身上。</p>
<p>基于此前提，勒庞指出了心理群体的特点，即冲动、易变而急躁；偏执、专横而保守；极易受暗示和亲信，同时道德水平十分低劣。群体表现得像原始人，即使是由文明人组成。除此之外，勒庞还分析了影响群体意见指向的若干因素。其中有间接者，包括成群时间、社会制度以及成员的种族、传统和教育等；同时也有直接因素，如形象、词语和套话，或是幻觉；当然，群体中领袖的作用也不容小觑，一个优秀的群体领袖总会利用各种动员手段操纵群体的信念。</p>
<p>总的来说，勒庞的观点是惊人的，但其论证方式却差强人意，因此也只能称之为观点，而非严密的理论。勒庞使用的是断言加举例的叙述方式。称之为断言，是因其缺乏足够的理论分析。举例的目的也只是对断言进行补充，非科学地佐证。而其中大部分都是革命或战争时期的例子，不具有典型性。除此之外，勒庞的观点中还夹带着对女性及其他种族的偏见。以上都是为后世学者所诟病的地方。整本书通读下来，可以感受到其中浓烈的个人色彩。尽管如此，其基石“集体精神统一律”却是普适于每一个时代的。</p>
<p>迫于时代的限制，勒庞研究而的群体也只是传统意义上的，即同一时空中聚集在一起的一群人。如今，随着互联网日新月异的发展，世界上又诞生了许多新型的群体。这些群体存活于网络之中，体量可以很小，也可以惊人的大；群体可以是封闭的（如 QQ 群等私人小圈子），也可以是开放的（如微博上的众网友）。新型的群体毫无疑问会有和前者不一样的地方。我们不妨将这样的群体称为“虚拟群体”，而将勒庞笔下的较为传统者称为“物理群体”。</p>
<p>那么，相比物理群体，网络上的虚拟群体又有什么特质呢？个人认为有如下两个要点：匿名性和信息对称性。而这两点应该成为我们思考网络群体心理的切入点。</p>
<p>匿名性不用多说，正是网络自身的一大特点。成百上千万网友会于一处，不用知会彼此的姓名，甚至没有物理上近距离的接触，连打照面也不是必须的，仅仅通过文字进行交流（当然，基于网络直播而成立的群体会有更丰富的交流方式）。匿名性有着奇妙的魔力。一方面，它容易让发言者放下言论不当的顾忌，从而让发言者作出与平日性格不符的过激举措——这是许多所谓“键盘侠”的心理起点；另一方面，与其他成员物理上的隔离又会让发言者留存一定的理性。物理群体中个人的盲从与易受暗示，部分因为人们顾忌他人对自己的看法。当某种暗示传播开时，如果自己表现得与其他人不一样，很容易招来周围人质疑的目光。这种质疑在很多人看来，是一种极大的心理压力，迫使个人不得不服从暗示。这是物理群体中“个人无意识”的由来。相比之下，虚拟群体中的个人反而可以对接受到的信息进行一定的思考、甄别，甚至作出与暗示不一致的反应。“大不了被骂一通，眼不见为净就好了”。综合来看，当个人融入虚拟群体时，个人也会表现出与以往不同的特质。这一点与物理群体相同，但原因可能不太一样。个人在群体中仍会保持一定的理性。这正是学者刘朋的观点“网络上的群体不再是一个情绪化集体，而趋向成为个体的相加”。</p>
<p>信息对称性是影响群体行为的另一个重要因素。许多群体事件，如 2003 年 SARS 时期的恐慌、2011 年福岛核泄露时的抢盐事件，其负面结果都是源于信息不对称。这也是群体容易盲从的另一个根源。当群体受到外部刺激时（外界暗示或是突发事件），如果没有足够的信息用于分析情况，刺激很容易在群体内部引起恐慌，从而滋生各种想象、谣言。在前网络时代，信息传播的渠道极为有限，传播的效率也不佳，人们习惯于被动地接收信息。在当时，信息不对称是常态。这也难怪勒庞断言群体没有理性而想象力活跃，毕竟有客观因素的限制。而网络时代的降临在一定程度上打破了这种限制。网络带来的信息对称主要有两个方面。一是作为群体中的成员，个人拥有了更加有力的手段获取信息。事件发生时，如果有意愿，个人完全可以通过各种渠道搜集相关信息，理性分析甄别，作出合适的判断。二是信息的来源，无论是群体中的领导者或是群体外的其他提供者，不在具有绝对的垄断地位。与物理群体不同的是，虚拟群体中的个人可以同时接收来自多个方向的信息。就算革命时期的演讲者在当今网络上慷慨激昂地布道，收到的效果也肯定不如当年（诚然，时代差异不容忽视）。信息对称使虚拟群体中的个人变得理性。这在网络时代初期不太明显——大概是由于传统媒介时代的惯性，而在近几年开始变得清晰起来。人们开始有意识地，并逐渐热衷于拆穿各种谣言。面对刻意性的煽动（一个例子是联合国的官方微博），人们也变得不太容易情绪化。</p>
<p>虚拟群体的这两种特质似乎会使其具有与物理群体不同的表现。个人尚存的理性使得“群体的思想”不容易保持一致。那这是否意味着舆论越来越难以被操纵呢？答案是否定的。事实上，两种群体间仍有着一脉相承的东西。</p>
<p>无论哪个时代，善于操纵舆论者总有层出不穷的办法适应新的变化，其关键点在于把握群体的信念。农民愿意在起义时赴汤蹈火，“文革”时期全国陷入狂热状态，都是因为鼓动者利用了群体的信念，或是切身利益，或是一致的信仰。鼓动者对其稍加提及，并加以渲染，即可使群体情绪化而失去理性。这在如今的网络空间中是难以见到的。网络时代的群体喜欢讲证据。激烈炽热的言辞已经不起作用了，网络群体需要图片、视频等新型证据，才会接受相关的观点或事实。这一部分得益于教育水平的提升——尽管勒庞否认了这一点，事实却证明教育对不盲从有积极作用——另一方面则归功于网民十几年积累下的经验。这种对多媒体信息的信任可以理解为一种信念。一旦有了真假难辨的多媒体信息，群体依然容易盲从。而随着技术的发展，伪造变得越来越容易。就算不使用伪造的手段，他们也可以利用群体中存在的偏见，借助“不完整的事实”或是“莫须有的强调”，对舆论进行操纵。如果某种偏见扎根得足够深入，因刺激而起的情绪完全可以盖过个体尚存的理性。一个例子的是近日的公交车坠河事件，当相关录像未公开时，媒体只是稍微强调了女司机的高跟鞋，随即便引发了网友对其的口诛笔伐。这些偏见可以认为是另一种信念。由此看来，虚拟群体和物理群体没有本质上的区别。只要外界刺激触碰到了群体中某些根深蒂固的爆发点，情绪化依旧会盖过理性。</p>
<p>总而言之，物理上的分隔会带来个人心理上的变化，信息上的对称又会使个人保持理性。二者使虚拟群体的盲从性与无意识性有所改善，却不能从根本改变“群体精神统一”这一点。这是人类动物本性驱使的。如今，网络群体暴力事件依然频频发生，但和之前的每一个时代一样，我们能做的其实很有限。呼吁保持理性，或是及时公开信息、制止谣言，产生的作用并不大。每个时代的无奈，终究还是要继续下去。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;勒庞所著此书，对社会心理学产生了深远的影响，直至今日仍被人们谈论着。这本书写于遥远的上世纪初，书中的内容，无论是论述方式或是观点，都有着鲜明的时代烙印。后世人难免会觉得勒庞表达了太多的偏见，但书中阐述的对于群体特征、感情与观念等的分析，又的确能解释社会上的一些现象，并给予社会启示。这正如 Robert C. Merton 所评价的“《乌合之众》的当代意义，在于它发现问题的功能而非解决问题的功能”。《乌合之众》提供的更多是一些观念上的指引。要将书中思想运用于某个时代，必须顾及两个时代的背景和群体特征上的差异。尤其是在当今，伴随着互联网的出现，群体的性质也发生了改变。本文将简单赏析《乌合之众》这本书，并尝试结合书中的观点，对网络时代的群体心理进行探讨。&lt;/p&gt;
    
    </summary>
    
      <category term="Essays" scheme="http://blog.hsfzxjy.site/categories/Essays/"/>
    
    
  </entry>
  
  <entry>
    <title>Haskell 笔记：State Monad</title>
    <link href="http://blog.hsfzxjy.site/haskell-state-monad/"/>
    <id>http://blog.hsfzxjy.site/haskell-state-monad/</id>
    <published>2018-12-14T16:00:00.000Z</published>
    <updated>2018-12-15T09:16:19.096Z</updated>
    
    <content type="html"><![CDATA[<p>一个依赖于外部状态 <code>s</code> 的伪函数 <code>f&#39; :: a -&gt; b</code>，我们可以将其改写为 <code>f :: a -&gt; s -&gt; (b, s)</code> 使其良定。即，在输入输出中显式传递状态 <code>s</code>。现在，我们需要利用 Monad 将状态传递过程隐藏起来。</p>
<p>注意到，输出值 <code>(b, s)</code> 中的末状态 <code>s</code> 不仅依赖于输入状态，更依赖于之前更改过状态的一系列函数及其逻辑。因此我们不能简单地将 Monad 定义为 <code>(a, s)</code> 类似的形式，否则两个函数用 <code>&gt;=&gt;</code> 结合的结果将与函数逻辑无关，这与我们的期望不符。</p>
<p>考虑如下定义：</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">newtype</span> <span class="type">State</span> s a = &#123; <span class="title">runState</span> :: <span class="title">s</span> -&gt; (<span class="title">a</span>, <span class="title">s</span>) &#125;</span></div></pre></td></tr></table></figure>
<p>由于 <code>-&gt;</code> 的右结合性，<code>f :: a -&gt; s -&gt; (b, s)</code> 和 <code>f :: a -&gt; State s b</code> 等价。固定 <code>s</code>，则 <code>State s</code> 可以成为一个 Monad。一个类型为 <code>State s a</code> 的值通常也被称为一个 state processor。</p>
<p>现在尝试定义 <code>(&gt;&gt;=) :: State s a -&gt; (a -&gt; State s b) -&gt; State s b</code>。若 <code>p &gt;&gt;= f</code>，则 <code>p</code> 蕴含了在此之前所有的状态处理逻辑，我们希望将 <code>p</code> 和 <code>f</code> 的逻辑融合在一起，成为一个新的 state processor，并作为返回值。</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="title">p</span> &gt;&gt;= f = </div><div class="line">    (</div><div class="line">        <span class="type">State</span> $ \s -&gt; (b, s'')</div><div class="line">        <span class="keyword">where</span></div><div class="line">            (a, s') = (runState p) s</div><div class="line">            p2 = f a <span class="comment">-- :: State s b</span></div><div class="line">            (b, s'') = (runState p2) s'</div><div class="line">    )</div></pre></td></tr></table></figure>
<p><code>return</code> 是平凡的：</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="title">return</span> a = <span class="type">State</span> $ (\s -&gt; (a, s))</div></pre></td></tr></table></figure>
<p><code>fmap</code> 可以作如下定义：</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="title">fmap</span> :: (a -&gt; b) -&gt; (<span class="type">State</span> s a) -&gt; (<span class="type">State</span> s b)</div><div class="line"><span class="title">fmap</span> f = </div><div class="line">    (</div><div class="line">        \pIn -&gt; (</div><div class="line">            \s -&gt; (b, s')</div><div class="line">            <span class="keyword">where</span></div><div class="line">                (a, s') = (runState pIn) s</div><div class="line">                b = f a</div><div class="line">        )</div></pre></td></tr></table></figure>
<p>如此一来，我们可以将一系列的依赖外部状态的函数串成一个依赖外部状态的函数，传以初始状态，便可得到结果。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;一个依赖于外部状态 &lt;code&gt;s&lt;/code&gt; 的伪函数 &lt;code&gt;f&amp;#39; :: a -&amp;gt; b&lt;/code&gt;，我们可以将其改写为 &lt;code&gt;f :: a -&amp;gt; s -&amp;gt; (b, s)&lt;/code&gt; 使其良定。即，在输入输出中显式传递状态 &lt;co
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Haskell" scheme="http://blog.hsfzxjy.site/tags/Haskell/"/>
    
  </entry>
  
  <entry>
    <title>Haskell 笔记：Monad 引论</title>
    <link href="http://blog.hsfzxjy.site/haskell-monad/"/>
    <id>http://blog.hsfzxjy.site/haskell-monad/</id>
    <published>2018-12-13T16:00:00.000Z</published>
    <updated>2018-12-15T08:27:43.637Z</updated>
    
    <content type="html"><![CDATA[<h2 id="动机"><a href="#动机" class="headerlink" title="动机"></a>动机</h2><p>pure functions 看似完美，但却不能模拟现实世界中的诸多任务。这是由于 pure functions 是良定的映射，对于特定的输入值会返回唯一的输出。这种模式在面对如下任务时会显得苍白无力：</p>
<ul>
<li>有可能失败的任务。如大多数的 IO。</li>
<li>依赖外部状态的任务。如（伪）随机数生成器。</li>
<li>非确定性任务，即对于确定的输入可能有多个输出。这种在 IP 中较为少见。</li>
<li>对外界会造成影响的任务。如大多数的写入过程。</li>
</ul>
<p>这些问题可以用数学中的<strong>域扩充技巧</strong>来解决。</p>
<h2 id="域扩充"><a href="#域扩充" class="headerlink" title="域扩充"></a>域扩充</h2><p>在数学中，当定义问题的范畴不足以容纳问题的解时，我们通常会对相关的范畴进行扩充。类似的技巧同样也可以应用在这里。</p>
<p>假设一个不良定的函数 <code>f: A -&gt; B</code>：</p>
<ul>
<li>如果 <code>f</code> 有可能失败，我们可以将 <code>B</code> 扩充为 <code>Err(B) ∪ { reasons of failures }</code>，其中 <code>reasons of failures</code> 可能是对异常的描述，也可以是空值一类的东西。则 <code>f&#39;: A -&gt; Err(B)</code> 是良定的映射，且与 <code>f</code> 行为一致。事实上，这就是 Maybe Monad 和 Either Monad。</li>
<li>如果 <code>f</code> 依赖于外部状态，我们定义 <code>Pref(B)</code> 为 <strong>从外部状态空间到 B 的映射的全体</strong>，则 <code>f&#39;: A -&gt; Pref(B)</code> 为良定的映射，且行为和 <code>f</code> 一致。换言之，对于特定的输入 <code>a</code>，<code>f&#39;(a)</code> 返回一个函数，其中蕴含了已知 <code>a</code> 时如何从各种不同状态得到结果的逻辑。事实上，这就是 State Monad。</li>
<li>如果 <code>f</code> 具有非确定性，我们将 <code>B</code> 扩充为 <code>Power(B)</code>，即 <code>B</code> 的幂集。则 <code>f&#39;: A -&gt; Power(B)</code> 为良定的映射，且行为与 <code>f</code> 一致。事实上，这就是 List Monad。</li>
<li>如果 <code>f</code> 依赖于真实世界，我们将 <code>B</code> 扩充为 <code>IO(B)</code>，其中的元素为一些值域为 <code>B</code> 的<strong>伪函数</strong>，可能对真实世界有影响。这些伪函数已经脱离了 pure functions 的范畴，但将它们看成元素是没有问题的。如此一来 <code>f&#39;: A -&gt; IO(B)</code> 为良定的映射，且行为与 <code>f</code> 一致。事实上，这就是 IO Monad。</li>
</ul>
<p>以上操作都有一个共同点，即对一个不良定函数的值域做了扩充，使之变成良定函数。如果用 Haskell 语言描述，它们都有相似的型：<code>f :: a -&gt; m b</code>，其中 <code>m</code> 为扩充规则。</p>
<p>一个问题随之而来：这样的新函数该怎么结合？为此我们要对相关逻辑进行抽象。这就是 Monad。</p>
<h2 id="Monad"><a href="#Monad" class="headerlink" title="Monad"></a>Monad</h2><p>这里我们尝试从实际需求出发，导出一个 Type Constructor 成为 Monad 的必要条件。</p>
<p>约定两个名称：</p>
<ul>
<li>称 <code>a -&gt; m b</code> 型函数为 monadic function</li>
<li>称 <code>a -&gt; b</code> 型函数为 non-monadic function</li>
</ul>
<p>首先需要解决的是 monadic functions 如何结合的问题。这个问题具有重要的现实意义。monadic function 常常代表某种计算任务，它们之间的结合相当于把若干计算任务串行化，而后者是非常常见的需求。</p>
<p>我们希望有一种运算符有如下的类型 <code>(b -&gt; m c) -&gt; (a -&gt; m b) -&gt; (a -&gt; m c)</code>，在此记为 <code>&gt;=&gt;</code> （因其形状，常被叫做 fish operator）。一个自然的想法是，Monad <code>m</code> 需要某种平凡的拆箱操作 <code>extract&#39; :: m a -&gt; a</code>。所谓“平凡”，即 <code>extract&#39;</code> 不应该丢失参数的任何信息。但这往往不能实现，因为 <code>m a</code> 通常会比 <code>a</code> 包含更多的信息，导致 <code>extract&#39;</code> 无法构成良定的映射。例如 <code>Maybe a</code> 中的值 <code>Nothing</code> 就无法在 <code>a</code> 中找到对应的值。</p>
<p>而事实上，我们不需要条件这么强的拆箱操作。在 <code>m</code> 已是 Functor 的情况下，拆箱操作可以弱化为 <code>join :: m (m a) -&gt; m a</code>。我们尝试用 <code>fmap</code>、 <code>join</code> 合成 <code>&gt;=&gt;</code>。</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="title">f</span> :: b -&gt; m c</div><div class="line"><span class="title">g</span> :: a -&gt; m b</div><div class="line"></div><div class="line"><span class="title">fmap</span> f :: m b -&gt; m (m c)</div><div class="line">(fmap f) . g :: a -&gt; m (m c)</div><div class="line"><span class="title">join</span> . (fmap f) . g :: a -&gt; m c</div><div class="line"></div><div class="line"><span class="comment">-- i.e. </span></div><div class="line"></div><div class="line"><span class="title">f</span> &gt;=&gt; g = join . (fmap f) . g</div></pre></td></tr></table></figure>
<p>Functor 的假设是容易成立的。当然我们可以定义多个不同的 <code>fmap</code>，如此产生的 Monad 会有不同的语义。<code>join</code> 的假设也是容易成立的，<code>m (m a)</code> 通常和 <code>m a</code> 包含相同多的信息。故此做法是实际可行的。</p>
<p>我们再考虑 monadic function 和 non-monadic function 结合的问题。期望有如此一个运算：<code>&gt;.&gt; :: (b -&gt; c) -&gt; (a -&gt; m b) -&gt; (a -&gt; m c)</code>。注意，此处返回值是 <code>a -&gt; m c</code> 而不是 <code>a -&gt; c</code>，因为我们不希望 <code>a -&gt; m b</code> 产生的额外信息有所丢失。自然地，我们希望有一个平凡的装箱操作，<code>return :: a -&gt; m a</code>。如此一来便可结合 <code>&gt;=&gt;</code> 完成上面的运算：</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="title">f</span> :: b -&gt; c</div><div class="line"><span class="title">g</span> :: a -&gt; m b</div><div class="line"></div><div class="line"><span class="title">return</span> . f :: b -&gt; m c</div><div class="line">(return . f) &gt;=&gt; g :: a -&gt; m c</div><div class="line"></div><div class="line"><span class="comment">-- i.e.</span></div><div class="line"></div><div class="line"><span class="title">f</span> &gt;.&gt; g :: (return . f) &gt;=&gt; g</div></pre></td></tr></table></figure>
<p>non-monadic function 和 monadic function 另一个方向的结合是平凡的。</p>
<p>综上我们可以得到成为 Monad 的基本条件：</p>
<ul>
<li>是 Functor，存在 <code>fmap :: (a -&gt; b) -&gt; m a -&gt; m b</code></li>
<li>有一个平凡的拆箱操作 <code>join :: m (m a) -&gt; m a</code></li>
<li>有一个平凡的装箱操作 <code>return :: a -&gt; m a</code></li>
</ul>
<p>为了描述平凡，我们要求三个函数必须满足如下公理（下面的 <code>f</code> 为 non-monadic function）：</p>
<ol>
<li><code>return . f == (fmap f) . return</code> （<code>return</code> 的平凡性）</li>
<li><code>join . fmap (fmap f) == (fmap f) . join</code> （<code>join</code> 的平凡性）</li>
</ol>
<blockquote>
<p>事实上在 Category Theory 中，还有另外两条公理：</p>
<ul>
<li><code>join . (fmap join) == join . join</code></li>
<li><code>join . fmap return == join . return == id</code></li>
</ul>
<p>以上四条公理描述了 <code>Id</code>（恒等 Functor）、<code>m</code>、<code>m^2</code>、<code>m^3</code> 之间的泛性质，并使图交换。</p>
</blockquote>
<h2 id="Monad-Typeclass"><a href="#Monad-Typeclass" class="headerlink" title="Monad Typeclass"></a>Monad Typeclass</h2><p>以下为 Prelude 中的定义：</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="type">Functor</span> m =&gt; m a <span class="keyword">where</span></span></div><div class="line"></div><div class="line">    return :: a -&gt; m a</div><div class="line">    (&gt;&gt;=)  :: m a -&gt; (a -&gt; m b) -&gt; m b</div></pre></td></tr></table></figure>
<p>此处没有出现 <code>join</code>，也没有 fish operator，而是使用了一个更常用的算符 <code>&gt;&gt;=</code> （通常称为 bind operator）。这是因为在实际中我们不直接将函数结合，而是使用 non-pointfree 的写法。</p>
<p>此外，还有 <code>&gt;&gt; :: m a -&gt; m b -&gt; m b</code> 运算符。<code>return</code>、<code>&gt;&gt;=</code>、<code>&gt;&gt;</code> 三者是构成 do-notation 的基础。此处不再赘述。</p>
<h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul>
<li><a href="https://en.wikibooks.org/wiki/Haskell/Category_theory" target="_blank" rel="noopener">Haskell/Category Theory</a></li>
<li><a href="https://cdsmith.wordpress.com/2012/04/18/why-do-monads-matter/" target="_blank" rel="noopener">Why Do Monads Matter?</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;动机&quot;&gt;&lt;a href=&quot;#动机&quot; class=&quot;headerlink&quot; title=&quot;动机&quot;&gt;&lt;/a&gt;动机&lt;/h2&gt;&lt;p&gt;pure functions 看似完美，但却不能模拟现实世界中的诸多任务。这是由于 pure functions 是良定的映射，对于特定的输
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Haskell" scheme="http://blog.hsfzxjy.site/tags/Haskell/"/>
    
  </entry>
  
  <entry>
    <title>Haskell 笔记：Applicative</title>
    <link href="http://blog.hsfzxjy.site/haskell-applicative/"/>
    <id>http://blog.hsfzxjy.site/haskell-applicative/</id>
    <published>2018-11-18T02:29:00.000Z</published>
    <updated>2018-11-21T06:25:19.775Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Motivation"><a href="#Motivation" class="headerlink" title="Motivation"></a>Motivation</h2><p><code>Functor</code> solves the problem of mapping regular one-parameter functions into a sub-category, but that’s not easy for functions with more than one parameters.</p>
<p>Let’s consider a function with two parameters <code>f :: a -&gt; b -&gt; c</code>, which can also read as <code>a -&gt; (b -&gt; c)</code>. Applying <code>fmap</code> on <code>f</code>, we will get <code>fmap f :: m a -&gt; m (b -&gt; c)</code>. There’s still some distance from what we want: <code>f&#39; :: m a -&gt; m b -&gt; m c</code>. To get <code>f&#39;</code>, we need a transform from <code>m (b -&gt; c)</code> to <code>m b -&gt; m c</code>. Here we denote it as <code>&lt;*&gt; :: m （b -&gt; c) -&gt; m b -&gt; m c</code>. We will later show that such transform is universal for functions with more parameters.</p>
<p>Now consider a function with three parameters <code>f :: a -&gt; b -&gt; c -&gt; d</code>. We are going to transform it into a wrapped-value version, with the help of <code>fmap</code> and <code>&lt;*&gt;</code>.</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="title">f</span> :: a -&gt; b -&gt; c -&gt; d</div><div class="line"></div><div class="line">(fmap f) :: m a -&gt; m (b -&gt; (c -&gt; d))</div><div class="line"></div><div class="line">\a_ b_ -&gt; (fmap f a_) &lt;*&gt; b_</div><div class="line">    :: m a -&gt; m b -&gt; m (c -&gt; d)</div><div class="line"></div><div class="line">\a_ b_ c_ -&gt; ((fmap f a_) &lt;*&gt; b_) &lt;*&gt; c_</div><div class="line">    :: m a -&gt; m b -&gt; m c -&gt; (m d)</div></pre></td></tr></table></figure>
<p>Here <code>\a_ b_ c_ -&gt; ((fmap f a_) &lt;*&gt; b_) &lt;*&gt; c_</code> is in the desired type. For most of the time, applying parameters directly is actually what we want, instead of the function itself, so the code could simply be written as <code>((fmap f a) &lt;*&gt; b) &lt;*&gt; c</code>, where <code>a</code>, <code>b</code> and <code>c</code> are wrapped values. Parenthesis could be omitted if precedences are set properly, which leads to a neat and easy-to-read form:</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="title">f</span> `fmap` a &lt;*&gt; b &lt;*&gt; c</div></pre></td></tr></table></figure>
<p>In haskell, <code>fmap</code> has an infix name <code>&lt;$&gt;</code>. So finally we get: <code>f &lt;$&gt; a &lt;*&gt; b &lt;*&gt; c</code>.</p>
<h2 id="Applicative"><a href="#Applicative" class="headerlink" title="Applicative"></a>Applicative</h2><p>Haskell pre-defined a type class <code>Applicative</code>, which captures the pattern <code>&lt;*&gt;</code>. Any type that implements <code>Applicative</code> works well with <code>&lt;$&gt;</code> and <code>&lt;*&gt;</code>.</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="type">Functor</span> f =&gt; <span class="type">Applicative</span> (<span class="title">f</span> :: * -&gt; *) <span class="keyword">where</span></span></div><div class="line">  pure :: a -&gt; f a</div><div class="line">  (&lt;*&gt;) :: f (a -&gt; b) -&gt; f a -&gt; f b</div><div class="line">  <span class="type">GHC</span>.<span class="type">Base</span>.liftA2 :: (a -&gt; b -&gt; c) -&gt; f a -&gt; f b -&gt; f c</div><div class="line">  (*&gt;) :: f a -&gt; f b -&gt; f b</div><div class="line">  (&lt;*) :: f a -&gt; f b -&gt; f a</div></pre></td></tr></table></figure>
<p>Note that an <code>Applicative</code> is also a <code>Functor</code>. Apart from <code>&lt;*&gt;</code>, there are some other helper functions or operators in <code>Applicative</code>.</p>
<p><code>pure</code> is equivalent to the default value constructor of <code>f</code>, e.g. <code>(:[])</code> for <code>List</code> or <code>Just</code> for <code>Maybe</code>. This may be handful when lifting an unwrapped value to a wrapped one.</p>
<p><code>liftA2</code> transforms a binary operator to the corresponding version. The function exists as binary operators would be frequently passed among high-order functions.</p>
<p><code>*&gt;</code> takes two wrapped parameters and simply returns the second one, which sequence up two wrapped values. This is quite useful for <code>Applicative</code> with action semantics, such as <code>IO</code>. In fact, it’s so useful that Haskell introduces a syntax sugar for it, known as the <code>do-notation</code>. Particularly:</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="title">do</span></div><div class="line">    putStrLn <span class="string">"1"</span></div><div class="line">    putStrLn <span class="string">"2"</span></div></pre></td></tr></table></figure>
<p>is equivalent to</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="title">putStrLn</span> <span class="string">"1"</span> *&gt; putStrLn <span class="string">"2"</span></div></pre></td></tr></table></figure>
<p><code>&lt;*</code> is similar. Both will be reviewed while studying Monad.</p>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;Motivation&quot;&gt;&lt;a href=&quot;#Motivation&quot; class=&quot;headerlink&quot; title=&quot;Motivation&quot;&gt;&lt;/a&gt;Motivation&lt;/h2&gt;&lt;p&gt;&lt;code&gt;Functor&lt;/code&gt; solves the proble
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Haskell" scheme="http://blog.hsfzxjy.site/tags/Haskell/"/>
    
  </entry>
  
  <entry>
    <title>Haskell 笔记：Category Theory and Functor</title>
    <link href="http://blog.hsfzxjy.site/haskell-category-theory-and-functor/"/>
    <id>http://blog.hsfzxjy.site/haskell-category-theory-and-functor/</id>
    <published>2018-11-18T00:50:00.000Z</published>
    <updated>2018-11-18T08:55:22.260Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Category-Theory"><a href="#Category-Theory" class="headerlink" title="Category Theory"></a>Category Theory</h2><p>A category has three components:</p>
<ul>
<li>A collection of objects.</li>
<li>A collection of morphisms, each of which map one object to another.</li>
<li>A notion of composition of these morphisms, i.e. morphisms can be composed. If <code>f: A -&gt; B</code> and <code>g: B -&gt; C</code> are morphisms, <code>f.g</code> generates a new morphism <code>A -&gt; C</code>.</li>
</ul>
<p>Note that a morphism has no specific semantics of mapping, but simply links two objects together. Morphisms are also called <strong>Arrows</strong>.</p>
<h3 id="Examples"><a href="#Examples" class="headerlink" title="Examples"></a>Examples</h3><h4 id="Set-Category-Set"><a href="#Set-Category-Set" class="headerlink" title="Set Category: Set"></a>Set Category: Set</h4><p>All sets and standard functions form a category. Functions need not to be surjective, since morphisms have no mapping semantics.</p>
<h4 id="Group-Category-Grp"><a href="#Group-Category-Grp" class="headerlink" title="Group Category: Grp"></a>Group Category: Grp</h4><p>All groups and homomorphisms between groups form a category. A group has specific algebaric structure, which morphisms should preserve.</p>
<h3 id="Laws"><a href="#Laws" class="headerlink" title="Laws"></a>Laws</h3><p>Three laws needed to be followed for a category:</p>
<ul>
<li>Composition should be associative.</li>
<li>Composition operation should be closed in the category, i.e. if <code>f: A -&gt; B</code> and <code>g: B -&gt; C</code>, there must be a <code>h: A -&gt; C</code> satisfying <code>h = f . g</code>.</li>
<li>For each object <code>A</code>, there should exist an identity morphism <code>id(A): A -&gt; A</code> s.t. for every <code>f: A -&gt; B</code>, <code>f = id(A) . f = f . id(B)</code>.</li>
</ul>
<p>Note that:</p>
<ul>
<li>There may exist serveral morphisms between <code>A</code> and <code>B</code>.</li>
<li>An identity has type <code>A -&gt; A</code>, but a morphism with such type needs not to be an identity.</li>
</ul>
<h3 id="Functors-in-Category-Theory"><a href="#Functors-in-Category-Theory" class="headerlink" title="Functors in Category Theory"></a>Functors in Category Theory</h3><p>A functor maps a category to another category. It should contains two mappings for objects and for morphisms, with composition operation and category laws preserved.</p>
<p>There’s a natural functor from Grp to Set, which maps groups to their underlying sets and group morphisms to the function which behave the same but are defined on sets instead of groups.</p>
<h2 id="Paramateric-Types-in-Haskell"><a href="#Paramateric-Types-in-Haskell" class="headerlink" title="Paramateric Types in Haskell"></a>Paramateric Types in Haskell</h2><p>It’s common to create new types that hold values of other types. <code>List[a]</code> type constructor creates types that holds sequential values of same type; <code>Maybe[a]</code> creates types that hold operation states (failure, or success with returned values). </p>
<p>Usually we want generated types to inherit functions from types being wrapped. For example, <code>List[Int]</code> should have element-wise addition as <code>Int</code> does, and <code>Maybe[Int]</code> should have similar operations with no burden of re-wrapping and unwrapping. <strong>Such ‘inheritance’ is only concerned with the structure of types, instead of specific functions, so should be done automatically if possible.</strong></p>
<h2 id="Hask-Category"><a href="#Hask-Category" class="headerlink" title="Hask Category"></a>Hask Category</h2><p>Haskell language itself forms a category, with <strong>all types</strong> being objects, and functions being morphisms. Such category is called Hask.</p>
<h2 id="Hask-Functors"><a href="#Hask-Functors" class="headerlink" title="Hask Functors"></a>Hask Functors</h2><figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="type">Functor</span> m <span class="keyword">where</span></span></div><div class="line">    fmap :: (a -&gt; b) -&gt; m a -&gt; m b</div></pre></td></tr></table></figure>
<p>A parameteric type implementing class <code>Functor</code> is a category functor mapping Hask to one of its sub-category, where types <code>m a</code> are the object collection. The type constructor <code>m</code> maps objects, and specific <code>fmap</code> defined on <code>m</code> maps corresponding functions.</p>
<p>It’s worth noted that <code>(a -&gt; b) -&gt; m a -&gt; m b</code> can also read as <code>(a -&gt; b) -&gt; (m a -&gt; m b)</code>, as <code>-&gt;</code> is right-associative. This may provide a clearer view of <code>fmap</code>, which takes a regular function in Hask and returns the corresponding function in sub-category.</p>
<p>Examples:</p>
<p><code>fmap (+) :: List[Int] -&gt; List[Int]</code> generates element-wise addition in <code>List[Int]</code>.</p>
<p><code>fmap (+) :: Maybe Int -&gt; Maybe Int</code> generates such function:</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="title">maybePlus</span> :: <span class="type">Maybe</span> <span class="type">Int</span> -&gt; <span class="type">Maybe</span> <span class="type">Int</span></div><div class="line"><span class="title">maybePlus</span> _        <span class="type">Nothing</span>  = <span class="type">Nothing</span></div><div class="line"><span class="title">maybePlus</span> <span class="type">Nothing</span>  _        = <span class="type">Nothing</span></div><div class="line"><span class="title">maybePlut</span> (<span class="type">Just</span> x) (<span class="type">Just</span> y) = <span class="type">Maybe</span> (x + y)</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;Category-Theory&quot;&gt;&lt;a href=&quot;#Category-Theory&quot; class=&quot;headerlink&quot; title=&quot;Category Theory&quot;&gt;&lt;/a&gt;Category Theory&lt;/h2&gt;&lt;p&gt;A category has thr
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Haskell" scheme="http://blog.hsfzxjy.site/tags/Haskell/"/>
    
  </entry>
  
  <entry>
    <title>Haskell 笔记：data, type, newtype</title>
    <link href="http://blog.hsfzxjy.site/haskell-data-type-newtype/"/>
    <id>http://blog.hsfzxjy.site/haskell-data-type-newtype/</id>
    <published>2018-11-15T16:00:00.000Z</published>
    <updated>2018-11-16T06:12:22.160Z</updated>
    
    <content type="html"><![CDATA[<p>新类型有自己的 data constructor （literals 可以看成特殊的 data constructor），由这一点来区分是否创建了新类型。</p>
<ul>
<li><code>data</code> 创建了新类型，可以有多个 data constructor。</li>
<li><code>newtype</code> 创建了新类型，只能有一个 data constructor，同时新类型的内存布局与原来的类型相同。</li>
<li><code>type</code> 没有创建新类型，只是建立了 alias，没有新的 data constructor。</li>
</ul>
<h2 id="type"><a href="#type" class="headerlink" title="type"></a>type</h2><p>常用于语义化类型，是业务逻辑层的概念。</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">type</span> <span class="type">ID</span> = <span class="type">Int</span></span></div><div class="line"></div><div class="line"><span class="title">a</span> = <span class="number">1</span> :: <span class="type">ID</span></div><div class="line"><span class="title">b</span> = a + <span class="number">2</span> <span class="comment">-- legal</span></div><div class="line"></div><div class="line"><span class="title">showID</span> :: <span class="type">ID</span> -&gt; <span class="type">IO</span> ()</div><div class="line"><span class="title">showID</span> x = print x <span class="comment">-- legal, since Int has already been an instance of class Show</span></div><div class="line"></div><div class="line"><span class="comment">-- illegal, since Int has already been instantiated</span></div><div class="line"><span class="class"><span class="keyword">instance</span> <span class="type">Show</span> <span class="type">ID</span> <span class="keyword">where</span></span></div><div class="line">    <span class="comment">-- ...</span></div></pre></td></tr></table></figure>
<h2 id="newtype"><a href="#newtype" class="headerlink" title="newtype"></a>newtype</h2><p>在编译期创建新类型，但差异在运行期被抹去。带有一个构造器。</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">newtype</span> <span class="type">ID'</span> = <span class="type">ID'</span> <span class="type">Int</span></span></div><div class="line"></div><div class="line"><span class="title">a</span> = <span class="type">ID'</span> <span class="number">1</span></div><div class="line"><span class="title">b</span> = a + <span class="number">2</span> <span class="comment">-- illegal, since Int and ID' are totally different types</span></div><div class="line"></div><div class="line"><span class="title">showID'</span> :: <span class="type">ID'</span> -&gt; <span class="type">IO</span> ()</div><div class="line"><span class="title">showID'</span> x = print x <span class="comment">-- illegal, since ID' is not an instance of Show</span></div><div class="line"></div><div class="line"><span class="comment">-- either</span></div><div class="line"><span class="title">showID'</span> (<span class="type">ID'</span> x) = print x</div><div class="line"><span class="comment">-- or</span></div><div class="line"><span class="class"><span class="keyword">instance</span> <span class="type">Show</span> <span class="type">ID'</span> <span class="keyword">where</span></span></div><div class="line">    show (<span class="type">ID'</span> x) = show x</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;新类型有自己的 data constructor （literals 可以看成特殊的 data constructor），由这一点来区分是否创建了新类型。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;data&lt;/code&gt; 创建了新类型，可以有多个 data constructo
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Haskell" scheme="http://blog.hsfzxjy.site/tags/Haskell/"/>
    
  </entry>
  
  <entry>
    <title>Haskell 笔记：folds</title>
    <link href="http://blog.hsfzxjy.site/haskell-fold/"/>
    <id>http://blog.hsfzxjy.site/haskell-fold/</id>
    <published>2018-11-09T16:00:00.000Z</published>
    <updated>2018-11-14T05:28:14.870Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Prelude-foldl"><a href="#Prelude-foldl" class="headerlink" title="Prelude.foldl"></a>Prelude.foldl</h2><p><code>foldl</code> 为 left-associative folding。</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="title">foldl</span> :: (b -&gt; a -&gt; b) -&gt; b -&gt; [a] -&gt; b</div><div class="line"><span class="title">foldl</span> f acc [] = acc</div><div class="line"><span class="title">foldl</span> f acc (x:xs) = foldl f (f acc x) xs</div></pre></td></tr></table></figure>
<p><code>foldl (+) 0 [1..3]</code> 等价于 <code>(((0 + 1) + 2) + 3)</code>。</p>
<ul>
<li>尾递归，因此有 strict 版本 <code>foldl&#39;</code></li>
<li>求值时必须先到达栈底，遍历完列表，因此无法处理无穷列表</li>
</ul>
<h2 id="Data-List-foldl’"><a href="#Data-List-foldl’" class="headerlink" title="Data.List.foldl’"></a>Data.List.foldl’</h2><p><code>foldl&#39;</code> 为 <code>foldl</code> 的 TRO 版本。</p>
<h2 id="Prelude-foldr"><a href="#Prelude-foldr" class="headerlink" title="Prelude.foldr"></a>Prelude.foldr</h2><p><code>foldr</code> 为 right-associative folding。</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="title">foldr</span> :: (a -&gt; b -&gt; b) -&gt; b -&gt; [a] -&gt; b</div><div class="line"><span class="title">foldr</span> f acc [] = acc</div><div class="line"><span class="title">foldr</span> f acc (x:xs) = f x (foldr f acc xs)</div></pre></td></tr></table></figure>
<p><code>foldr (+) 0 [1..3]</code> 等价于 <code>(0 + (1 + (2 + 3)))</code></p>
<ul>
<li>没有尾递归，有爆栈的危险。</li>
<li>有向右展开的特点，而 Haskell 中许多数据结构都有向右递归的特点（如 Cons），因此可以很好地处理无穷递归的数据，从而更加通用。</li>
</ul>
<h2 id="Prelude-foldl1-amp-amp-Prelude-foldr1"><a href="#Prelude-foldl1-amp-amp-Prelude-foldr1" class="headerlink" title="Prelude.foldl1 &amp;&amp; Prelude.foldr1"></a>Prelude.foldl1 &amp;&amp; Prelude.foldr1</h2><p>Helper functions。将 operator 限制为同一种类型，同时约去 accumulator。</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="title">foldl1</span> :: (a -&gt; a -&gt; a) -&gt; [a] -&gt; a</div><div class="line"><span class="title">foldl1</span> f (x:xs) = foldl f x xs</div><div class="line"><span class="title">foldl1</span> _ [] = error</div><div class="line"></div><div class="line"><span class="title">foldr1</span> :: (a -&gt; a -&gt; a) -&gt; [a] -&gt; a</div><div class="line"><span class="title">foldr1</span> f (x:xs) = foldr f x xs</div><div class="line"><span class="title">foldr1</span> _ [] = error</div></pre></td></tr></table></figure>
<p>即，<code>foldr1</code> 将列表的第一个值作为 accumulator，将剩余部分作为 list，传给 <code>foldr</code>。<code>foldl</code> 同理。</p>
<h2 id="实践"><a href="#实践" class="headerlink" title="实践"></a>实践</h2><h3 id="用-folds-实现-reverse"><a href="#用-folds-实现-reverse" class="headerlink" title="用 folds 实现 reverse"></a>用 folds 实现 <code>reverse</code></h3><figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="title">reversel</span>, reverser :: [a] -&gt; [a]</div><div class="line"><span class="title">reversel</span> list = foldl (\acc x -&gt; x : acc) [] list</div><div class="line"></div><div class="line"><span class="title">reverser</span> list = foldr (\x acc -&gt; acc ++ [x]) [] list</div></pre></td></tr></table></figure>
<h3 id="用-foldr-实现-foldl"><a href="#用-foldr-实现-foldl" class="headerlink" title="用 foldr 实现 foldl"></a>用 foldr 实现 foldl</h3><p>先归纳出 <code>foldr</code> 的泛性质。如果一个函数 <code>g</code> s.t. </p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="title">g</span> [] = v</div><div class="line"><span class="title">g</span> (x:xs) = f x (g xs)</div></pre></td></tr></table></figure>
<p>则 <code>g list === foldr f v list</code>.</p>
<p>再看 <code>foldl</code> 的定义：</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line"><span class="title">foldl</span> f v [] = v</div><div class="line"><span class="title">foldl</span> f v (x:xs) = foldl f (f v x) xs</div><div class="line"></div><div class="line">===&gt;</div><div class="line"></div><div class="line"><span class="title">foldl</span> f v list = g list v</div><div class="line">    <span class="keyword">where</span> </div><div class="line">        g [] v = v</div><div class="line">        g (x:xs) v = g xs (f v x)</div><div class="line">        <span class="comment">-- 从左到右依次更新 v</span></div><div class="line"></div><div class="line">===&gt;</div><div class="line"></div><div class="line"><span class="title">foldl</span> f v list = g list v</div><div class="line">    <span class="keyword">where</span> </div><div class="line">        g [] = id</div><div class="line">        g (x:xs) = \v -&gt; g xs (f v x)</div></pre></td></tr></table></figure>
<p>应有 <code>g (x:xs) === k x (g xs)</code>，我们计算 <code>k</code>：</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="title">g</span> (x:xs)       === k x (g xs)</div><div class="line"><span class="title">g</span> (x:xs) v     === k x (g xs) v</div><div class="line"><span class="title">g</span> xs (f v x)   === k x (g xs) v</div><div class="line">(g xs) (f v x) === k x (g xs) v </div><div class="line"><span class="title">g'</span> (f v x)     === k x g' v</div><div class="line"><span class="title">k</span>              === \x g' v -&gt; g' (f v x)</div></pre></td></tr></table></figure>
<p>所以</p>
<figure class="highlight haskell"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="title">foldl</span> f v xs = </div><div class="line">    (foldr</div><div class="line">        (\x g' v -&gt; g' (f v x))</div><div class="line">        id</div><div class="line">        xs</div><div class="line">    ) v</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;Prelude-foldl&quot;&gt;&lt;a href=&quot;#Prelude-foldl&quot; class=&quot;headerlink&quot; title=&quot;Prelude.foldl&quot;&gt;&lt;/a&gt;Prelude.foldl&lt;/h2&gt;&lt;p&gt;&lt;code&gt;foldl&lt;/code&gt; 为 left-
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Haskell" scheme="http://blog.hsfzxjy.site/tags/Haskell/"/>
    
  </entry>
  
  <entry>
    <title>使用 Aria2 在 Ubuntu 中下载百度云资源</title>
    <link href="http://blog.hsfzxjy.site/download-in-ubuntu-with-aria/"/>
    <id>http://blog.hsfzxjy.site/download-in-ubuntu-with-aria/</id>
    <published>2018-06-10T16:00:00.000Z</published>
    <updated>2018-06-11T05:08:04.832Z</updated>
    
    <content type="html"><![CDATA[<p>可以实现满带宽下载。</p>
<h1 id="配置-Aria2"><a href="#配置-Aria2" class="headerlink" title="配置 Aria2"></a>配置 Aria2</h1><p>从 <a href="https://github.com/aria2/aria2/releases" target="_blank" rel="noopener">Github</a> 下载源码 <code>./configure &amp;&amp; make -j8 &amp;&amp; sudo make install</code>。</p>
<h1 id="配置-Chrome-插件"><a href="#配置-Chrome-插件" class="headerlink" title="配置 Chrome 插件"></a>配置 Chrome 插件</h1><p>clone <code>https://github.com/acgotaku/BaiduExporter</code>。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">$ <span class="built_in">cd</span> ariac</div><div class="line">$ cat &gt; start.sh</div><div class="line">&gt; <span class="comment">#!/bin/bash</span></div><div class="line">&gt; aria2c --conf=aria2.conf</div><div class="line">&gt; ^D</div><div class="line">$ chmod +x start.sh</div><div class="line">$ ./start.sh</div></pre></td></tr></table></figure>
<h1 id="安装-Chrome-插件"><a href="#安装-Chrome-插件" class="headerlink" title="安装 Chrome 插件"></a>安装 Chrome 插件</h1><p>打开 <code>chrome://extensions</code>，<code>Load Unpacked</code> 选择 <code>chrome/release</code>。</p>
<p>完成后在百度云页面上会有 <code>导出下载</code> 按钮。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;可以实现满带宽下载。&lt;/p&gt;
&lt;h1 id=&quot;配置-Aria2&quot;&gt;&lt;a href=&quot;#配置-Aria2&quot; class=&quot;headerlink&quot; title=&quot;配置 Aria2&quot;&gt;&lt;/a&gt;配置 Aria2&lt;/h1&gt;&lt;p&gt;从 &lt;a href=&quot;https://github.co
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Ubuntu" scheme="http://blog.hsfzxjy.site/tags/Ubuntu/"/>
    
  </entry>
  
  <entry>
    <title>从伪并行的 Python 多线程说起</title>
    <link href="http://blog.hsfzxjy.site/python-gil-and-threads/"/>
    <id>http://blog.hsfzxjy.site/python-gil-and-threads/</id>
    <published>2018-03-09T16:00:00.000Z</published>
    <updated>2018-06-06T09:53:03.405Z</updated>
    
    <content type="html"><![CDATA[<h2 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h2><ul>
<li>作者电脑 CPU 为 4 核，因此使用 4 个线程测试是合理的</li>
<li>本文使用的 <code>cpython</code> 版本为 <code>3.6.4</code></li>
<li>本文使用的 <code>pypy</code> 版本为 <code>5.9.0-beta0</code>，兼容 Python 3.5 语法</li>
<li>本文使用的 <code>jython</code> 版本为 <code>2.7.0</code>，兼容 Python 2.7 语法</li>
<li>若无特殊说明，作语言解时，<code>python</code> 指 Python 语言；作解释器解时，<code>python</code> 指 <code>cpython</code></li>
</ul>
<a id="more"></a>
<p>本文使用的测速函数代码如下：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">from</span> __future__ <span class="keyword">import</span> print_function</div><div class="line"></div><div class="line"><span class="keyword">import</span> sys</div><div class="line">PY2 = sys.version_info[<span class="number">0</span>] == <span class="number">2</span></div><div class="line"></div><div class="line"><span class="comment"># 因为 Jython 不兼容 Python 3 语法，此处必须 hack 掉 range 以保证都是迭代器版本</span></div><div class="line"><span class="keyword">if</span> PY2:</div><div class="line">    range = xrange  <span class="comment"># noqa</span></div><div class="line"></div><div class="line"><span class="keyword">from</span> time <span class="keyword">import</span> time</div><div class="line"><span class="keyword">from</span> threading <span class="keyword">import</span> Thread</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">spawn_n_threads</span><span class="params">(n, target)</span>:</span></div><div class="line">    <span class="string">"""</span></div><div class="line">    启动 n 个线程并行执行 target 函数</div><div class="line">    """</div><div class="line"></div><div class="line">    threads = []</div><div class="line"></div><div class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> range(n):</div><div class="line">        thread = Thread(target=target)</div><div class="line">        thread.start()</div><div class="line">        threads.append(thread)</div><div class="line"></div><div class="line">    <span class="keyword">for</span> thread <span class="keyword">in</span> threads:</div><div class="line">        thread.join()</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">(target, number=<span class="number">10</span>, spawner=spawn_n_threads)</span>:</span></div><div class="line">    <span class="string">"""</span></div><div class="line">    分别启动 1, 2, 3, 4 个控制流，重复 number 次，计算运行耗时</div><div class="line">    """</div><div class="line"></div><div class="line">    <span class="keyword">for</span> n <span class="keyword">in</span> (<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, ):</div><div class="line"></div><div class="line">        start_time = time()</div><div class="line">        <span class="keyword">for</span> _ <span class="keyword">in</span> range(number):  <span class="comment"># 执行 number 次以减少偶然误差</span></div><div class="line">            spawner(n, target)</div><div class="line">        end_time = time()</div><div class="line"></div><div class="line">        print(<span class="string">'Time elapsed with &#123;&#125; branch(es): &#123;:.6f&#125; sec(s)'</span>.format(n, end_time - start_time))</div></pre></td></tr></table></figure>
<h2 id="并行？伪并行？"><a href="#并行？伪并行？" class="headerlink" title="并行？伪并行？"></a>并行？伪并行？</h2><p>学过操作系统的同学都知道，线程是现代操作系统底层一种轻量级的多任务机制。一个进程空间中可以存在多个线程，每个线程代表一条控制流，共享全局进程空间的变量，又有自己私有的内存空间。</p>
<p>多个线程可以同时执行。此处的“同时”，在较早的单核架构中表现为“伪并行”，即让线程以极短的时间间隔交替执行，从人的感觉上看它们就像在同时执行一样。但由于仅有一个运算单元，当线程皆执行计算密集型任务时，多线程可能会出现 1 + 1 &gt; 2 的反效果。</p>
<p>而“真正的并行”只能在多核架构上实现。对于计算密集型任务，巧妙地使用多线程或多进程将其分配至多个 CPU 上，通常可以成倍地缩短运算时间。</p>
<p>作为一门优秀的语言，python 为我们提供了操纵线程的库 <code>threading</code>。使用 <code>threading</code>，我们可以很方便地进行并行编程。但下面的例子可能会让你对“并行”的真实性产生怀疑。</p>
<p>假设我们有一个计算斐波那契数列的函数：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">fib</span><span class="params">()</span>:</span></div><div class="line"></div><div class="line">    a = b = <span class="number">1</span></div><div class="line"></div><div class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">100000</span>):</div><div class="line">        a, b = b, a + b</div></pre></td></tr></table></figure>
<p>此处我们不记录其结果，只是为了让它产生一定的计算量，使运算时间开销远大于线程创建、切换的时间开销。现在我们执行 <code>test(fib)</code>，尝试在不同数量的线程中执行这个函数。如果线程是“真并行”，时间开销应该不会随线程数大幅上涨。但执行结果却让我们大跌眼镜：</p>
<figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># CPython，fib</span></div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">1</span> branch(es): <span class="number">1.246095</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">2</span> branch(es): <span class="number">2.535884</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">3</span> branch(es): <span class="number">3.837506</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">4</span> branch(es): <span class="number">5.107638</span> <span class="built_in">sec</span>(s)</div></pre></td></tr></table></figure>
<p>从结果中可以发现：时间开销几乎是正比于线程数的！这明显和多核架构的“真并行”相矛盾。这是为什么呢？</p>
<p>一切的罪魁祸首都是一个叫 GIL 的东西。</p>
<h2 id="GIL"><a href="#GIL" class="headerlink" title="GIL"></a>GIL</h2><h3 id="GIL-是什么"><a href="#GIL-是什么" class="headerlink" title="GIL 是什么"></a>GIL 是什么</h3><p>GIL 的全名是 the Global Interpreter Lock （全局解释锁），是常规 python 解释器（当然，有些解释器没有）的核心部件。我们看看官方的解释：</p>
<blockquote>
<p>The Python interpreter is not fully thread-safe. In order to support multi-threaded Python programs, there’s a global lock, called <strong>the global interpreter lock</strong> or <strong>GIL</strong>, that must be held by the current thread before it can safely access Python objects.</p>
<p>– via <a href="https://docs.python.org/3/c-api/init.html#thread-state-and-the-global-interpreter-lock" target="_blank" rel="noopener">Python 3.6.4 Documentation</a></p>
</blockquote>
<p>可见，这是一个用于保护 Python 内部对象的全局锁（在进程空间中唯一），保障了解释器的线程安全。</p>
<p>这里用一个形象的例子来说明 GIL 的必要性（对资源抢占问题非常熟悉的可以跳过不看）：</p>
<blockquote>
<p>我们把整个进程空间看做一个车间，把线程看成是多条不相交的流水线，把线程控制流中的字节码看作是流水线上待处理的物品。Python 解释器是工人，整个车间仅此一名。操作系统是一只上帝之手，会随时把工人从一条流水线调到另一条——这种“随时”是不由分说的，即不管处理完当前物品与否。    </p>
<p>若没有 GIL。假设工人正在流水线 A 处理 A1 物品，根据 A1 的需要将房间温度（一个全局对象）调到了 20 度。这时上帝之手发动了，工人被调到流水线 B 处理 B1 物品，根据 B1 的需要又将房间温度调到了 50 度。这时上帝之手又发动了，工人又调回 A 继续处理 A1。但此时 A1 暴露在了 50 度的环境中，安全问题就此产生了。</p>
<p>而 GIL 相当于一条锁链，一旦工人开始处理某条流水线上的物品，GIL 便会将工人和该流水线锁在一起。而被锁住的工人只会处理该流水线上的物品。就算突然被调到另一条流水线，他也不会干活，而是干等至重新调回原来的流水线。这样每个物品在被处理的过程中便总是能保证全局环境不会突变。</p>
</blockquote>
<p>GIL 保证了线程安全性，但很显然也带来了一个问题：每个时刻只有一条线程在执行，即使在多核架构中也是如此——毕竟，解释器只有一个。如此一来，单进程的 Python 程序便无法利用到多核的优势了。</p>
<h3 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h3><p>为了验证确实是 GIL 搞的鬼，我们可以用不同的解释器再执行一次。这里使用 pypy（有 GIL）和 jython （无 GIL）作测试：</p>
<figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># PyPy, fib</span></div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">1</span> branch(es): <span class="number">0.868052</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">2</span> branch(es): <span class="number">1.706454</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">3</span> branch(es): <span class="number">2.594260</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">4</span> branch(es): <span class="number">3.449946</span> <span class="built_in">sec</span>(s)</div></pre></td></tr></table></figure>
<figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># Jython, fib</span></div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">1</span> branch(es): <span class="number">2.984000</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">2</span> branch(es): <span class="number">3.058000</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">3</span> branch(es): <span class="number">4.404000</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">4</span> branch(es): <span class="number">5.357000</span> <span class="built_in">sec</span>(s)</div></pre></td></tr></table></figure>
<p>从结果可以看出，用 pypy 执行时，时间开销和线程数也是几乎成正比的；而 jython 的时间开销则是以较为缓慢的速度增长的。jython 由于下面还有一层 JVM，单线程的执行速度很慢，但在线程数达到 4 时，时间开销只有单线程的两倍不到，仅仅稍逊于 cpython 的 4 线程运行结果（5.10 secs）。由此可见，<strong>GIL 确实是造成伪并行现象的主要因素</strong>。</p>
<h2 id="如何解决？"><a href="#如何解决？" class="headerlink" title="如何解决？"></a>如何解决？</h2><p>GIL 是 Python 解释器正确运行的保证，Python 语言本身没有提供任何机制访问它。但在特定场合，我们仍有办法降低它对效率的影响。</p>
<h3 id="使用多进程"><a href="#使用多进程" class="headerlink" title="使用多进程"></a>使用多进程</h3><p>线程间会竞争资源是因为它们共享同一个进程空间，但进程的内存空间是独立的，自然也就没有必要使用解释锁了。</p>
<p>许多人非常忌讳使用多进程，理由是进程操作（创建、切换）的时间开销太大了，而且会占用更多的内存。这种担心其实没有必要——除非是对并发量要求很高的应用（如服务器），多进程增加的时空开销其实都在可以接受的范围中。更何况，我们可以使用进程池减少频繁创建进程带来的开销。</p>
<p>下面新建一个 <code>spawner</code>，以演示多进程带来的性能提升：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">spawn_n_processes</span><span class="params">(n, target)</span>:</span></div><div class="line"></div><div class="line">    threads = []</div><div class="line"></div><div class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> range(n):</div><div class="line">        thread = Process(target=target)</div><div class="line">        thread.start()</div><div class="line">        threads.append(thread)</div><div class="line"></div><div class="line">    <span class="keyword">for</span> thread <span class="keyword">in</span> threads:</div><div class="line">        thread.join()</div></pre></td></tr></table></figure>
<p>使用 cpython 执行 <code>test(fib, spawner=spawn_n_processes)</code>，结果如下：</p>
<figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># CPython, fib, multi-processing</span></div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">1</span> branch(es): <span class="number">1.260981</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">2</span> branch(es): <span class="number">1.343570</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">3</span> branch(es): <span class="number">2.183770</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">4</span> branch(es): <span class="number">2.732911</span> <span class="built_in">sec</span>(s)</div></pre></td></tr></table></figure>
<p>可见这里出现了“真正的并行”，程序效率得到了提升。</p>
<h3 id="使用-C-扩展"><a href="#使用-C-扩展" class="headerlink" title="使用 C 扩展"></a>使用 C 扩展</h3><p>GIL 并不是完全的黑箱，CPython 在解释器层提供了控制 GIL 的开关——这就是 <code>Py_BEGIN_ALLOW_THREADS</code> 和 <code>Py_END_ALLOW_THREADS</code> 宏。这一对宏允许你在自定义的 C 扩展中释放 GIL，从而可以重新利用多核的优势。</p>
<p>沿用上面的例子，自定义的 C 扩展函数好比是流水线上一个特殊的物品。这个物品承诺自己不依赖全局环境，同时也不会要求工人去改变全局环境。同时它带有 <code>Py_BEGIN_ALLOW_THREADS</code> 和 <code>Py_END_ALLOW_THREADS</code> 两个机关，前者能砍断 GIL 锁链，这样工人被调度走后不需要干等，而是可以直接干活；后者则将锁链重新锁上，保证操作的一致性。</p>
<p>这里同样用一个 C 扩展做演示。由于 C 实现的斐波那契数列计算过快，此处采用另一个计算 PI 的函数：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// cfib.c</span></div><div class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;python3.6m/Python.h&gt;</span></span></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">static</span> PyObject* <span class="title">fib</span><span class="params">(PyObject* self, PyObject* args)</span></span></div><div class="line">&#123;</div><div class="line">    Py_BEGIN_ALLOW_THREADS</div><div class="line">    <span class="keyword">double</span> n = <span class="number">90000000</span>, i;</div><div class="line">    <span class="keyword">double</span> s = <span class="number">1</span>;</div><div class="line">    <span class="keyword">double</span> pi = <span class="number">3</span>;</div><div class="line"></div><div class="line">    <span class="keyword">for</span> (i = <span class="number">2</span>; i &lt;= n * <span class="number">2</span>; i += <span class="number">2</span>) &#123;</div><div class="line">        pi = pi + s * (<span class="number">4</span> / (i * (i + <span class="number">1</span>) * (i + <span class="number">2</span>)));</div><div class="line">        s = -s;</div><div class="line">    &#125;</div><div class="line">    Py_END_ALLOW_THREADS</div><div class="line">    <span class="keyword">return</span> Py_None;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="comment">// 模块初始化代码略去</span></div></pre></td></tr></table></figure>
<p>使用 cpython 执行 <code>test(cfib.fib)</code>，结果如下：</p>
<figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># CPython, cfib, non-GIL</span></div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">1</span> branch(es): <span class="number">1.334247</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">2</span> branch(es): <span class="number">1.439759</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">3</span> branch(es): <span class="number">1.603779</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">4</span> branch(es): <span class="number">1.689330</span> <span class="built_in">sec</span>(s)</div></pre></td></tr></table></figure>
<p>若注释掉以上两个宏，则结果如下：</p>
<figure class="highlight coq"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"># CPython, cfib, <span class="built_in">with</span>-GIL</div><div class="line"><span class="keyword">Time</span> elapsed <span class="built_in">with</span> <span class="number">1</span> branch(es): <span class="number">1.331415</span> sec(s)</div><div class="line"><span class="keyword">Time</span> elapsed <span class="built_in">with</span> <span class="number">2</span> branch(es): <span class="number">2.671651</span> sec(s)</div><div class="line"><span class="keyword">Time</span> elapsed <span class="built_in">with</span> <span class="number">3</span> branch(es): <span class="number">4.022696</span> sec(s)</div><div class="line"><span class="keyword">Time</span> elapsed <span class="built_in">with</span> <span class="number">4</span> branch(es): <span class="number">5.337917</span> sec(s)</div></pre></td></tr></table></figure>
<p>可见其中的性能差异。因此当你想做一些计算密集型任务时，不妨尝试用 C 实现，以此规避 GIL。</p>
<p>值得注意的是，一些著名的科学计算库（如 <code>numpy</code>）为了提升性能，其底层也是用 C 实现的，并且会在做一些线程安全操作（如 <code>numpy</code> 的数组操作）时释放 GIL。因此对于这些库，我们可以放心地使用多线程。以下是一个例子：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">import</span> numpy</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">np_example</span><span class="params">()</span>:</span></div><div class="line">    ones = numpy.ones(<span class="number">10000000</span>)</div><div class="line">    numpy.exp(ones)</div></pre></td></tr></table></figure>
<p>用 CPython 执行 <code>test(np_example)</code> 结果如下：</p>
<figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># CPython, np_example</span></div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">1</span> branch(es): <span class="number">3.708392</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">2</span> branch(es): <span class="number">2.462703</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">3</span> branch(es): <span class="number">3.578331</span> <span class="built_in">sec</span>(s)</div><div class="line">Time elapsed <span class="keyword">with</span> <span class="number">4</span> branch(es): <span class="number">4.276800</span> <span class="built_in">sec</span>(s)</div></pre></td></tr></table></figure>
<h3 id="让线程做该做的事"><a href="#让线程做该做的事" class="headerlink" title="让线程做该做的事"></a>让线程做该做的事</h3><p>读到这，有同学可能会奇怪了：我在使用 python 多线程写爬虫时可从来没有这种问题啊——用 4 个线程下载 4 个页面的时间与单线程下载一个页面的时间相差无几。</p>
<p>这里就要谈到 GIL 的第二种释放时机了。除了调用 <code>Py_BEGIN_ALLOW_THREADS</code>，解释器还会在<strong>发生阻塞 IO</strong>（如网络、文件）时释放 GIL。发生阻塞 IO 时，调用方线程会被挂起，无法进行任何操作，直至内核返回；IO 函数一般是原子性的，这确保了调用的线程安全性。因此在大多数阻塞 IO 发生时，解释器没有理由加锁。</p>
<p>以爬虫为例：当 Thread1 发起对 Page1 的请求后，Thread1 会被挂起，<strong>此时 GIL 释放</strong>。当控制流切换至 Thread2 时，由于没有 GIL，不必干等，而是可以直接请求 Page2……如此一来，四个请求可以认为是几乎同时发起的。时间开销便与单线程请求一次一样。</p>
<p>有人反对使用阻塞 IO，因为若想更好利用阻塞时的时间，必须使用多线程或进程，这样会有很大的上下文切换开销，而非阻塞 IO + 协程显然是更经济的方式。但当若干任务之间没有偏序关系时，一个任务阻塞是可以接受的（毕竟不会影响到其他任务的执行），同时也会简化程序的设计。而在一些通信模型（如 Publisher-Subscriber）中，“阻塞”是必要的语义。</p>
<p>多个阻塞 IO 需要多条非抢占式的控制流来承载，这些工作交给线程再合适不过了。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><ol>
<li>由于 GIL 的存在，大多数情况下 Python 多线程无法利用多核优势。</li>
<li>C 扩展中可以接触到 GIL 的开关，从而规避 GIL，重新获得多核优势。</li>
<li>IO 阻塞时，GIL 会被释放。</li>
</ol>
<h2 id="相关链接"><a href="#相关链接" class="headerlink" title="相关链接"></a>相关链接</h2><ol>
<li><a href="https://wiki.python.org/moin/GlobalInterpreterLock" target="_blank" rel="noopener">GlobalInterpreterLock - Python Wiki</a></li>
<li><a href="https://en.wikipedia.org/wiki/Blocking_%28computing%29" target="_blank" rel="noopener">Blocking(computing) - Wikipedia</a></li>
<li><a href="https://docs.python.org/3/extending/extending.html" target="_blank" rel="noopener">Extending Python with C or C++</a></li>
<li><a href="https://pypy.org/" target="_blank" rel="noopener">PyPy</a></li>
<li><a href="http://www.jython.org/" target="_blank" rel="noopener">Jython</a></li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;写在前面&quot;&gt;&lt;a href=&quot;#写在前面&quot; class=&quot;headerlink&quot; title=&quot;写在前面&quot;&gt;&lt;/a&gt;写在前面&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;作者电脑 CPU 为 4 核，因此使用 4 个线程测试是合理的&lt;/li&gt;
&lt;li&gt;本文使用的 &lt;code&gt;cpython&lt;/code&gt; 版本为 &lt;code&gt;3.6.4&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;本文使用的 &lt;code&gt;pypy&lt;/code&gt; 版本为 &lt;code&gt;5.9.0-beta0&lt;/code&gt;，兼容 Python 3.5 语法&lt;/li&gt;
&lt;li&gt;本文使用的 &lt;code&gt;jython&lt;/code&gt; 版本为 &lt;code&gt;2.7.0&lt;/code&gt;，兼容 Python 2.7 语法&lt;/li&gt;
&lt;li&gt;若无特殊说明，作语言解时，&lt;code&gt;python&lt;/code&gt; 指 Python 语言；作解释器解时，&lt;code&gt;python&lt;/code&gt; 指 &lt;code&gt;cpython&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Python" scheme="http://blog.hsfzxjy.site/tags/Python/"/>
    
      <category term="Multi-Threading" scheme="http://blog.hsfzxjy.site/tags/Multi-Threading/"/>
    
      <category term="GIL" scheme="http://blog.hsfzxjy.site/tags/GIL/"/>
    
  </entry>
  
  <entry>
    <title>RMPE 论文读书笔记</title>
    <link href="http://blog.hsfzxjy.site/RMPE-Notes/"/>
    <id>http://blog.hsfzxjy.site/RMPE-Notes/</id>
    <published>2018-03-07T16:00:00.000Z</published>
    <updated>2018-03-08T17:23:20.929Z</updated>
    
    <content type="html"><![CDATA[<p>论文地址：<a href="https://arxiv.org/pdf/1612.00137" target="_blank" rel="external">https://arxiv.org/pdf/1612.00137</a></p>
<a id="more"></a>
<h1 id="现有的-Pose-Estimation-解决方案"><a href="#现有的-Pose-Estimation-解决方案" class="headerlink" title="现有的 Pose Estimation 解决方案"></a>现有的 Pose Estimation 解决方案</h1><h2 id="Single-Person-Pose-Estimation-SPPE"><a href="#Single-Person-Pose-Estimation-SPPE" class="headerlink" title="Single Person Pose Estimation (SPPE)"></a>Single Person Pose Estimation (SPPE)</h2><p>只尝试估计一个人的姿势。早期曾使用诸如随机森林等机器学习方法，近几年采用深度学习方案，大致有如下几种：</p>
<ul>
<li>基于 DNN 的</li>
<li>基于 CNN 的（如 RMPE 使用的 Hourglass）</li>
<li>其他架构，如 <a href="http://ieeexplore.ieee.org/document/6909610/" target="_blank" rel="external">DeepPose</a></li>
</ul>
<p>局限：</p>
<p>要求人能被精准定位，对人的位置十分敏感。</p>
<h2 id="Multi-Person-Pose-Estimation-MPPE"><a href="#Multi-Person-Pose-Estimation-MPPE" class="headerlink" title="Multi Person Pose Estimation (MPPE)"></a>Multi Person Pose Estimation (MPPE)</h2><p>可以估计多人的姿势，不同架构的框架对噪音（如 人的重叠、人的鲜有姿态）有不同的敏感度。</p>
<h3 id="part-based-MPPE"><a href="#part-based-MPPE" class="headerlink" title="part-based MPPE"></a>part-based MPPE</h3><p>将人看做若干 part 的组合，先对特定的 part 进行识别，再组合起来得到人的完整姿态。</p>
<p>局限：</p>
<p>检测 part 时只考虑了局部的小区域，在某些情况下表现不好。</p>
<h3 id="two-step-MPPE"><a href="#two-step-MPPE" class="headerlink" title="two-step MPPE"></a>two-step MPPE</h3><p>先检测每个人的 bounding box，再在每个 box 中独立地估计姿势。由于 bounding box 中仅有一人（而且可以做到占据 box 的一大部分），估计姿势可以采用现成的 SPPE 方案。但 SPPE 普遍对人的位置依赖较高，因此 two-step MPPE 对 bounding box 的性能有较高的要求。</p>
<p><strong>RMPE 基于此</strong>。</p>
<h1 id="RMPE"><a href="#RMPE" class="headerlink" title="RMPE"></a>RMPE</h1><h2 id="思想"><a href="#思想" class="headerlink" title="思想"></a>思想</h2><p>尽可能精确地识别出每个人的 bounding box，<strong>并做适当变换使其便于被 SPPE 处理</strong>；再交给 SPPE 进行估计姿势。为了增强 RMPE 的鲁棒性，应该用一些手段从有限的训练集中生成尽量多的样本。</p>
<h2 id="Pipeline"><a href="#Pipeline" class="headerlink" title="Pipeline"></a>Pipeline</h2><p>两条分支：</p>
<p>(Object Detector) -&gt; (STN) -&gt; (Proposals Generator) -&gt; (master SPPE) -&gt; (SDTN, STN 的逆操作) -&gt; Pose NMS -&gt; Output</p>
<p>(Object Detector) -&gt; (STN) -&gt; (Parallel SPPE)</p>
<h2 id="细节"><a href="#细节" class="headerlink" title="细节"></a>细节</h2><ol>
<li>有别于传统的 Object Detector，RMPE 在其后增加了 STN 作为图像正规化手段。这是因为 SPPE 对人的位置非常敏感，倘若能固定住人的位置（如居中），则可最大化 SPPE 的性能。</li>
<li>RMPE 中有两条 SPPE 分支，其中的 parallel SPPE 的参数是固定的，它将 STN 后的图像直接与正规化后的 label （即将原 label 做适当变换使人处于正中）比较得到误差并由此进行训练。训练的结果可使 STN 正确地将人变换至中间位置，从而最优化 master SPPE。</li>
<li>考虑到对于一个人，Object Detector 只能产生一个 bounding box，RMPE 在 STN 后面又安插了 Proposals Generator 用于<strong>生成更多的样本</strong>。Proposals Generator 会对 Object Detector 产生的 bounding box 做一定的偏移，再传入 master SPPE。</li>
<li>考虑到 Proposals Generator 会对一个人生成多个 bounding box，而对于不同的 bounding box，master SPPE 会做出不同的姿态估计，RMPE 引入 Pose NMS（Pose Non-Maximum Suppression）对这些姿态进行合并。Pose NMS 综合考虑了姿态的位置与置信度因素。</li>
</ol>
<h2 id="实现（测试期）"><a href="#实现（测试期）" class="headerlink" title="实现（测试期）"></a>实现（测试期）</h2><p>在 Pipeline 中，有两个组件 Object Detector 和 SPPE 是被泛化，可以使用任何解决方案。在测试时，作者使用了 VGG-based SSD-512 作为 Object Detector，及 Stacked Hourglass Model 作为 SPPE 的实现，以达到最大的性能。</p>
<h2 id="新组件的必要性"><a href="#新组件的必要性" class="headerlink" title="新组件的必要性"></a>新组件的必要性</h2><p>较于传统的架构，RMPE 引入了 STN (及附属的 Parallel SPPE)、Proposals Generator 及 Pose NMS 这三个新组件。在作者分别移除了它们后，RMPE 的性能显著下降。可见这些组件引入的必要性。</p>
<h1 id="相关组件"><a href="#相关组件" class="headerlink" title="相关组件"></a>相关组件</h1><h2 id="Spatial-Transformer-Network-STN"><a href="#Spatial-Transformer-Network-STN" class="headerlink" title="Spatial Transformer Network (STN)"></a>Spatial Transformer Network (STN)</h2><p>原始论文：<a href="https://arxiv.org/abs/1506.02025" target="_blank" rel="external">https://arxiv.org/abs/1506.02025</a></p>
<p>由 Localization Net、Grid Generator、Sampler 三部分组成，其中 Localization Net 为一个传统的 CNN。STN 可以被嵌入在具体任务的 CNN 中以增强其性能。STN 不需要额外的监督学习，随着原来的网络一起训练即可。</p>
<h2 id="Stacked-Hourglass-Network"><a href="#Stacked-Hourglass-Network" class="headerlink" title="Stacked Hourglass Network"></a>Stacked Hourglass Network</h2><p>原始论文：<a href="https://arxiv.org/abs/1603.06937" target="_blank" rel="external">https://arxiv.org/abs/1603.06937</a></p>
<p>Hourglass 是一个基于 CNN 的 SPPE。采用若干“沙漏状”模块（先自上而下降低分辨率，再自下而上采样）堆叠，相比其他 CNN 有更好的性能。</p>
<h1 id="相关数据集"><a href="#相关数据集" class="headerlink" title="相关数据集"></a>相关数据集</h1><p>MPII Human Pose Dataset: <a href="http://human-pose.mpi-inf.mpg.de/" target="_blank" rel="external">http://human-pose.mpi-inf.mpg.de/</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;论文地址：&lt;a href=&quot;https://arxiv.org/pdf/1612.00137&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://arxiv.org/pdf/1612.00137&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="RMPE" scheme="http://blog.hsfzxjy.site/tags/RMPE/"/>
    
      <category term="Machine Learning" scheme="http://blog.hsfzxjy.site/tags/Machine-Learning/"/>
    
  </entry>
  
  <entry>
    <title>一个 Reentrant Error 引发的对 Python 信号机制的探索和思考</title>
    <link href="http://blog.hsfzxjy.site/caveats-for-signal-handler-in-python/"/>
    <id>http://blog.hsfzxjy.site/caveats-for-signal-handler-in-python/</id>
    <published>2017-08-16T16:00:00.000Z</published>
    <updated>2018-03-08T17:23:20.897Z</updated>
    
    <content type="html"><![CDATA[<h2 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h2><p>前几天工作时遇到了一个匪夷所思的问题。经过几次尝试后问题得以解决，但问题产生的原因却仍令人费解。查找 SO 无果，我决定翻看 Python 的源码。断断续续地研究了几天，终于恍然大悟。撰此文以记。</p>
<a id="more"></a>
<p>本文环境：</p>
<ul>
<li>Ubuntu 16.04 (64 bit)</li>
<li>Python 3.6.2</li>
</ul>
<p>使用的 C 源码可以从 <a href="https://www.python.org/ftp/python/3.6.2/Python-3.6.2.tgz" target="_blank" rel="external">Python 官网</a> 获取。</p>
<h2 id="起因"><a href="#起因" class="headerlink" title="起因"></a>起因</h2><p>工作时用到了 celery 作为异步任务队列，为方便调试，我写了一个脚本用以启动/关闭 celery 主进程。代码简化后如下：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">import</span> sys</div><div class="line"><span class="keyword">import</span> subprocess</div><div class="line"><span class="comment"># ...</span></div><div class="line">celery_process = subprocess.Popen(</div><div class="line">    [<span class="string">'celery'</span>, <span class="string">'-A'</span>, <span class="string">'XXX'</span>, <span class="string">'worker'</span>],</div><div class="line">    stdout=subprocess.PIPE,</div><div class="line">    stderr=sys.stderr</div><div class="line">)</div><div class="line"><span class="keyword">try</span>:</div><div class="line">    <span class="comment"># Start and wait for server process</span></div><div class="line"><span class="keyword">except</span> KeyboardInterrupt:</div><div class="line">    <span class="comment"># Ctrl + C pressed</span></div><div class="line">    celery_process.terminate()</div><div class="line">    celery_process.wait()</div></pre></td></tr></table></figure>
<p>代码启动了 celery worker，并尝试在捕获到 <code>KeyboardInterrupt</code> 异常时将其热关闭。</p>
<p>初看上去没什么问题。然而实际测试时却发生了十分诡异的事情：按下 <code>Ctrl+C</code> 后，程序 <strong>偶尔</strong> 会抛出这样的异常：<code>RuntimeError: reentrant call inside &lt;_io.BufferedWriter name=&#39;&lt;stdout&gt;&#39;&gt;</code>。诡异之处有两点：</p>
<ul>
<li>异常发生的时机有随机性</li>
<li>异常的 traceback 指向 celery 包，也就是说这是在 celery 主进程内部发生的异常</li>
</ul>
<p>这个结果大大出乎了我的意料。随机性异常是众多最难缠的问题之一，因为这常常意味着并发问题，涉及底层知识，病灶隐蔽，调试难度大，同时没有有效的手段判断问题是否彻底解决（可能只是降低了频率）。</p>
<h2 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h2><p>异常信息中有两个词很关键：<code>reentrant</code> 和 <code>stdout</code>。<code>reentrant call</code> 说明有一个不可重入的函数被递归调用了；<code>stdout</code> 则指明了发生的地点和时机。初步可以判定：<strong>由于某种原因，有两股控制流在同时操控 <code>stdout</code></strong>。</p>
<p>“可重入”是什么？根据 <a href="https://en.wikipedia.org/wiki/Reentrancy_%28computing%29" target="_blank" rel="external">Wikipedia</a> 的定义：<strong>如果一个子程序能在执行时被中断并在之后被正确地、安全地唤起，它就被称为可重入的</strong>。依赖于全局数据的过程是不可重入的，如 <code>printf</code>（依赖于全局文件描述符）、<code>malloc</code>（依赖与和堆相关的一系列数据结构）等函数。需要注意的是，可重入性（reentrant）与 线程安全性（thread-safe）并不等价，甚至不存在包含关系，<a href="https://en.wikipedia.org/wiki/Reentrancy_%28computing%29#Examples" target="_blank" rel="external">Wikipedia</a> 中给出了相关的反例。</p>
<p>多次尝试后，出现了一条线索：有时候 <code>worker: Warm shutdown (MainProcess)</code> 这个字符串会被二次打印，时机不确定。这句话是 celery 将要热关闭时的提示语，二次出现只可能是主进程收到了第二个信号。阅读 <a href="http://docs.celeryproject.org/en/latest/userguide/workers.html#process-signals" target="_blank" rel="external">celery 的文档</a> 可知，<code>SIGINT</code> 和 <code>SIGTERM</code> 信号可以引发热关闭。回头浏览我的代码，其中只有一处发送了 <code>SIGTERM</code> 信号（<code>celery_process.terminate()</code>），至于另一个神秘的信号，我怀疑是 <code>SIGINT</code>。</p>
<p>SO 一下，结果印证了我的猜想：</p>
<blockquote>
<p>If you are generating the SIGINT with Ctrl-C on a Unix system, then the signal is being sent to the entire process group.<br>– via <a href="https://stackoverflow.com/questions/6803395/child-process-receives-parents-sigint" target="_blank" rel="external">StackOverflow</a></p>
</blockquote>
<p><code>SIGINT</code> 信号不仅会发送到父进程，而是会发到整个进程组，默认情况下包括了所有子进程。也就是说——在拦截了 <code>KeyboardInterrupt</code> 之后执行的 <code>celery_process.terminate()</code> 是多此一举，因为 <code>SIGINT</code> 信号也会被发送至 celery 主进程，同样会引起热关闭。代码稍作修改即可正常运行：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># ...</span></div><div class="line"><span class="keyword">try</span>:</div><div class="line">    <span class="comment"># Start and wait for server process</span></div><div class="line"><span class="keyword">except</span> KeyboardInterrupt:</div><div class="line">    <span class="comment"># Ctrl + C pressed</span></div><div class="line">    <span class="keyword">pass</span></div><div class="line"><span class="keyword">else</span>:</div><div class="line">    <span class="comment"># Signal SIGTERM if no exception raised</span></div><div class="line">    celery_process.terminate()</div><div class="line"><span class="keyword">finally</span>:</div><div class="line">    <span class="comment"># Wait for it to avoid it becoming orphan</span></div><div class="line">    celery_process.wait()</div></pre></td></tr></table></figure>
<h2 id="猜测"><a href="#猜测" class="headerlink" title="猜测"></a>猜测</h2><p>UNIX 信号处理是一个相当奇葩的过程——当进程收到一个信号时，内核会选择一条线程（以一定的规则），中断其当前控制流，将控制流强行转给信号处理函数，待其执行完毕后再将控制流交还给原线程。时序图如下：</p>
<p><img src="/assets/caveats-for-signal-handler-in-python/bVS7gW" alt=""></p>
<p>由于控制流转换发生在同一条线程上，许多线程间同步机制会失效甚至报错。因此信号处理函数的编写要比线程函数更加严格，对同一个文件输出是被禁止并且无解的，因为很可能会发生这样的事情：</p>
<p><img src="/assets/caveats-for-signal-handler-in-python/bVS7nP" alt=""></p>
<p>而且这个问题不能通过加锁来解决（因为是在同一个线程中，会死锁）。</p>
<p>因此，我猜测异常发生时的事件时序是这样的：在 <code>print</code> 未执行完时中断，又在信号处理函数中调用 print，触发了重入检测，引起 <code>RuntimeError</code>：</p>
<p><img src="/assets/caveats-for-signal-handler-in-python/bVS831" alt=""></p>
<h2 id="疑云又起"><a href="#疑云又起" class="headerlink" title="疑云又起"></a>疑云又起</h2><p>不幸的是，我的猜想很快被推翻了。</p>
<p>在翻看 Python <code>signal</code> 模块的官方文档，我看到了如下叙述：</p>
<blockquote>
<p>A Python signal handler does not get executed inside the low-level (C) signal handler. Instead, the low-level signal handler sets a flag which tells the virtual machine to execute the corresponding Python signal handler at a later point(for example at the next bytecode instruction).<br>– via <a href="https://docs.python.org/3/library/signal.html?highlight=signal#execution-of-python-signal-handlers" target="_blank" rel="external">Python Documentation</a></p>
</blockquote>
<p>也就是说，<strong>Python 中使用 <code>signal.signal</code> 注册的信号处理函数并不会在收到信号时立即执行，而只是简单做一个标记，将其延迟至之后的某个时机</strong>。这么做可以尽量快地结束异常控制流，减少其对被阻断进程的影响。</p>
<p>这番表述可以说是推翻了我的猜想，因为 Signal Handler 中的 <code>print</code> 并没有在异常控制流中执行。那异常又是怎么产生的呢？</p>
<p>文档说 Python Signal Handler 会被延后至某个时机进行，但并没有明示是什么时候。对于这个疑问，<a href="https://stackoverflow.com/questions/19699314/how-to-use-a-separate-thread-for-asynchronous-signal-handling-in-python" target="_blank" rel="external">这个提问的被采纳回答</a> 则斩钉截铁地将其具体化到了“某两个 Python 字节码之间”。</p>
<p>我们知道，Python 程序在执行前会被编译成 Python 内定的字节码<br>（bytecode）,Python 虚拟机实际执行的正是这些字节码。倘若该回答是正确的，则立即有如下推论：<strong>在处理信号的过程中，字节码具有原子性（atomic）</strong>。也就是说，主线程总是在两个字节码之间决定是否转移控制流， 而 <strong>不会</strong> 出现以下情况：</p>
<p><img src="/assets/caveats-for-signal-handler-in-python/bVS7EH" alt=""></p>
<p>这很显然与我的程序结果相悖：<code>print</code> 与 <code>print</code> 所调用的 <code>io.BufferedWriter.write</code> 和 <code>io.BufferedWriter.flush</code> 都是用纯 C 代码编写的，对其的调用只消耗一条字节码（<code>CALL_FUNCTION</code> 或 <code>CALL_FUNCTION_KW</code>），在信号中断的影响下，这几个函数仍保持原子性，在时序图上互不重叠，更不会发生重入。</p>
<p><img src="/assets/caveats-for-signal-handler-in-python/bVS7ag" alt=""></p>
<p>因此，<strong>除了在两个字节码之间，应该还有其他时机唤起了 Python Signal Handler</strong>。</p>
<p>至此，问题已触及 Python 的地板了，需向更底层挖掘才能找到答案。</p>
<h2 id="深入源码"><a href="#深入源码" class="headerlink" title="深入源码"></a>深入源码</h2><p>信号注册逻辑位于 <code>Modules/signalmodule.c</code> 文件中。 313 行的 <code>signal_handler</code> 是信号处理函数的最外层包装，由系统调用 <code>signal</code> 或 <code>sigaction</code> 注册至内核，并在信号发生时被内核回调，是异常控制流的入口。<code>signal_handler</code> 主要调用了 239 行处的 <code>trip_signal</code> 函数，其中有这样一段代码：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">Handlers[sig_num].tripped = <span class="number">1</span>;</div><div class="line"></div><div class="line"><span class="keyword">if</span> (!is_tripped) &#123;</div><div class="line">    is_tripped = <span class="number">1</span>;</div><div class="line">    Py_AddPendingCall(checksignals_witharg, <span class="literal">NULL</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这段代码便是文档中所说的逻辑：做标记并延后 Python Signal Handler。其中 <code>checksignals_witharg</code> 即为被延后调用的函数，位于 192 行，核心代码只有一句：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span></span></div><div class="line"><span class="title">checksignals_witharg</span><span class="params">(<span class="keyword">void</span> * unused)</span></div><div class="line">&#123;</div><div class="line">    <span class="keyword">return</span> PyErr_CheckSignals();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p><code>PyErr_CheckSignals</code> 位于 1511 行：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">int</span></span></div><div class="line"><span class="title">PyErr_CheckSignals</span><span class="params">(<span class="keyword">void</span>)</span></div><div class="line">&#123;</div><div class="line">    <span class="keyword">int</span> i;</div><div class="line">    PyObject *f;</div><div class="line"></div><div class="line">    <span class="keyword">if</span> (!is_tripped)</div><div class="line">        <span class="keyword">return</span> <span class="number">0</span>;</div><div class="line"></div><div class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> WITH_THREAD</span></div><div class="line">    <span class="keyword">if</span> (PyThread_get_thread_ident() != main_thread)</div><div class="line">        <span class="keyword">return</span> <span class="number">0</span>;</div><div class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></div><div class="line"></div><div class="line">    is_tripped = <span class="number">0</span>;</div><div class="line"></div><div class="line">    <span class="keyword">if</span> (!(f = (PyObject *)PyEval_GetFrame()))</div><div class="line">        f = Py_None;</div><div class="line"></div><div class="line">    <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt; NSIG; i++) &#123;</div><div class="line">        <span class="keyword">if</span> (Handlers[i].tripped) &#123;</div><div class="line">            PyObject *result = <span class="literal">NULL</span>;</div><div class="line">            PyObject *arglist = Py_BuildValue(<span class="string">"(iO)"</span>, i, f);</div><div class="line">            Handlers[i].tripped = <span class="number">0</span>;</div><div class="line"></div><div class="line">            <span class="keyword">if</span> (arglist) &#123;</div><div class="line">                result = PyEval_CallObject(Handlers[i].func,</div><div class="line">                                           arglist);</div><div class="line">                Py_DECREF(arglist);</div><div class="line">            &#125;</div><div class="line">            <span class="keyword">if</span> (!result)</div><div class="line">                <span class="keyword">return</span> <span class="number">-1</span>;</div><div class="line"></div><div class="line">            Py_DECREF(result);</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="keyword">return</span> <span class="number">0</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>可见，这个函数便是异步回调的最里层，包含了执行 Python Signal Handler 的逻辑。</p>
<p>至此我们可以发现，整个 Python 中有两个办法可以唤起 Python Signal Handler，一个是调用 <code>checksignals_witharg</code>，另一个是调用 <code>PyErr_CheckSignals</code>。前者只是后者的简单封包。</p>
<p><code>checksignals_witharg</code> 在 Python 源码中只出现了一次（不包括定义，下同），没有被直接调用的迹象。但需要注意的是，<code>checksignals_witharg</code> 曾被当做 <code>Py_AddPendingCall</code> 的参数，<code>Py_AddPendingCall</code> 所做的工作时将其加入到一个全局队列中。与之对应的出队操作是 <code>Py_MakePendingCalls</code>，位于 <code>Python/ceval.c</code> 的 464 行。此函数会间接调用 <code>checksignals_witharg</code>，在 Python 源码中被调用了 3 次：</p>
<ul>
<li><code>Modules/_threadmodule.c</code> 52 行的 <code>acquire_timed</code></li>
<li><code>Modules/main.c</code> 310 行的 <code>run_file</code></li>
<li><code>Python/ceval.c</code> 722 行的 <code>_PyEval_EvalFrameDefault</code></li>
</ul>
<p>值得注意的是，<code>_PyEval_EvalFrameDefault</code> 是一个长达 2600 多行的状态机，是解析字节码的核心逻辑所在。此处调用出现于状态机主循环开始处——这印证了上面回答中的部分说法，即 Python 会在两个字节码中间唤起 Python Signal Hanlder。</p>
<p>而 <code>PyErr_CheckSignals</code> 在 Python 源码中出现了 80 多处，遍布 Python 的各个模块中——这说明该回答的另一半说法是错误的：<strong>除了在两个字节码之间，Python 还可能在其他角落唤起 Python Signal Handler</strong>。其中有两处值得注意，它们都位于 <code>Modules/_io/bufferedio.c</code> 中：</p>
<ul>
<li>1884 行的 <code>_bufferedwriter_flush_unlocked</code></li>
<li>1939 行的 <code>_io_BufferedWriter_write_impl</code></li>
</ul>
<p>这两个函数是 <code>io.BufferedWriter</code> 类的底层实现，会被 <code>print</code> 间接调用。仔细观察可以发现，它们都有着相似的结构：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">ENTER_BUFFERED(self)</div><div class="line"><span class="comment">// ...</span></div><div class="line">PyErr_CheckSignals();</div><div class="line"><span class="comment">// ...</span></div><div class="line">LEAVE_BUFFERED(self)</div></pre></td></tr></table></figure>
<p><code>ENTER_BUFFERED</code> 是一个宏，会尝试申请无阻塞线程锁以保证函数不会被重入：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="meta">#<span class="meta-keyword">define</span> ENTER_BUFFERED(self) \</span></div><div class="line">    ( (PyThread_acquire_lock(self-&gt;lock, 0) ? \</div><div class="line">       1 : _enter_buffered_busy(self)) \</div><div class="line">     &amp;&amp; (self-&gt;owner = PyThread_get_thread_ident(), 1) )</div></pre></td></tr></table></figure>
<p><img src="/assets/caveats-for-signal-handler-in-python/bVS9AJ" alt=""></p>
<p>至此，真相已经大白了。</p>
<h2 id="真相"><a href="#真相" class="headerlink" title="真相"></a>真相</h2><p>当信号中断发生在 <code>_bufferedwriter_flush_unlocked</code> 或 <code>_io_BufferedWriter_write_impl</code> 中时，这两个函数中的 <code>PyErr_CheckSignals</code> 会直接唤起 Python Signal Handler，而此时由 <code>ENTER_BUFFERED</code> 上的锁尚未解开，若 Python Signal Handler 中又有 <code>print</code> 函数调用，则会导致再次 <code>ENTER_BUFFERED</code> 上锁失败，从而抛出异常。时序图如下：</p>
<p><img src="/assets/caveats-for-signal-handler-in-python/bVS9lQ" alt=""></p>
<h2 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h2><p>为什么不将 Python Signal Handler 调用的地点统一在一个地方，而是散布在程序的各处呢？阅读相关代码，我认为有两点原因：</p>
<ul>
<li>信号中断会使某些系统调用行为异常，从而使系统调用的调用者不知如何处理，此时需要调用 Signal Handler 进行可能的状态恢复。一个例子是 <code>write</code> 系统调用，信号中断会导致数据部分写回，与此相关的一大批 I/O 函数（包括出问题的 <code>_bufferedwriter_flush_unlocked</code> 和 <code>_io_BufferedWriter_write_impl</code>）便只能相应地调用 <code>PyErr_CheckSignals</code>。</li>
<li>某些函数需要做计算密集型任务，为了防止 Python Signal Handler 的调用被过长地延后（其实主要是为了及时响应键盘中断，防止程序无法从前台结束），必须适时地检查并调用 Python Signal Handler。一个例子是 <code>Objects/longobject.c</code> 中的诸函数，<code>longobject.c</code> 定义了 Python 特有的无限长整型，其相关的运算可能耗时相当长，必须做这样的处理。</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li>Python Signal Handler 的调用会被延后，但时机不止在两个字节码之间，而是可能出现在任何地方。</li>
<li>由于第一条，Python Signal Handler 中尽量都使用 <strong>可重入的</strong> 的函数，以避免奇怪的问题。可重入性可以从文档获知，也可以结合定义由源码推断出来。</li>
<li>有疑问，翻源码。人会说谎，代码不会。</li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;写在前面&quot;&gt;&lt;a href=&quot;#写在前面&quot; class=&quot;headerlink&quot; title=&quot;写在前面&quot;&gt;&lt;/a&gt;写在前面&lt;/h2&gt;&lt;p&gt;前几天工作时遇到了一个匪夷所思的问题。经过几次尝试后问题得以解决，但问题产生的原因却仍令人费解。查找 SO 无果，我决定翻看 Python 的源码。断断续续地研究了几天，终于恍然大悟。撰此文以记。&lt;/p&gt;
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="POSIX" scheme="http://blog.hsfzxjy.site/tags/POSIX/"/>
    
      <category term="Python" scheme="http://blog.hsfzxjy.site/tags/Python/"/>
    
      <category term="Reentrancy" scheme="http://blog.hsfzxjy.site/tags/Reentrancy/"/>
    
  </entry>
  
  <entry>
    <title>Linux 文件权限</title>
    <link href="http://blog.hsfzxjy.site/linux-file-permissions/"/>
    <id>http://blog.hsfzxjy.site/linux-file-permissions/</id>
    <published>2017-07-02T16:00:00.000Z</published>
    <updated>2018-03-08T17:23:20.861Z</updated>
    
    <content type="html"><![CDATA[<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>Linux 中的每一个文件都有其 <strong>所属用户</strong> 及 <strong>所属用户组</strong>，根据这两个属性可将文件访问者分为三类：<strong>所属用户自己</strong>、<strong>所属用户组中的用户</strong> 和 <strong>其他用户</strong>，我们可以针对不同的访问者设置不同的用户权限。</p>
<p>“访问”可分为三类：<strong>读</strong>、<strong>写</strong> 与 <strong>执行</strong>。我们可以用 <code>ls -l</code> 命令查看一个文件的权限：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">$ touch <span class="built_in">test</span></div><div class="line">$ ls <span class="_">-l</span> <span class="built_in">test</span></div><div class="line">-rw-rw-r-- 1 hsfzxjy hsfzxjy 0 Jul  3 23:44 <span class="built_in">test</span></div></pre></td></tr></table></figure>
<p>首部的 <code>-rw-rw-r--</code> 即为文件的权限位。权限应该分为四部分来看：<code>-/rw-/rw-/r--</code>。第一部分标志文件的类型，如 普通文件（<code>-</code>）、目录（<code>d</code>）、UNIX 套接字（<code>s</code>）、符号链接（<code>l</code>）、块设备（<code>b</code>）等等。接下来的三个部分依次代表 <strong>所属用户</strong>、<strong>所属用户组</strong>、<strong>其他用户</strong> 的权限，每部分由三个标志位组成：<strong>读标志位</strong>、<strong>写标志位</strong>、<strong>执行标志位</strong>。</p>
<h1 id="目录的权限"><a href="#目录的权限" class="headerlink" title="目录的权限"></a>目录的权限</h1><p>目录是一种特殊的文件，因此也拥有文件权限的概念，但权限的语义与普通文件稍有差异：</p>
<ul>
<li>读：读取目录下文件列表，相关命令如 <code>ls</code></li>
<li>写：创建、删除目录下的文件，相关命令如 <code>touch</code>（当文件不存在时）、<code>rm</code> 等</li>
<li>执行：进入目录，相关命令如 <code>cd</code></li>
</ul>
<h1 id="特殊权限"><a href="#特殊权限" class="headerlink" title="特殊权限"></a>特殊权限</h1><p>出于某些特殊目的，Linux 中存在两个特殊的权限位：粘滞位（<code>t</code>）、Set Id（<code>s</code>）。这两个权限可以 <strong>叠加</strong> 在执行权限位上，其中 Set Id 可以置于 <strong>所属用户</strong> 和 <strong>所属用户组</strong> 的权限组上，而 粘滞位 只能置于 <strong>其他用户</strong> 权限组上。当特殊权限被设置时，执行权限位上即会显示 <code>s/t</code> （已有 <code>x</code> 权限）或 <code>S/T</code> （尚未有 <code>x</code> 权限）。</p>
<h2 id="粘滞位"><a href="#粘滞位" class="headerlink" title="粘滞位"></a>粘滞位</h2><p>粘滞位的作用是 <strong>防止他人误删自己的文件</strong>。当某个目录的其他用户权限组有 <code>w</code> 权限时，系统中的其他用户即可随意删除目录中的文件。而一旦叠加上 <code>t</code> 权限，只有文件的所有者方能删除文件。一个经典的例子是 <code>/tmp</code>：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ ls <span class="_">-l</span> /</div><div class="line">drwxrwxrwt  13 root root 12288 Jul  4 00:15 tmp/</div></pre></td></tr></table></figure>
<h2 id="Set-Id"><a href="#Set-Id" class="headerlink" title="Set Id"></a>Set Id</h2><p>Linux 中的进程也有自己所属用户与用户组。一般而言，进程的所属用户即为其发起者，但这会引起一些麻烦。一个例子是 <code>passwd</code> 命令，该命令需要修改属于 <code>root</code> 用户的系统文件以保存密码，倘若进程所属用户即为所属者，此功能则无法实现。</p>
<p>Set Id 权限的作用是：在文件被执行时，将其有效用户/用户组设置为文件的用户/用户组，而不是当前执行者。下面是一个演示：</p>
<p>设当前用户为 <code>hsfzxjy</code>，我们在 <code>/tmp</code> 下创建一个 <code>test</code> 文件，并删去其他用户的 <code>r</code> 权限：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">$ <span class="built_in">cd</span> /tmp</div><div class="line">$ <span class="built_in">echo</span> <span class="built_in">test</span> text &gt; <span class="built_in">test</span></div><div class="line">$ chmod o-r <span class="built_in">test</span></div><div class="line">$ ll <span class="built_in">test</span></div><div class="line">-rw-rw---- 1 hsfzxjy hsfzxjy 0 Jul  4 00:28 <span class="built_in">test</span></div></pre></td></tr></table></figure>
<p>由于 <code>test</code> 文件的所属用户是 <code>hsfzxjy</code>，其他用户没有权限读取其中的内容：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ sudo -u mysql cat <span class="built_in">test</span></div><div class="line">cat: <span class="built_in">test</span>: Permission denied</div></pre></td></tr></table></figure>
<p>现在我们修改一下 <code>cat</code> 命令的权限，为了不影响系统文件，我们拷贝一份 <code>cat</code> 副本至当前目录：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">$ cp /bin/cat .</div><div class="line">$ chmod u+s cat</div><div class="line">$ ll cat</div><div class="line">-rwsr-xr-x 1 hsfzxjy hsfzxjy 52080 Jul  4 00:34 cat*</div></pre></td></tr></table></figure>
<p>再以 <code>mysql</code> 的身份执行命令：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ sudo -u mysql ./cat <span class="built_in">test</span></div><div class="line"><span class="built_in">test</span> text</div></pre></td></tr></table></figure>
<p>可见 <code>./cat</code> 在执行时所属用户是 <code>hsfzxjy</code>。我们可以使用 <code>ps</code> 命令更清楚地看到这点：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line">$ sudo -u mysql cat</div><div class="line"><span class="comment"># 在另一个终端中</span></div><div class="line">$ ps -eo euser,ruser,comm | grep cat</div><div class="line">mysql    mysql    cat</div><div class="line"></div><div class="line"><span class="comment"># -----------</span></div><div class="line"></div><div class="line">$ sudo -u mysql ./cat</div><div class="line"><span class="comment"># 在另一个终端中</span></div><div class="line">$ ps -eo euser,ruser,comm | grep cat</div><div class="line">hsfzxjy  mysql    cat</div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;概念&quot;&gt;&lt;a href=&quot;#概念&quot; class=&quot;headerlink&quot; title=&quot;概念&quot;&gt;&lt;/a&gt;概念&lt;/h1&gt;&lt;p&gt;Linux 中的每一个文件都有其 &lt;strong&gt;所属用户&lt;/strong&gt; 及 &lt;strong&gt;所属用户组&lt;/strong&gt;，根据这两个属
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Linux" scheme="http://blog.hsfzxjy.site/tags/Linux/"/>
    
      <category term="File Permissions" scheme="http://blog.hsfzxjy.site/tags/File-Permissions/"/>
    
  </entry>
  
  <entry>
    <title>HSFZMUN 4.0 部署小记</title>
    <link href="http://blog.hsfzxjy.site/HSFZMUN-4.0-configuration/"/>
    <id>http://blog.hsfzxjy.site/HSFZMUN-4.0-configuration/</id>
    <published>2017-02-24T16:00:00.000Z</published>
    <updated>2018-03-08T17:23:20.829Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>技术流水账一篇，记录踩过的坑</p>
</blockquote>
<h2 id="Channels-异构"><a href="#Channels-异构" class="headerlink" title="Channels 异构"></a>Channels 异构</h2><p>Django Channels 官方文档宣称 channels 的最佳配置是使用其自带的服务器组件 Daphne，但在开发中我发现 daphne 处理普通请求比在 WSGI 架构下慢了好几倍，更何况使用 daphne 派发静态文件是十分不切实际的。于是我将 <code>http.request</code> 和 <code>websocket.*</code> 两个 channel 解耦，前者使用 nginx 配合 uwsgi 处理，后者使用 nginx 反向代理至 daphne 处理。这样一来便可充分利用两种架构的优势。</p>
<p><strong>旧架构：</strong></p>
<p><img src="/assets/django-channels-configuration/old-arch.png" alt=""></p>
<p><strong>新架构：</strong></p>
<p><img src="/assets/django-channels-configuration/new-arch.png" alt=""></p>
<a id="more"></a>
<h2 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h2><ul>
<li>Ubuntu Server 16.04（与开发环境相同）</li>
<li>python 3.5.2</li>
<li>virtualenv &amp; virtualenvwrapper</li>
<li>nginx</li>
<li>uwsgi</li>
<li>redis（作为缓存和 Channel Layer）</li>
</ul>
<h2 id="Channels-Session-based-Authentication"><a href="#Channels-Session-based-Authentication" class="headerlink" title="Channels Session-based Authentication"></a>Channels Session-based Authentication</h2><p>channels 有提供基于 HTTP Session 的认证方式，但由于 websocket 和 http 在此不是同域请求（端口号不同），主域名下的 cookies 不会随 websocket 请求发送。故在发起 websocket 链接时要带上一个 GET 参数 <code>session_key</code>。在模板中该参数可由 <code>{ {request.session.session_key} }</code> 获得。</p>
<h2 id="uwsgi"><a href="#uwsgi" class="headerlink" title="uwsgi"></a>uwsgi</h2><p>安装：</p>
<figure class="highlight sh"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ sudo pip install uwsgi <span class="comment"># 全局安装</span></div></pre></td></tr></table></figure>
<p>编写配置文件 <code>/etc/uwsgi/sites/hsfzmun.ini</code>：</p>
<figure class="highlight ini"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="section">[uwsgi]</span></div><div class="line"><span class="attr">uid</span> = git</div><div class="line"><span class="attr">chdir</span> = /home/git/hsfzmun/server</div><div class="line"><span class="attr">module</span> = config.wsgi:application</div><div class="line"><span class="attr">home</span> = /home/git/virtualenvs/hsfzmun # Virtualenv 路径</div><div class="line"><span class="attr">master</span> = <span class="literal">true</span></div><div class="line"><span class="attr">processes</span> = <span class="number">10</span></div><div class="line"><span class="attr">socket</span> = /var/www/hsfzmun.sock # 使用 Unix Socket 与 nginx 通信</div><div class="line"><span class="attr">chmod-socket</span> = <span class="number">666</span></div><div class="line"><span class="attr">vacuum</span> = <span class="literal">true</span></div></pre></td></tr></table></figure>
<p>配置 systemd 服务（<code>uwsgi.service</code>）：</p>
<figure class="highlight ini"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="section">[Unit]</span></div><div class="line"><span class="attr">Description</span>=uWSGI Module</div><div class="line"><span class="section"></span></div><div class="line">[Service]</div><div class="line"><span class="attr">ExecStartPre</span>=/bin/bash -c <span class="string">'mkdir -p /run/uwsgi; chown git:www-data /run/uwsgi'</span></div><div class="line"><span class="attr">ExecStart</span>=/usr/local/bin/uwsgi --emperor /etc/uwsgi/sites --touch-reload=/home/git/hsfzmun/server/uwsgi_params</div><div class="line"><span class="attr">Restart</span>=always</div><div class="line"><span class="attr">KillSignal</span>=SIGQUIT</div><div class="line"><span class="attr">Type</span>=notify</div><div class="line"><span class="attr">NotifyAccess</span>=all</div><div class="line"><span class="section"></span></div><div class="line">[Install]</div><div class="line"><span class="attr">WantedBy</span>=multi-user.target</div></pre></td></tr></table></figure>
<p>uwsgi 有个优点：可以通过 <code>--touch-reload</code> 参数简洁地重启服务，这样只需一条 <code>touch</code> 命令便可以完成新代码的部署。</p>
<h2 id="daphne"><a href="#daphne" class="headerlink" title="daphne"></a>daphne</h2><p>daphne 有两个模块：Interface Server 和 Workers。前者负责处理 Websocket、long-polling 等请求，并将其抽象化为 channels 传递给 Workers，Workers 则负责执行具体的业务逻辑。这么做有一个好处就是 <strong>降低了底层与业务逻辑的耦合度，即使业务层崩溃也不会使连接断开，同时也减少了新代码部署对整个系统的印象</strong>。部署新代码时只需重启 Workers 即可。</p>
<p><code>daphnei.service</code>：</p>
<figure class="highlight ini"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="section">[Unit]</span></div><div class="line"><span class="attr">Description</span>=Daphne Interface Server</div><div class="line"><span class="section"></span></div><div class="line">[Service]</div><div class="line"><span class="attr">ExecStart</span>=/home/git/cmd/daphnei</div><div class="line"><span class="attr">Restart</span>=always</div><div class="line"><span class="attr">KillSignal</span>=SIGQUIT</div><div class="line"><span class="attr">Type</span>=simple</div><div class="line"><span class="attr">NotifyAccess</span>=all</div><div class="line"><span class="section"></span></div><div class="line">[Install]</div><div class="line"><span class="attr">WantedBy</span>=multi-user.target</div></pre></td></tr></table></figure>
<p><code>daphnew.service</code>：</p>
<figure class="highlight ini"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="section">[Unit]</span></div><div class="line"><span class="attr">Description</span>=Daphne Worker</div><div class="line"><span class="section"></span></div><div class="line">[Service]</div><div class="line"><span class="attr">ExecStart</span>=/home/git/cmd/daphnew</div><div class="line"><span class="attr">Type</span>=simple</div><div class="line"><span class="attr">KillSignal</span>=SIGQUIT</div><div class="line"><span class="attr">Restart</span>=always</div><div class="line"><span class="attr">NotifyAccess</span>=all</div><div class="line"><span class="section"></span></div><div class="line">[Install]</div><div class="line"><span class="attr">WantedBy</span>=multi-user.target</div></pre></td></tr></table></figure>
<p><strong>此处踩了一个坑</strong>：起初 <code>Type</code> 处的值是 <code>Notify</code>，因为我是仿照 <code>uwsgi.service</code> 编写这两个文件的。<code>Notify</code> 型服务要求主进程自行通知 <code>systemd</code> 自己的启动/停止状态，但 daphne 没有这样的机制，从而导致 <code>systemctl</code> 阻塞，并以 90 秒为周期不断重启服务。幸好 StackOverflow 上的大神指出了这个错误。</p>
<p>这里我将具体的服务脚本独立出来，因为 <code>ExecStart</code> 不支持编写多行语句。</p>
<p><code>cmd/daphnei</code>：</p>
<figure class="highlight sh"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="meta">#!/bin/bash</span></div><div class="line"><span class="built_in">cd</span> /home/git/hsfzmun/server</div><div class="line">/home/git/virtualenvs/hsfzmun/bin/daphne -b 0.0.0.0 -p 8001 -v2 config.asgi:channel_layer --access-log=/var/www/daphnei.log</div></pre></td></tr></table></figure>
<p><code>cmd/daphnew</code>：</p>
<figure class="highlight sh"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="meta">#!/bin/bash</span></div><div class="line"><span class="built_in">cd</span> /home/git/hsfzmun/server</div><div class="line">/home/git/virtualenvs/hsfzmun/bin/python manage.py runworker --threads 5 --only-channels=websocket.*</div></pre></td></tr></table></figure>
<p><strong>此处 daphne 有个 bug</strong>：理论上 verbosity 不应该影响程序的行为，但如果不加 <code>-v2</code> 参数 nginx 会报 <code>502 Bad Gateway</code>。</p>
<h2 id="nginx"><a href="#nginx" class="headerlink" title="nginx"></a>nginx</h2><p>此处的难点是反向代理 websockets，因为 nginx 默认不识别 websocket 协议。为了能正确指定协议头，只能将所有 websocket 请求路由到某一子路径下。</p>
<p><code>nginx.service</code>：</p>
<figure class="highlight nginx"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div></pre></td><td class="code"><pre><div class="line"><span class="section">server</span> &#123;</div><div class="line">  <span class="attribute">listen</span> <span class="number">80</span>;</div><div class="line">  <span class="attribute">server_name</span> &lt;ip&gt; product;</div><div class="line"></div><div class="line">  <span class="comment"># 静态文件</span></div><div class="line">  <span class="attribute">location</span> /static &#123;</div><div class="line">    <span class="attribute">alias</span> /var/www/static;</div><div class="line">  &#125;</div><div class="line"></div><div class="line">  <span class="comment"># 用户上传的文件</span></div><div class="line">  <span class="attribute">location</span> /m &#123;</div><div class="line">    <span class="attribute">alias</span> /var/www/media;</div><div class="line">  &#125;</div><div class="line"></div><div class="line">  <span class="comment"># uwsgi 反向代理</span></div><div class="line">  <span class="attribute">location</span> / &#123;</div><div class="line">    <span class="attribute">include</span> /var/www/uwsgi_params;</div><div class="line">    <span class="attribute">uwsgi_pass</span> unix:/var/www/hsfzmun.sock;</div><div class="line">  &#125;</div><div class="line"></div><div class="line">  <span class="comment"># daphne 反向代理</span></div><div class="line">  <span class="attribute">location</span> /ws &#123;</div><div class="line">    <span class="attribute">proxy_pass</span> http://0.0.0.0:8001;</div><div class="line">    <span class="attribute">proxy_http_version</span> <span class="number">1</span>.<span class="number">1</span>;</div><div class="line">    <span class="attribute">proxy_set_header</span> Upgrade <span class="variable">$http_upgrade</span>;</div><div class="line">    <span class="attribute">proxy_set_header</span> Connection <span class="string">"upgrade"</span>;</div><div class="line"></div><div class="line">    <span class="attribute">proxy_redirect</span>     <span class="literal">off</span>;</div><div class="line">    <span class="attribute">proxy_set_header</span>   Host <span class="variable">$host</span>;</div><div class="line">    <span class="attribute">proxy_set_header</span>   X-Real-IP <span class="variable">$remote_addr</span>;</div><div class="line">    <span class="attribute">proxy_set_header</span>   X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</div><div class="line">    <span class="attribute">proxy_set_header</span>   X-Forwarded-Host <span class="variable">$server_name</span>;</div><div class="line">    <span class="attribute">proxy_read_timeout</span>  <span class="number">36000s</span>;</div><div class="line">    <span class="attribute">proxy_send_timeout</span>  <span class="number">36000s</span>;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="Git-自动化部署"><a href="#Git-自动化部署" class="headerlink" title="Git 自动化部署"></a>Git 自动化部署</h2><p>去年的 WiseCity 3.0 采用 Github 中转的方式进行部署，这对 Github 的稳定性有很高的要求。这次则将生产服务器作为 Git Server，使用 Git Hooks 进行自动化部署，大大提高了生产效率。</p>
<p>创建 git 用户：</p>
<figure class="highlight sh"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ useradd git</div><div class="line">$ passwd git</div></pre></td></tr></table></figure>
<p>设置 SSH authorized_keys（略）。</p>
<p>创建仓库：</p>
<figure class="highlight sh"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ <span class="built_in">cd</span> ~</div><div class="line">$ git init --bare hsfzmun.git</div></pre></td></tr></table></figure>
<p>创建钩子 <code>hooks/post-receive</code>：</p>
<figure class="highlight sh"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div></pre></td><td class="code"><pre><div class="line"><span class="meta">#!/bin/bash</span></div><div class="line"><span class="comment"># Checkout Repository</span></div><div class="line">GIT_WORK_TREE=/home/git/hsfzmun git checkout <span class="_">-f</span></div><div class="line"></div><div class="line"><span class="comment"># Activate Virtualenv</span></div><div class="line"><span class="built_in">cd</span> /home/git/hsfzmun/server</div><div class="line"><span class="built_in">export</span> LC_ALL=C</div><div class="line"><span class="built_in">export</span> VIRTUALENVWRAPPER_PYTHON=/usr/bin/python3</div><div class="line"><span class="built_in">export</span> WORKON_HOME=~/virtualenvs</div><div class="line"><span class="built_in">source</span> /usr/<span class="built_in">local</span>/bin/virtualenvwrapper.sh</div><div class="line">workon hsfzmun</div><div class="line"></div><div class="line"><span class="comment"># Internationalization</span></div><div class="line">./manage.py compilemessages</div><div class="line"></div><div class="line"><span class="comment"># Collect Static Files</span></div><div class="line">./manage.py collectstatic --noinput</div><div class="line"></div><div class="line"><span class="comment"># Migrate Databases</span></div><div class="line">./manage.py migrate</div><div class="line"></div><div class="line"><span class="comment"># Restart uWSGI</span></div><div class="line">touch ./uwsgi_params</div><div class="line"></div><div class="line"><span class="comment"># Restart Daphne Workers</span></div><div class="line">sudo systemctl restart daphnew --no-block</div><div class="line"></div><div class="line"><span class="built_in">echo</span> <span class="string">"All operations done."</span></div></pre></td></tr></table></figure>
<p>此钩子会在每次 push 之后执行，自动更新静态文件和数据库表并重启相关服务。</p>
<p>本地添加远程仓库（略）。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;技术流水账一篇，记录踩过的坑&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;Channels-异构&quot;&gt;&lt;a href=&quot;#Channels-异构&quot; class=&quot;headerlink&quot; title=&quot;Channels 异构&quot;&gt;&lt;/a&gt;Channels 异构&lt;/h2&gt;&lt;p&gt;Django Channels 官方文档宣称 channels 的最佳配置是使用其自带的服务器组件 Daphne，但在开发中我发现 daphne 处理普通请求比在 WSGI 架构下慢了好几倍，更何况使用 daphne 派发静态文件是十分不切实际的。于是我将 &lt;code&gt;http.request&lt;/code&gt; 和 &lt;code&gt;websocket.*&lt;/code&gt; 两个 channel 解耦，前者使用 nginx 配合 uwsgi 处理，后者使用 nginx 反向代理至 daphne 处理。这样一来便可充分利用两种架构的优势。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;旧架构：&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/assets/django-channels-configuration/old-arch.png&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;新架构：&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/assets/django-channels-configuration/new-arch.png&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Ubuntu" scheme="http://blog.hsfzxjy.site/tags/Ubuntu/"/>
    
      <category term="Django" scheme="http://blog.hsfzxjy.site/tags/Django/"/>
    
      <category term="Django Channels" scheme="http://blog.hsfzxjy.site/tags/Django-Channels/"/>
    
      <category term="Nginx" scheme="http://blog.hsfzxjy.site/tags/Nginx/"/>
    
  </entry>
  
  <entry>
    <title>午后雨·科大</title>
    <link href="http://blog.hsfzxjy.site/raining-in-ustc/"/>
    <id>http://blog.hsfzxjy.site/raining-in-ustc/</id>
    <published>2016-08-21T16:00:00.000Z</published>
    <updated>2018-03-08T17:22:51.013Z</updated>
    
    <content type="html"><![CDATA[<p>从昏睡中惊醒——现在是下午，屋内却很昏暗。屋外，隐约有一阵持续的背景噪音，我拉开窗帘，只见白茫茫的一片。</p>
<p>下雨了，而且是大暴雨。</p>
<p>打开窗，一股辛辣扑面而至——雨和着尘土的气息，再熟悉不过了。伴随着辛辣，一种沁人心脾的清凉涌入屋内，稀释着绿军衣挥发的氨味，一同驱走的，还有午后应有的烦躁。</p>
<p>八月的这里，原来可以不那么热。</p>
<p>一直以为，旱和热是这里夏天的常态。走出机场时，第一感受，也是几天来唯一的感受，便是热——无风的热，黏人；无云的热，灼人。想到将在这样的地方生活，心中顿生怯意——</p>
<p>但现在看来，似乎并不那么糟。</p>
<p>合上窗，雨声再次成为背景音。白色的水汽慢慢爬上玻璃，模糊了摇曳的树，漫水的街，雨中的整个世界。</p>
<p>这一幕似乎很熟悉。</p>
<p>没错，在更早些的时候，当我还在另一个校园时。那是个多雨的城市，雨大而急。记忆中，不知有多少次，也是透过模糊的窗，看着屋外湿透的一切。</p>
<p>面对暴雨，心中有过埋怨，有过恐惧，有过敬畏——但此时，我却体会到了偶遇老友般的亲切。</p>
<p>在陌生的校园，我又遇到了熟悉的雨。</p>
<p>我开始喜欢这里了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;从昏睡中惊醒——现在是下午，屋内却很昏暗。屋外，隐约有一阵持续的背景噪音，我拉开窗帘，只见白茫茫的一片。&lt;/p&gt;
&lt;p&gt;下雨了，而且是大暴雨。&lt;/p&gt;
&lt;p&gt;打开窗，一股辛辣扑面而至——雨和着尘土的气息，再熟悉不过了。伴随着辛辣，一种沁人心脾的清凉涌入屋内，稀释着绿军衣挥发
    
    </summary>
    
      <category term="Essays" scheme="http://blog.hsfzxjy.site/categories/Essays/"/>
    
    
      <category term="生活杂感" scheme="http://blog.hsfzxjy.site/tags/%E7%94%9F%E6%B4%BB%E6%9D%82%E6%84%9F/"/>
    
  </entry>
  
  <entry>
    <title>最后的雨夜·广州</title>
    <link href="http://blog.hsfzxjy.site/leaving-guangzhou/"/>
    <id>http://blog.hsfzxjy.site/leaving-guangzhou/</id>
    <published>2016-08-17T16:00:00.000Z</published>
    <updated>2018-03-08T17:23:19.417Z</updated>
    
    <content type="html"><![CDATA[<p> 明天就要走了。</p>
<p> 我贪婪地呼吸着这座城市，想要记住一切。</p>
<p> 这是一个雨夜。雨不大，淅淅沥沥的，一反八月羊城的狂暴。雨丝划过夜空，拂去了暑气，夹带着一丝清冽；雨点打在地上，泛起一阵辛辣，这是雨的独特气息。昔日湿热而令人厌烦的南方雨，此刻却如此亲近。</p>
<p> 步入校园，城市的灯光隐去了，笼下来的是夜的静谧。走在林荫道，可以闻到空中漫着的甘甜——这是芒果花的香气。</p>
<p> 记忆中，最为深刻的花香便是芒果花。这种热带的植物，在羊城开得到处都是——在童年的大院里，在中学的校道里，在这邻近的大学里，在记忆中的每一个角落。游子最难以忘怀的，当属这南国的果香。</p>
<p> 出了大学的门，转过街角，城市的灯又亮了。这是学校后门的一条食街，寄存了我六年的回忆。</p>
<p> 那股诱人的酸菜味，来自一家无名的东北菜馆。“15块吃到饱”。豪爽的东北大叔，曾在那个三月给予我信心。</p>
<p> 空中那悠扬的，是全家的迎客铃。数不清有多少个早晨，因匆忙而在此买饭团。一口咬下，让柴鱼的美味充盈鼻腔，让翻腾的蒸汽吹走清晨的倦意。</p>
<p> 远处是那家熟悉的M记。24 小时永不停歇。在这里，我度过了高三的每个周末，一份板烧一杯咖啡，一小时的免费 wifi，抹去一上午的沮丧，换来下午持续的好心情。</p>
<p> 再往前便是学校。此时正值军训，校园里却一片静寂——也许是太晚了。六年前，梦从这里开始；六年后，我又将梦重新埋在这里。</p>
<p> 雨下大了，我却不急——毕竟，我将去往一个少雨的城市，这里的雨，我要细细记忆。</p>
<p> 这是一座现代化的都市。雨中的光光影影，雨中 BRT 的笛鸣，雨中泛着的柏油气息，都是这座城市的印记。</p>
<p> 这是一座古老的城市。人们仍执念于古老的语言，过着悠然的生活。饱经沧桑，却历久弥新。</p>
<p> 天一亮，我就将去往一个陌生的城市。那里少雨，泛着中原常见的普通。我将在那里度过四年，甚至更久。</p>
<p> 而在此之前，一个恋家的动物，要努力带走所有的回忆。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt; 明天就要走了。&lt;/p&gt;
&lt;p&gt; 我贪婪地呼吸着这座城市，想要记住一切。&lt;/p&gt;
&lt;p&gt; 这是一个雨夜。雨不大，淅淅沥沥的，一反八月羊城的狂暴。雨丝划过夜空，拂去了暑气，夹带着一丝清冽；雨点打在地上，泛起一阵辛辣，这是雨的独特气息。昔日湿热而令人厌烦的南方雨，此刻却如此亲近
    
    </summary>
    
      <category term="Essays" scheme="http://blog.hsfzxjy.site/categories/Essays/"/>
    
    
      <category term="生活杂感" scheme="http://blog.hsfzxjy.site/tags/%E7%94%9F%E6%B4%BB%E6%9D%82%E6%84%9F/"/>
    
  </entry>
  
  <entry>
    <title>揭秘·变态的平方根倒数算法</title>
    <link href="http://blog.hsfzxjy.site/uncover-the-secret-of-fast-inverse-square-root-algorithm/"/>
    <id>http://blog.hsfzxjy.site/uncover-the-secret-of-fast-inverse-square-root-algorithm/</id>
    <published>2016-08-02T16:00:00.000Z</published>
    <updated>2018-03-08T17:23:20.797Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>神的时代已离去<br>神的故事却化为传说<br>流落凡间<br>供凡人传颂、膜拜</p>
</blockquote>
<h2 id="这是什么"><a href="#这是什么" class="headerlink" title="这是什么"></a>这是什么</h2><p>在上世纪 90 年代，出现过一款不可思议的游戏——雷神之锤（Quake series）。除了优秀的情节设定和精美的画面，最让人称道的莫过于它的运行效率——要知道在那个计算机配置低下的时代，一段小动画都是一个奇迹，但 Quake 却能流畅地运行于各种配置的电脑上。</p>
<p>直至 2005 年，当 Quake Engine 开源时，Quake 系列的秘密才被揭开。在代码库中，人们发现了许多堪称神来之笔的算法。它们以极其变态的高效率，压榨着计算机的性能，进而才支撑起了 90 年代 3D 游戏的传奇。其中的某些算法，甚至比系统原生的实现还要快！</p>
<p>我们今天的主角——快速平方根倒数算法（Fast Inverse Square Root）就是其中一个。</p>
<a id="more"></a>
<p>这是一个用于计算 $1 \over {\sqrt x}$ 的算法。这是一步重要的运算，因为在 3D 绘图中，计算机需要大量求解一个矢量的方向矢量，平方根倒数为其中的一步（$(v_x,v_y, v_z) \over {\sqrt{v_x^2+v_y^2+v_z^2}})$），而且为最麻烦的一步。如果能在此作优化，渲染效率无异会极大地提高。</p>
<p>然而，高效并不是它的唯一特点，吸引人们的更是其中的一个神秘的常数——$(5f3759df)_{16}$。</p>
<p>且看代码：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">float</span> <span class="title">Q_rsqrt</span><span class="params">( <span class="keyword">float</span> number )</span></span></div><div class="line">&#123;</div><div class="line">	<span class="keyword">long</span> i;</div><div class="line">	<span class="keyword">float</span> x2, y;</div><div class="line">	<span class="keyword">const</span> <span class="keyword">float</span> threehalfs = <span class="number">1.5F</span>;</div><div class="line"></div><div class="line">	x2 = number * <span class="number">0.5F</span>;</div><div class="line">	y  = number;</div><div class="line">	i  = * ( <span class="keyword">long</span> * ) &amp;y;                       <span class="comment">// evil floating point bit level hacking</span></div><div class="line">	i  = <span class="number">0x5f3759df</span> - ( i &gt;&gt; <span class="number">1</span> );               <span class="comment">// what the fuck?</span></div><div class="line">	y  = * ( <span class="keyword">float</span> * ) &amp;i;</div><div class="line">	y  = y * ( threehalfs - ( x2 * y * y ) );   <span class="comment">// 1st iteration</span></div><div class="line"><span class="comment">//	y  = y * ( threehalfs - ( x2 * y * y ) );   // 2nd iteration, this can be removed</span></div><div class="line"></div><div class="line">	<span class="keyword">return</span> y;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>翻译成数学语言就是：</p>
<ol>
<li>设输入数为 $number$，令 $ x_2=number/2, y=number $。</li>
<li>上式中，$y$ 是一个 32 位浮点数（可以理解为数学中的实数），现在我们将其“看作”一个整数，并赋给 $i$。</li>
<li>最神奇的一步出现了——用一个十六进制数 $5f3759df$ 减去 $i$ 自身右移一位的结果，并将结果赋给 $i$。</li>
<li>将整数 $i$ 重新“看作”是一个浮点数，并赋值给 $y$。</li>
<li>令 $y=y(1.5-x_2 \times y^2)$。</li>
<li>重复一遍操作 5（事实上这一步可以忽略）。</li>
</ol>
<p>在此需要向无计算机基础的朋友解释一下：</p>
<ul>
<li>右移（Right Shifting）指的是：将一个数表示成二进制之后整体向右移动一位，并抹去溢出的末位。如 $(4)_{10} &gt;&gt; 1=(100)_2 &gt;&gt; 1=(10)_2=(2)_{10}$。其效果等价为整除 2，但由于有 CPU 指令直接支持，速度比整除快若干个数量级。</li>
<li>无论是实数还是整数，在计算机中只不过是一串二进制序列。因此一串二进制序列，既可以被看作一个实数，也可以被看作一个整数。</li>
</ul>
<p>这个算法得到的只是一个近似值。对于 $[0.01, max\_float]$ 内的所有浮点数，最大误差为 $0.175%$（见 <a href="https://en.wikipedia.org/wiki/Fast_inverse_square_root" target="_blank" rel="external">Accuracy</a>）。但它却比内置算法 <code>1.0/sqrt(x)</code> 快 4 倍，可谓瑕不掩瑜。</p>
<p>那么，它究竟是怎么实现的呢？</p>
<h2 id="牛顿法"><a href="#牛顿法" class="headerlink" title="牛顿法"></a>牛顿法</h2><p>我们从后面开始分析。</p>
<p>从代码中可以看出：算法最后有两步相同的操作，像是在对一个数进行某种迭代。而其中的第二步被注释掉了，似乎是因为和性能损耗相比对结果的二次迭代意义不大，也说明 <strong>一次迭代的结果在误差允许范围内</strong>——这让人想到了牛顿法。</p>
<p>牛顿法是什么？</p>
<p>牛顿法是一种常用的求方程数值解的方法。其叙述如下：</p>
<blockquote>
<p>若在某个区间 $I$ 中，$f(x)$ 连续可导，且有唯一零点 $x_0$，则任取 $x_1 \in I$，定义数列 $x_{n+1}=x_n-{f(x_n) \over {f’(x_n)}}$，则有 $lim_{n \to \infty}{x_n}=x_0$。</p>
</blockquote>
<p>用牛顿法进行迭代，可以完成对解任意精度的数值逼近。下面我们尝试写出 $x=1 \over {\sqrt {a}}$的迭代式。</p>
<p>令 $f(x)={ {1 \over x^2}-a}$，则有</p>
<p>$$ x_{n+1}=x_n-{f(x_n) \over {f’(x_n)}} $$<br>$$ =x_n-{ { {1 \over x_n^2} -a} \over {-2 \over {x_n^3} } } $$<br>$$ ={3 \over 2}x_n - { {ax_n^3} \over 2} $$<br>$$ ={x_n}(1.5-{a \over 2}x_n^2)$$</p>
<p>如果我们将$x_{n+1}$、$x_n$替换成$y$，将$a \over 2$替换成$x2$，可以发现和算法的最后一步是吻合的。由此可知：<strong>算法的最后确实采用了牛顿法</strong>。</p>
<p>也许你注意到：能解出 $1 \over {\sqrt a}$ 的方程不止这一条，迭代式的形式有很多。事实上，作者有意选择了这条方程——因为只有从这条方程得出的迭代式是 <strong>不用除法的</strong>。除法的性能十分糟糕，应该尽量避免。</p>
<p>但是，牛顿法不是需要迭代多次的吗？怎么在这里只进行一次就足够精确了呢？</p>
<p>牛顿法的收敛过程依赖于初值$x_1$的选取。若想一步到位，除非初值本身已经足够精确了。</p>
<p>初值是什么？</p>
<p>就是那神奇的第 3 步得到的结果。</p>
<h2 id="神奇的-0x5f3759df"><a href="#神奇的-0x5f3759df" class="headerlink" title="神奇的 0x5f3759df"></a>神奇的 0x5f3759df</h2><p>这是整个算法的奥妙所在。我们再来回顾一下：</p>
<ol>
<li>上式中，$y$ 是一个 32 位浮点数（可以理解为数学中的实数），现在我们将其“看作”一个整数，并赋给 $i$。</li>
<li>最神奇的一步出现了——用一个十六进制数 $5f3759df$ 减去 $i$ 自身右移一位的结果，并将结果赋给 $i$。</li>
<li>将整数 $i$ 重新“看作”是一个浮点数，并赋值给 $y$。</li>
</ol>
<p>有了上一节的分析，几乎可以肯定：这是为了得到 $1 \over {\sqrt {number} }$ 的一个粗略值，即应该有 $y \approx {1 \over {\sqrt {number} } }$。</p>
<p>为了进一步的论证，我们首先要了解一个知识点：</p>
<h3 id="IEEE-754-（浮点数储存标准）"><a href="#IEEE-754-（浮点数储存标准）" class="headerlink" title="IEEE 754 （浮点数储存标准）"></a>IEEE 754 （浮点数储存标准）</h3><p><a href="http://grouper.ieee.org/groups/754/" target="_blank" rel="external">IEEE 754: Standard for Binary Floating-Point Arithmetic</a></p>
<p><a href="https://en.wikipedia.org/wiki/IEEE_floating_point" target="_blank" rel="external">IEEE floating point - Wikipedia</a></p>
<p>这是计算机内实数的储存标准。</p>
<p>在本算法中，我们使用的是 32 位浮点数（即 用32个二进制位表示一个实数）。储存方式如下：</p>
<blockquote>
<p>对任意一个实数 $x$，总可以将其分解为如下形式：<br>$$ x=2^E \times (1+M)\ (E \in Z,M \in [0, 1))$$<br>则 32 个二进制位安排如下：<br>$$ S,EEEEEEEE,MMMMMMMMMMMMMMMMMMMMMM $$</p>
<ul>
<li>首位为符号位，取 <code>0</code> 为正数，取 <code>1</code> 为负数</li>
<li>接下来 8 位为 <strong>带符号</strong> 指数。根据带符号数的储存方式，该数减去 127 才为真实的指数 $E$</li>
<li>接下来 23 位为底数。是 $M$ 左移 23 位再取整的结果</li>
</ul>
</blockquote>
<p>举个例子：将 $0.15625=(0.00101)_2$ 换算成浮点数：</p>
<p>$$ 0.15625=+2^{-3}\times(1+0.25) $$</p>
<p>则有：</p>
<ul>
<li>符号位为 $0$</li>
<li>$E=-3$，换算成带符号正数为 $-3+127=124=(01111100)_2$</li>
<li>$M=0.25=(0.01)_2$，左移 23 位后为 $(01000000000000000000000)_2$</li>
</ul>
<p>从而浮点表示为：$0,01111100,01000000000000000000000$</p>
<h3 id="回到原题"><a href="#回到原题" class="headerlink" title="回到原题"></a>回到原题</h3><p>现在，让我们思考一下：</p>
<p>如何只用加、减、乘和位运算神出鬼没地快速逼近 $1 \over {\sqrt x}$？</p>
<p>若想回答这个问题，得看我们对 $x$ 了解多少。</p>
<p>从上一小节的浮点数标准可以看出：<strong>计算机看到的 x 和我们接触的 x 结构不同</strong>，早在程序的编译期，$x$ 就被拆成了指数和底数两部分，并被打包存好——这一步是不耗时间的，但却给我们提供了海量的信息。</p>
<p>等等！指数和底数！似乎在暗示着什么！</p>
<p>我们，是不是可以通过对数进行运算？</p>
<p>设想，如果我们能利用以上信息，轻易地转换 $x$ 和 $\log_2 x$，由 $\log_2 {1 \over {\sqrt x}}={-{1 \over 2}}{\log_2 x}$ 就可以求得 $1 \over {\sqrt x}$ 的值了。</p>
<p>那么，$\log_2 x$ 该如何表示呢？且看以下变换。</p>
<p>根据 IEEE 754，对于一个 $x=2^E \times (1+M)&gt;0$，如果我们将其 32 位浮点表示看作一个 32 位整数 $I$，则有</p>
<p>$$I=2^{23}\times (E+127)+2^{23}\times M$$<br>$$=2^{23}\times (E+127+M)$$</p>
<p>这个 $I$ 为已知量。通过已知量 $I$，我们可以得到已知量 $E+M$。</p>
<p>而另一方面，</p>
<p>$$ \log_2 x=\log_2 {2^E \times (1+M)} $$<br>$$ = E + \log_2(1+M) $$</p>
<p>记</p>
<p>$$ F(M) = M $$<br>$$ G(M) = \log_2(1+M) $$</p>
<p><strong>注意到：当 $M\in[0,1)$ 时，$G(M)\in[0,1)$。同时观察图像可以发现 $G(M)$ 在此区间接近线性。</strong></p>
<p><img src="/assets/log2_1_x_and_x.png" alt=""></p>
<p>因此，我们可以通过上下移动 $F(M)$ 对 $G(M)$ 进行线性拟合。</p>
<p>注意一下这个例子中的误差衡量标准：<strong>我们在尽量减小误差的同时，也要保证误差分布尽量均匀，从而最大误差要尽量小</strong>。</p>
<p>综合以上考虑，问题最终化简为：</p>
<blockquote>
<p>找到一个$\sigma$，对$\Delta(M)=\log_2(1+M)-(M+\sigma)$，使$|\Delta(M)|$在 $[0,1)$ 上的最大值尽量小。</p>
</blockquote>
<p>易知：$|\Delta(0)|=|\Delta(1)|=\sigma$，另一个可能的极值点$M_0$满足$\Delta’(M_0)=0$，解得$M_0={1\over \ln 2}$</p>
<p>当 $max\{|\Delta(M)|\}$ 最小时，必有：$\Delta(M_0)=\sigma$。解得 $\sigma=0.0430356660279671$。</p>
<p>从而我们有：$ \log_2 (1+M) \approx M+\sigma $。进而：</p>
<p>$$ I=2^{23}\times(E+M+127)$$<br>$$ = 2^{23}\times(127+E+M+\sigma-\sigma)$$<br>$$ \approx 2^{23}\times(127-\sigma+(E+\log_2(1+M)))$$<br>$$ = 2^{23}\times(127-\sigma+\log_2 x)$$</p>
<p>则有：</p>
<p>$$ {I \over 2^{23}} + \sigma - 127 \approx \log_2 x $$</p>
<p>记 $r={1 \over {\sqrt x} }$ 对应的 $I$ 值为 $I_r$。则：</p>
<p>$$ I_r \approx 2^{23}\times(127-\sigma+\log_2 m)$$<br>$$ = 2^{23}\times(127-\sigma-{1 \over 2}{\log_2 x})$$<br>$$ \approx 2^{23}\times(127-\sigma-{1 \over 2}({I \over 2^{23}} + \sigma - 127))$$<br>$$ = (127-\sigma)\times 2^{22} \times 3-{I \over 2}$$<br>$$ = (5f37bcb6)_{16}-{I \over 2}$$<br>$$ = (5f37bcb6)_{16}-(I &gt;&gt; 1)$$</p>
<p>这便是步骤 3 的那个式子，神秘的常数终于出现了。</p>
<p>这里算得的常数和原算法有一些不同，主要是 $\sigma$ 取值差异造成的。如果我们取 $\sigma= 0.0450466$ 就会得到原算法的常数。作者选取了后者，应该是综合考虑了牛顿法迭代产生的误差。我没有深入研究——事实上，在允许范围内微调 $\sigma$ 不会对精度和速度产生显著影响。</p>
<h2 id="Lomont-的另一种诠释"><a href="#Lomont-的另一种诠释" class="headerlink" title="Lomont 的另一种诠释"></a>Lomont 的另一种诠释</h2><p>尽管 Quake Engine 的源码直到 2005 年才被公开，这个算法却在 2000 年左右即为人们所知。2003 年，数学家 Chris Lomont 曾写过一篇<a href="http://www.lomont.org/Math/Papers/2003/InvSqrt.pdf" target="_blank" rel="external">论文</a>讨论过这一常数的由来。他采用了另一种不同的方法。</p>
<p>Lomont 也发现 <code>i  = 0x5f3759df - ( i &gt;&gt; 1 )</code> 实为 $1 \over {\sqrt x}$ 的粗略逼近，但他没有深究这个式子背后的数学含义，而是将此常数泛化为一个变量 $R$，通过最小化其与 $1 \over {\sqrt x}$ 的误差，反解出常数 $R$，其中运用的一些技巧值得品味。</p>
<p>有趣的是，他在论文中提供了另一个他认为最佳的常数 <code>5f375a86</code>，并通过实测证实了他的想法。但此常数只是以毫厘之差险胜原算法（一次迭代 $0.175228$ vs $0.175124$，二次迭代 $4.66 \times 10^{-4}$ vs $4.65437 \times 10^{-4}$）。Lomont 在论文中表达了他的惊讶，这个实验同时也使那位不知名的作者（传闻说是 Quake 的作者 Carmack，然而他自己不承认）更加扑朔迷离。</p>
<h2 id="总结-amp-推广"><a href="#总结-amp-推广" class="headerlink" title="总结 &amp; 推广"></a>总结 &amp; 推广</h2><p>这个算法的核心其实很简单：先使用某种手段近似估计解，再用牛顿法迭代增加精确度。同时，它也启示我们： <strong>浮点数表示和对数之间存在某种联系</strong>，而对数恰是我们简化计算的利器。</p>
<p>按照这个思路，我们可以拓展出立方根倒数的对应算法，也可以将 32 位算法改写为 64 位（Lomont 在他的论文中提供了 64 位的常数 <code>0x5fe6ec85e7de30da</code>）。现如今，这些算法的加速效应已经不太明显了，但深入剖析上古神话背后的理论，也未尝不是件有趣的事。</p>
<blockquote>
<p>也许<br>他真的是神</p>
</blockquote>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><ul>
<li><a href="https://en.wikipedia.org/wiki/Fast_inverse_square_root" target="_blank" rel="external">Fast inverse square root - Wikipedia</a></li>
<li><a href="http://www.lomont.org/Math/Papers/2003/InvSqrt.pdf" target="_blank" rel="external">FAST INVERSE SQUARE ROOT - CHRIS LOMONT</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;神的时代已离去&lt;br&gt;神的故事却化为传说&lt;br&gt;流落凡间&lt;br&gt;供凡人传颂、膜拜&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;这是什么&quot;&gt;&lt;a href=&quot;#这是什么&quot; class=&quot;headerlink&quot; title=&quot;这是什么&quot;&gt;&lt;/a&gt;这是什么&lt;/h2&gt;&lt;p&gt;在上世纪 90 年代，出现过一款不可思议的游戏——雷神之锤（Quake series）。除了优秀的情节设定和精美的画面，最让人称道的莫过于它的运行效率——要知道在那个计算机配置低下的时代，一段小动画都是一个奇迹，但 Quake 却能流畅地运行于各种配置的电脑上。&lt;/p&gt;
&lt;p&gt;直至 2005 年，当 Quake Engine 开源时，Quake 系列的秘密才被揭开。在代码库中，人们发现了许多堪称神来之笔的算法。它们以极其变态的高效率，压榨着计算机的性能，进而才支撑起了 90 年代 3D 游戏的传奇。其中的某些算法，甚至比系统原生的实现还要快！&lt;/p&gt;
&lt;p&gt;我们今天的主角——快速平方根倒数算法（Fast Inverse Square Root）就是其中一个。&lt;/p&gt;
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Fast Inverse Square Root Algorithm" scheme="http://blog.hsfzxjy.site/tags/Fast-Inverse-Square-Root-Algorithm/"/>
    
  </entry>
  
  <entry>
    <title>神坑·Python 装饰类无限递归</title>
    <link href="http://blog.hsfzxjy.site/recursively-calling-when-decorating-a-python-class/"/>
    <id>http://blog.hsfzxjy.site/recursively-calling-when-decorating-a-python-class/</id>
    <published>2016-07-29T16:00:00.000Z</published>
    <updated>2018-03-08T17:23:20.761Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>《神坑》系列将会不定期更新一些可遇而不可求的坑<br>防止他人入坑，也防止自己再次入坑</p>
</blockquote>
<h2 id="简化版问题"><a href="#简化版问题" class="headerlink" title="简化版问题"></a>简化版问题</h2><p>现有两个 <code>View</code> 类：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">View</span><span class="params">(object)</span>:</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">method</span><span class="params">(self)</span>:</span></div><div class="line">        <span class="comment"># Do something...</span></div><div class="line">        <span class="keyword">pass</span></div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">ChildView</span><span class="params">(View)</span>:</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">method</span><span class="params">(self)</span>:</span></div><div class="line">        <span class="comment"># Do something else ...</span></div><div class="line">        super(ChildView, self).method()</div></pre></td></tr></table></figure>
<p>以及一个用于修饰该类的装饰器函数 <code>register</code>——用于装饰类的装饰器很常见（如 <code>django.contrib.admin</code> 的 <code>register</code>），通常可极大地减少定义相似类时的工作量：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">Mixin</span><span class="params">(object)</span>:</span></div><div class="line">    <span class="keyword">pass</span></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">register</span><span class="params">(cls)</span>:</span></div><div class="line"></div><div class="line">    <span class="keyword">return</span> type(</div><div class="line">        <span class="string">'DecoratedView'</span>,</div><div class="line">        (Mixin, cls),</div><div class="line">        &#123;&#125;</div><div class="line">    )</div></pre></td></tr></table></figure>
<p>这个装饰器为被装饰类附加上一个额外的父类 <code>Mixin</code>，以增添自定义的功能。</p>
<p>完整的代码如下：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">Mixin</span><span class="params">(object)</span>:</span></div><div class="line">    <span class="keyword">pass</span></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">register</span><span class="params">(cls)</span>:</span></div><div class="line"></div><div class="line">    <span class="keyword">return</span> type(</div><div class="line">        cls.__name__,</div><div class="line">        (Mixin, cls),</div><div class="line">        &#123;&#125;</div><div class="line">    )</div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">View</span><span class="params">(object)</span>:</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">method</span><span class="params">(self)</span>:</span></div><div class="line">        <span class="comment"># Do something...</span></div><div class="line">        <span class="keyword">pass</span></div><div class="line"></div><div class="line"><span class="meta">@register</span></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">ChildView</span><span class="params">(View)</span>:</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">method</span><span class="params">(self)</span>:</span></div><div class="line">        <span class="comment"># Do something else ...</span></div><div class="line">        super(ChildView, self).method()</div></pre></td></tr></table></figure>
<p>看上去似乎没什么问题。然而一旦调用 <code>View().method()</code>，却会报出诡异的 无限递归 错误：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># ...</span></div><div class="line">File <span class="string">"test.py"</span>, line <span class="number">23</span>, <span class="keyword">in</span> method</div><div class="line">  super(ChildView, self).method()</div><div class="line">File <span class="string">"test.py"</span>, line <span class="number">23</span>, <span class="keyword">in</span> method</div><div class="line">  super(ChildView, self).method()</div><div class="line">File <span class="string">"test.py"</span>, line <span class="number">23</span>, <span class="keyword">in</span> method</div><div class="line">  super(ChildView, self).method()</div><div class="line">RuntimeError: maximum recursion depth exceeded <span class="keyword">while</span> calling a Python object</div></pre></td></tr></table></figure>
<p>【一脸懵逼】</p>
<h2 id="猜想-amp-验证"><a href="#猜想-amp-验证" class="headerlink" title="猜想 &amp; 验证"></a>猜想 &amp; 验证</h2><p>从 Traceback 中可以发现：是 <code>super(ChildView, self).method()</code> 在不停地调用自己——这着实让我吃了一惊，因为 <strong>按理说 <code>super</code> 应该沿着继承链查找父类</strong>，可为什么在这里 <code>super</code> 神秘地失效了呢？</p>
<p>为了验证 <code>super(...).method</code> 的指向，可以尝试将该语句改为 <code>print(super(ChildView, self).method)</code>，并观察结果：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">&lt;bound method ChildView.method of &lt;__main__.ChildView object at <span class="number">0xb70fec6c</span>&gt;&gt;</div></pre></td></tr></table></figure>
<p>输出表明： <strong><code>method</code> 的指向确实有误</strong>，此处本应为 <code>View.method</code>。</p>
<p><code>super</code> 是 python 内置方法，肯定不会出错。那，会不会是 <code>super</code> 的参数有误呢？</p>
<p><code>super</code> 的签名为 <code>super(cls, instance)</code>，宏观效果为 <strong>遍历 <code>cls</code> 的继承链查找父类方法，并以 <code>instance</code> 作为 <code>self</code> 进行调用</strong>。如今查找结果有误，说明 <strong>继承链是错误的</strong>，因而极有可能是 <code>cls</code> 出错。</p>
<p>因此，有必要探测一下 <code>ChildView</code> 的指向。在 <code>method</code> 中加上一句： <code>print(ChildView)</code>：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">__main__</span>.<span class="title">DecoratedView</span>'&gt;</span></div></pre></td></tr></table></figure>
<p>原来，作用域中的 <code>ChildView</code> 已经被改变了。</p>
<h2 id="真相"><a href="#真相" class="headerlink" title="真相"></a>真相</h2><p>一切都源于装饰器语法糖。我们回忆一下装饰器的等价语法：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="meta">@decorator</span></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">Class</span>:</span></div><div class="line">    <span class="keyword">pass</span></div></pre></td></tr></table></figure>
<p>等价于</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">Class</span>:</span></div><div class="line">    <span class="keyword">pass</span></div><div class="line"></div><div class="line">Class = decorator(Class)</div></pre></td></tr></table></figure>
<p>这说明：<strong>装饰器会更改该作用域内被装饰名称的指向</strong>。</p>
<p>这本来没什么，但和 <code>super</code> 一起使用时却会出问题。通常情况下我们会将本类的名称传给 <code>super</code>（在这里为 <code>ChildView</code>），而本类名称和装饰器语法存在于同一作用域中，从而在装饰时被一同修改了（在本例中指向了子类 <code>DecoratedView</code>），进而使 <code>super(...).method</code> 指向了 <code>DecoratedView</code> 的最近祖先也就是 <code>ChildView</code> 自身的 <code>method</code> 方法，导致递归调用。</p>
<h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><p>找到了病因，就不难想到解决方法了。核心思路就是：<strong>不要更改被装饰名称的引用</strong>。</p>
<p>如果你只是想在内部使用装饰后的新类，可以在装饰器方法中使用 <code>DecoratedView</code>，而在装饰器返回时 <code>return cls</code>，以保持引用不变：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">register</span><span class="params">(cls)</span>:</span></div><div class="line"></div><div class="line">    decorated = type(</div><div class="line">        <span class="string">'DecoratedView'</span>,</div><div class="line">        (Mixin, cls),</div><div class="line">        &#123;&#125;</div><div class="line">    )</div><div class="line"></div><div class="line">    <span class="comment"># Do something with decorated</span></div><div class="line"></div><div class="line">    <span class="keyword">return</span> cls</div></pre></td></tr></table></figure>
<p>这种方法的缺点是：从外部无法使用 <code>ChildView.another_method</code> 调用 <code>Mixin</code> 上的方法。可如果真的有这样的需求，可以采用另一个解决方案：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">register</span><span class="params">(cls)</span>:</span></div><div class="line"></div><div class="line">    cls.another_method = Mixin.another_method</div><div class="line">    <span class="keyword">return</span> cls</div></pre></td></tr></table></figure>
<p>即通过赋值的方式为 <code>cls</code> 添加 <code>Mixin</code> 上的新方法，缺点是较为繁琐。</p>
<p>两种方法各有利弊，要根据实际场景权衡使用。</p>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;《神坑》系列将会不定期更新一些可遇而不可求的坑&lt;br&gt;防止他人入坑，也防止自己再次入坑&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;简化版问题&quot;&gt;&lt;a href=&quot;#简化版问题&quot; class=&quot;headerlink&quot; title=&quot;简化版问
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="Python" scheme="http://blog.hsfzxjy.site/tags/Python/"/>
    
      <category term="Decorator Pattern" scheme="http://blog.hsfzxjy.site/tags/Decorator-Pattern/"/>
    
      <category term="Infinite Recursion" scheme="http://blog.hsfzxjy.site/tags/Infinite-Recursion/"/>
    
  </entry>
  
  <entry>
    <title>Python “黑魔法” 之 Encoding &amp; Decoding</title>
    <link href="http://blog.hsfzxjy.site/python-encoding-and-decoding/"/>
    <id>http://blog.hsfzxjy.site/python-encoding-and-decoding/</id>
    <published>2016-07-20T16:00:00.000Z</published>
    <updated>2018-03-08T17:23:20.729Z</updated>
    
    <content type="html"><![CDATA[<h2 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h2><blockquote>
<ul>
<li>本文为科普文</li>
<li>本文中的例子在 Ubuntu 14.04 / Python 2.7.11 下运行成功，Python 3+ 的接口有些许不同，需要读者自行转换</li>
</ul>
</blockquote>
<h2 id="引子"><a href="#引子" class="headerlink" title="引子"></a>引子</h2><p>先看一段代码：</p>
<p><code>example.py</code>：<br><figure class="highlight gauss"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="meta"># -*- coding=yi -*-</span></div><div class="line"></div><div class="line">从 math 导入 <span class="built_in">sin</span>, <span class="built_in">pi</span></div><div class="line"></div><div class="line">打印 '<span class="built_in">sin</span>(<span class="built_in">pi</span>) =', <span class="built_in">sin</span>(<span class="built_in">pi</span>)</div></pre></td></tr></table></figure></p>
<p>这是什么？！是 Python 吗？可以运行吗？——想必你会问。</p>
<p>我可以明确告诉你：这不是 Python，<strong>但它可以用 Python 解释器运行</strong>。当然，如果你愿意，可以叫它 “Yython” （易语言 + Python）。</p>
<p><img src="//sf.gg/img/bVzuHw" alt=""></p>
<p>怎么做到的？也许你已经注意到第一行的奇怪注释——没错，秘密全在这里。</p>
<p>这种黑魔法，还要从 <a href="https://www.python.org/dev/peps/pep-0263/" target="_blank" rel="external">PEP 263</a> 说起。</p>
<a id="more"></a>
<h2 id="古老的-PEP-263"><a href="#古老的-PEP-263" class="headerlink" title="古老的 PEP 263"></a>古老的 PEP 263</h2><p>我相信 99% 的中国 Python 开发者都曾经为一个问题而头疼——字符编码。那是每个初学者的梦靥。</p>
<p>还记得那天吗？当你试图用代码向它示好：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">print</span> <span class="string">'你好'</span></div></pre></td></tr></table></figure>
<p>它却给你当头一棒：</p>
<figure class="highlight applescript"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">SyntaxError: Non-<span class="built_in">ASCII character</span> '\xe4' <span class="keyword">in</span> <span class="built_in">file</span> chi.py <span class="keyword">on</span> line <span class="number">1</span>, <span class="keyword">but</span> no encoding declared</div></pre></td></tr></table></figure>
<p>【一脸懵逼】</p>
<p>于是，你上网查找解决方案。很快，你便有了答案：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># -*- coding=utf-8 -*-</span></div><div class="line"></div><div class="line"><span class="keyword">print</span> <span class="string">'你好'</span></div></pre></td></tr></table></figure>
<p>其中第一行的注释用于指定解析该文件的编码。</p>
<p>这个特新来自 2001 年的 <a href="https://www.python.org/dev/peps/pep-0263/" target="_blank" rel="external">PEP 263 – Defining Python Source Code Encodings</a>，它的出现是为了解决一个反响广泛的问题：</p>
<blockquote>
<p>In Python 2.1, Unicode literals can only be written using the<br>    Latin-1 based encoding “unicode-escape”. This makes the<br>    programming environment rather unfriendly to Python users who live<br>    and work in non-Latin-1 locales such as many of the Asian<br>    countries. Programmers can write their 8-bit strings using the<br>    favorite encoding, but are bound to the “unicode-escape” encoding<br>    for Unicode literals.</p>
</blockquote>
<p>Python 默认用 ASCII 编码解析文件，给 15 年前的非英文世界开发者造成了不小的困扰——看来 Guido 老爹有些个人主义，设计时只考虑到了英文世界。</p>
<p>提案者设想：<strong>使用一种特殊的文件首注释，用于指定代码的编码</strong>。这个注释的正则原型是这样的：</p>
<figure class="highlight markdown"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">^[<span class="string"> \t\v</span>]<span class="emphasis">*#.*</span>?coding[<span class="string">:=</span>][<span class="symbol"> \t</span>]*([-_.a-zA-Z0-9]+)</div></pre></td></tr></table></figure>
<p>也就是说 <code># -*- coding=utf-8 -*-</code> 并不是唯一的写法，只是 Emacs 推荐写法而已。诸如 <code># coding=utf-8</code>、<code># encoding: utf-8</code> 都是合法的——因此你不必惊讶于他人编码声明与你不同。</p>
<p>正则的捕获组 <code>([-_.a-zA-Z0-9]+)</code> 将会被用作查找编码的名称，查找到的编码信息会被用于解码文件。也就是说，<code>import example</code> 背后其实相当于有如下转换过程：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">with</span> open(<span class="string">'example.py'</span>, <span class="string">'r'</span>) <span class="keyword">as</span> f:</div><div class="line">    content = f.read()</div><div class="line">    encoding = extract_encoding_info(content) <span class="comment"># 解析首注释</span></div><div class="line">    exec(content.decode(encoding))</div></pre></td></tr></table></figure>
<p>问题其实又回到我们常用的 <code>str.encode</code> 和 <code>str.decode</code> 上来了。</p>
<p>可 Python 怎么这么强大？！几乎所有编码它都认得！这是怎么做到的？是标准库？还是内置于解释器中？</p>
<p>一切，都是 <code>codecs</code> 模块在起作用。</p>
<h2 id="codecs"><a href="#codecs" class="headerlink" title="codecs"></a>codecs</h2><p><code>codecs</code> 算是较为冷门的一个模块，更为常用的是 <code>str</code> 的 <code>encode</code>/<code>decode</code> 的方法——但它们本质都是对 <code>codecs</code> 的调用。</p>
<p>打开 <code>/path/to/your/python/lib/encodings/</code> 目录，你会发现有许多以编码名称命名的 <code>.py</code> 文件，如 <code>utf_8.py</code>、<code>latin_1.py</code>。这些都是系统预定义的编码系统，实现了应对各种编码的逻辑——也就是说：编码系统其实也是普通的模块。</p>
<p>除了内置的编码，用户也可以 <strong>自行定义编码系统</strong>。<code>codecs</code> 暴露了一个 <code>register</code> 函数，用于注册自定义编码。<code>register</code> 签名如下：</p>
<blockquote>
<p><strong>codecs.register(search_function)</strong><br>Register a codec search function. Search functions are expected to take one argument, the encoding name in all lower case letters, and return a CodecInfo object having the following attributes:</p>
<ul>
<li>name: The name of the encoding;</li>
<li>encode: The stateless encoding function;</li>
<li>decode: The stateless decoding function;</li>
<li>incrementalencoder: An incremental encoder class or factory function;</li>
<li>incrementaldecoder: An incremental decoder class or factory function;</li>
<li>streamwriter: A stream writer class or factory function;</li>
<li>streamreader: A stream reader class or factory function.</li>
</ul>
</blockquote>
<p><code>encode</code> 和 <code>decode</code> 是无状态的编码/解码的函数，简单说就是：前一个被编解码的字符串与后一个没有关联。如果你想用 <code>codecs</code> 系统进行语法树解析，解析逻辑最好不要写在这里，因为代码的连续性无法被保证；<code>incremental*</code> 则是有状态的解析类，能弥补 <code>encode</code>、<code>decode</code> 的不足；<code>stream*</code> 是流相关的解析类，行为通常与 <code>encode</code>/<code>decode</code> 相同。</p>
<p>关于这六个对象的具体写法，可以参考 <code>/path/to/your/python/lib/encodings/rot_13.py</code>，该文件实现了一个简单的密码系统。</p>
<p>那么，是时候揭开真相了。</p>
<h2 id="所谓的-“Yython”"><a href="#所谓的-“Yython”" class="headerlink" title="所谓的 “Yython”"></a>所谓的 “Yython”</h2><p>黑魔法其实并不神秘，照猫画虎定义好相应的接口即可。作为例子，这里只处理用到的关键字：</p>
<p><code>yi.py</code>：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div><div class="line">64</div><div class="line">65</div><div class="line">66</div><div class="line">67</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># encoding=utf8</span></div><div class="line"></div><div class="line"><span class="keyword">import</span> codecs</div><div class="line"></div><div class="line">yi_map = &#123;</div><div class="line">    <span class="string">u'从'</span>: <span class="string">'from'</span>,</div><div class="line">    <span class="string">u'导入'</span>: <span class="string">'import'</span>,</div><div class="line">    <span class="string">u'打印'</span>: <span class="string">'print'</span></div><div class="line">&#125;</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">encode</span><span class="params">(input)</span>:</span></div><div class="line">    <span class="keyword">for</span> key, value <span class="keyword">in</span> yi_map.items():</div><div class="line">        input = input.replace(value, key)</div><div class="line"></div><div class="line">    <span class="keyword">return</span> input.encode(<span class="string">'utf8'</span>)</div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">decode</span><span class="params">(input)</span>:</span></div><div class="line">    input = input.decode(<span class="string">'utf8'</span>)</div><div class="line">    <span class="keyword">for</span> key, value <span class="keyword">in</span> yi_map.items():</div><div class="line">        input = input.replace(key, value)</div><div class="line"></div><div class="line">    <span class="keyword">return</span> input</div><div class="line"></div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">Codec</span><span class="params">(codecs.Codec)</span>:</span></div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">encode</span><span class="params">(self, input, errors=<span class="string">"strict"</span>)</span>:</span></div><div class="line">        input = encode(input)</div><div class="line"></div><div class="line">        <span class="keyword">return</span> (input, len(input))</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">decode</span><span class="params">(self, input, errors=<span class="string">"strict"</span>)</span>:</span></div><div class="line">        input = decode(input)</div><div class="line"></div><div class="line">        <span class="keyword">return</span> (input, len(input))</div><div class="line"></div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">IncrementalEncoder</span><span class="params">(codecs.IncrementalEncoder)</span>:</span></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">encode</span><span class="params">(self, input, final=False)</span>:</span></div><div class="line">        <span class="keyword">return</span> encode(input)</div><div class="line"></div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">IncrementalDecoder</span><span class="params">(codecs.IncrementalDecoder)</span>:</span></div><div class="line">    <span class="function"><span class="keyword">def</span> <span class="title">decode</span><span class="params">(self, input, final=False)</span>:</span></div><div class="line">        <span class="keyword">return</span> decode(input)</div><div class="line"></div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">StreamWriter</span><span class="params">(Codec, codecs.StreamWriter)</span>:</span></div><div class="line">    <span class="keyword">pass</span></div><div class="line"></div><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">StreamReader</span><span class="params">(Codec, codecs.StreamReader)</span>:</span></div><div class="line">    <span class="keyword">pass</span></div><div class="line"></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">def</span> <span class="title">register_entry</span><span class="params">(encoding)</span>:</span></div><div class="line">    <span class="keyword">return</span> codecs.CodecInfo(</div><div class="line">        name=<span class="string">'yi'</span>,</div><div class="line">        encode=Codec().encode,</div><div class="line">        decode=Codec().decode,</div><div class="line">        incrementalencoder=IncrementalEncoder,</div><div class="line">        incrementaldecoder=IncrementalDecoder,</div><div class="line">        streamwriter=StreamWriter,</div><div class="line">        streamreader=StreamReader</div><div class="line">    ) <span class="keyword">if</span> encoding == <span class="string">'yi'</span> <span class="keyword">else</span> <span class="keyword">None</span></div></pre></td></tr></table></figure>
<p>在命令行里注册一下，就可以看到激动人心的结果了：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> codecs, yi</div><div class="line"><span class="meta">&gt;&gt;&gt; </span>codecs.register(yi.register_entry)</div><div class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> example</div><div class="line">sin(pi) = <span class="number">1.22464679915e-16</span></div></pre></td></tr></table></figure>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>有时，对习以为常的东西深入了解一下，说不定会有惊人的发现。</p>
<h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><ul>
<li><a href="https://docs.python.org/2.7/library/codecs.html" target="_blank" rel="external">codecs - Codec registry and base classes</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;写在前面&quot;&gt;&lt;a href=&quot;#写在前面&quot; class=&quot;headerlink&quot; title=&quot;写在前面&quot;&gt;&lt;/a&gt;写在前面&lt;/h2&gt;&lt;blockquote&gt;
&lt;ul&gt;
&lt;li&gt;本文为科普文&lt;/li&gt;
&lt;li&gt;本文中的例子在 Ubuntu 14.04 / Python 2.7.11 下运行成功，Python 3+ 的接口有些许不同，需要读者自行转换&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;
&lt;h2 id=&quot;引子&quot;&gt;&lt;a href=&quot;#引子&quot; class=&quot;headerlink&quot; title=&quot;引子&quot;&gt;&lt;/a&gt;引子&lt;/h2&gt;&lt;p&gt;先看一段代码：&lt;/p&gt;
&lt;p&gt;&lt;code&gt;example.py&lt;/code&gt;：&lt;br&gt;&lt;figure class=&quot;highlight gauss&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;1&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;2&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;3&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;4&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;5&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;meta&quot;&gt;# -*- coding=yi -*-&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;从 math 导入 &lt;span class=&quot;built_in&quot;&gt;sin&lt;/span&gt;, &lt;span class=&quot;built_in&quot;&gt;pi&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;打印 &#39;&lt;span class=&quot;built_in&quot;&gt;sin&lt;/span&gt;(&lt;span class=&quot;built_in&quot;&gt;pi&lt;/span&gt;) =&#39;, &lt;span class=&quot;built_in&quot;&gt;sin&lt;/span&gt;(&lt;span class=&quot;built_in&quot;&gt;pi&lt;/span&gt;)&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;这是什么？！是 Python 吗？可以运行吗？——想必你会问。&lt;/p&gt;
&lt;p&gt;我可以明确告诉你：这不是 Python，&lt;strong&gt;但它可以用 Python 解释器运行&lt;/strong&gt;。当然，如果你愿意，可以叫它 “Yython” （易语言 + Python）。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;//sf.gg/img/bVzuHw&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
&lt;p&gt;怎么做到的？也许你已经注意到第一行的奇怪注释——没错，秘密全在这里。&lt;/p&gt;
&lt;p&gt;这种黑魔法，还要从 &lt;a href=&quot;https://www.python.org/dev/peps/pep-0263/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;PEP 263&lt;/a&gt; 说起。&lt;/p&gt;
    
    </summary>
    
      <category term="Coding" scheme="http://blog.hsfzxjy.site/categories/Coding/"/>
    
    
      <category term="codecs" scheme="http://blog.hsfzxjy.site/tags/codecs/"/>
    
      <category term="Python" scheme="http://blog.hsfzxjy.site/tags/Python/"/>
    
  </entry>
  
</feed>
